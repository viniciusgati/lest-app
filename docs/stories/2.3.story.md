---
id: "2.3"
title: "Frontend — Temas + Nota Rápida"
status: Draft
epic: 2
story: 3
created_at: "2026-03-10"
updated_at: "2026-03-10"
---

# Story 2.3 — Frontend — Temas + Nota Rápida

## Status: Draft

## Story

**Como** usuário do EduTrack,
**Quero** gerenciar os temas de cada matéria e registrar anotações rápidas por tema,
**Para que** eu possa organizar meu conteúdo detalhado e manter contexto de estudo por assunto.

---

## Contexto

Última story do Epic 2. Backend de temas pronto (Story 2.1), CRUD de matérias funcionando (Story 2.2). Esta story implementa duas telas:

1. **`/subjects/:id`** — Lista de temas da matéria com CRUD e % de acertos por tema (placeholder `—` até Epic 5)
2. **`/subjects/:id/topics/:id`** — Detalhe do tema: % de acertos, nota rápida editável, próxima revisão (SM-2), histórico de sessões (placeholder até Epic 4)

A **nota rápida** é um campo de texto livre salvo automaticamente (auto-save com debounce) ao editar.

**Dependência:** Stories 2.1 e 2.2 concluídas.

---

## Acceptance Criteria

### Tela de Temas (`/subjects/:id`)
- [ ] **AC1** — Header exibe nome da matéria com botão voltar para `/subjects`
- [ ] **AC2** — Lista os temas da matéria com % de acertos por tema (exibe `—` enquanto sem sessões)
- [ ] **AC3** — Estado vazio com CTA quando não há temas
- [ ] **AC4** — Botão "Novo Tema" abre dialog com campo de nome
- [ ] **AC5** — Criar tema atualiza lista imediatamente
- [ ] **AC6** — Cada tema tem botão editar (nome) e botão excluir com confirmação
- [ ] **AC7** — Clicar no nome do tema navega para `/subjects/:id/topics/:id`

### Tela de Detalhe do Tema (`/subjects/:id/topics/:id`)
- [ ] **AC8** — Header exibe nome do tema com botão voltar para `/subjects/:id`
- [ ] **AC9** — Exibe % de acertos do tema (placeholder `—` até Epic 5)
- [ ] **AC10** — Exibe próxima revisão sugerida (campo `next_review` do topic)
- [ ] **AC11** — Campo "Nota rápida" editável com auto-save (debounce 800ms após parar de digitar)
- [ ] **AC12** — Indicador visual de "Salvando..." e "Salvo ✓" durante/após auto-save
- [ ] **AC13** — Histórico de sessões exibe placeholder "Nenhuma sessão registrada" (Epic 4 preencherá)
- [ ] **AC14** — Erros de API exibidos via toast

### Geral
- [ ] **AC15** — Testes Jest cobrem: carregamento de temas, CRUD, auto-save da nota

---

## Tasks / Subtasks

- [ ] **Task 1** — Criar TopicService e models (AC2, AC5, AC11)
  - [ ] Criar `frontend/src/app/core/models/topic.model.ts` com interface `Topic`
  - [ ] Criar `frontend/src/app/core/services/topic.service.ts`
  - [ ] Implementar métodos: `getAll(subjectId)`, `create(subjectId, name)`, `update(subjectId, id, data)`, `delete(subjectId, id)`

- [ ] **Task 2** — Tela de temas `/subjects/:id` (AC1–AC7)
  - [ ] Criar `frontend/src/app/features/subjects/subject-detail/subject-detail.component.ts`
  - [ ] Buscar matéria pelo ID para exibir nome no header
  - [ ] Carregar temas via `TopicService.getAll(subjectId)`
  - [ ] Renderizar lista com nome e % de acertos (`—` por padrão)
  - [ ] Dialog reutilizável para criar e editar tema
  - [ ] Confirmação de exclusão via `p-confirmDialog`
  - [ ] Botão voltar: `RouterLink` para `/subjects`

- [ ] **Task 3** — Atualizar rota de `/subjects/:id` (AC1, AC7)
  - [ ] Adicionar rota `subjects/:id` em `app.routes.ts` apontando para `SubjectDetailComponent`
  - [ ] Adicionar rota `subjects/:subjectId/topics/:id` apontando para `TopicDetailComponent`

- [ ] **Task 4** — Tela de detalhe do tema (AC8–AC13)
  - [ ] Criar `frontend/src/app/features/subjects/topic-detail/topic-detail.component.ts`
  - [ ] Buscar topic pelo ID via `TopicService`
  - [ ] Exibir card de % de acertos (placeholder `—`)
  - [ ] Exibir próxima revisão formatada em português (ex: "15 de março de 2026")
  - [ ] Destacar próxima revisão em vermelho se `next_review < hoje`

- [ ] **Task 5** — Auto-save da nota rápida (AC11, AC12)
  - [ ] Campo `<textarea>` com `FormControl` para `notes`
  - [ ] Usar `RxJS debounceTime(800)` + `distinctUntilChanged()` no `valueChanges`
  - [ ] Ao disparar: chamar `TopicService.update()` apenas com `{ notes }`
  - [ ] Estado visual: `saveStatus` signal com valores `idle | saving | saved | error`
  - [ ] Exibir "Salvando..." durante request e "Salvo ✓" por 2s após sucesso

- [ ] **Task 6** — Placeholder de histórico de sessões (AC13)
  - [ ] Seção "Histórico de Sessões" com `p-card`
  - [ ] Exibir mensagem: "Nenhuma sessão registrada. Agende sua primeira revisão na Agenda."
  - [ ] Link para `/agenda`

- [ ] **Task 7** — Escrever testes Jest (AC15)
  - [ ] `topic.service.spec.ts` — testa cada método HTTP com URLs corretas
  - [ ] `subject-detail.component.spec.ts` — testa lista, criação e exclusão de temas
  - [ ] `topic-detail.component.spec.ts` — testa auto-save com debounce (usando `fakeAsync` + `tick`)

- [ ] **Task 8** — Verificação final (todos os ACs)
  - [ ] Testar fluxo: Matérias → clicar matéria → criar tema → clicar tema → editar nota
  - [ ] Verificar auto-save: digitar na nota, aguardar 800ms, confirmar "Salvo ✓"
  - [ ] Verificar `next_review` em vermelho para data passada
  - [ ] Testar em viewport mobile (375px)

---

## Dev Notes

### Interface Topic

```typescript
// frontend/src/app/core/models/topic.model.ts
export interface Topic {
  id: number;
  subject_id: number;
  name: string;
  notes: string | null;
  ease_factor: number;
  interval: number;
  next_review: string; // ISO date string 'YYYY-MM-DD'
  created_at: string;
}

// Para exibição na lista de temas — enriquecida com métricas (Epic 5)
export interface TopicWithStats extends Topic {
  accuracy_percentage: number | null; // null até haver sessões
}
```

### TopicService

```typescript
// frontend/src/app/core/services/topic.service.ts
import { Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
import { Topic } from '../models/topic.model';

@Injectable({ providedIn: 'root' })
export class TopicService {
  private url(subjectId: number) {
    return `/api/v1/subjects/${subjectId}/topics`;
  }

  constructor(private http: HttpClient) {}

  getAll(subjectId: number): Observable<Topic[]> {
    return this.http.get<Topic[]>(this.url(subjectId));
  }

  getOne(subjectId: number, id: number): Observable<Topic> {
    return this.http.get<Topic>(`${this.url(subjectId)}/${id}`);
  }

  create(subjectId: number, name: string): Observable<Topic> {
    return this.http.post<Topic>(this.url(subjectId), { topic: { name } });
  }

  update(subjectId: number, id: number, data: Partial<Pick<Topic, 'name' | 'notes'>>): Observable<Topic> {
    return this.http.put<Topic>(`${this.url(subjectId)}/${id}`, { topic: data });
  }

  delete(subjectId: number, id: number): Observable<void> {
    return this.http.delete<void>(`${this.url(subjectId)}/${id}`);
  }
}
```

### SubjectDetailComponent (tela `/subjects/:id`)

```typescript
// frontend/src/app/features/subjects/subject-detail/subject-detail.component.ts
import { Component, OnInit } from '@angular/core';
import { ActivatedRoute, RouterLink } from '@angular/router';
import { CommonModule } from '@angular/common';
import { ReactiveFormsModule, FormBuilder, Validators } from '@angular/forms';
import { ButtonModule } from 'primeng/button';
import { CardModule } from 'primeng/card';
import { DialogModule } from 'primeng/dialog';
import { ConfirmDialogModule } from 'primeng/confirmdialog';
import { ToastModule } from 'primeng/toast';
import { ProgressSpinnerModule } from 'primeng/progressspinner';
import { InputTextModule } from 'primeng/inputtext';
import { BadgeModule } from 'primeng/badge';
import { MessageService, ConfirmationService } from 'primeng/api';
import { TopicService } from '../../../core/services/topic.service';
import { SubjectService } from '../../../core/services/subject.service';
import { Topic } from '../../../core/models/topic.model';
import { Subject } from '../../../core/models/subject.model';

@Component({
  selector: 'app-subject-detail',
  standalone: true,
  imports: [
    CommonModule, RouterLink, ReactiveFormsModule,
    ButtonModule, CardModule, DialogModule, ConfirmDialogModule,
    ToastModule, ProgressSpinnerModule, InputTextModule, BadgeModule
  ],
  providers: [MessageService, ConfirmationService],
  templateUrl: './subject-detail.component.html'
})
export class SubjectDetailComponent implements OnInit {
  subject: Subject | null = null;
  topics: Topic[] = [];
  loading = false;
  dialogVisible = false;
  editingTopic: Topic | null = null;
  subjectId!: number;

  form = this.fb.group({
    name: ['', [Validators.required, Validators.maxLength(100)]]
  });

  constructor(
    private route: ActivatedRoute,
    private topicService: TopicService,
    private subjectService: SubjectService,
    private messageService: MessageService,
    private confirmationService: ConfirmationService,
    private fb: FormBuilder
  ) {}

  ngOnInit() {
    this.subjectId = Number(this.route.snapshot.paramMap.get('id'));
    this.loadSubject();
    this.loadTopics();
  }

  loadSubject() {
    // Busca nome da matéria para o header
    // SubjectService.getOne() pode ser adicionado ou usar dados em cache
  }

  loadTopics() {
    this.loading = true;
    this.topicService.getAll(this.subjectId).subscribe({
      next: (data) => { this.topics = data; this.loading = false; },
      error: () => { this.loading = false; }
    });
  }

  openCreate() {
    this.editingTopic = null;
    this.form.reset();
    this.dialogVisible = true;
  }

  openEdit(topic: Topic) {
    this.editingTopic = topic;
    this.form.patchValue({ name: topic.name });
    this.dialogVisible = true;
  }

  save() {
    if (this.form.invalid) return;
    const name = this.form.value.name!;

    const request$ = this.editingTopic
      ? this.topicService.update(this.subjectId, this.editingTopic.id, { name })
      : this.topicService.create(this.subjectId, name);

    request$.subscribe({
      next: (saved) => {
        if (this.editingTopic) {
          const idx = this.topics.findIndex(t => t.id === saved.id);
          if (idx > -1) this.topics[idx] = saved;
        } else {
          this.topics = [...this.topics, saved];
        }
        this.dialogVisible = false;
      },
      error: (err) => {
        const msg = err.error?.errors?.[0] ?? 'Erro ao salvar tema';
        this.messageService.add({ severity: 'error', summary: 'Erro', detail: msg });
      }
    });
  }

  confirmDelete(topic: Topic) {
    this.confirmationService.confirm({
      message: `Excluir "${topic.name}"? O histórico de sessões será removido.`,
      header: 'Confirmar exclusão',
      icon: 'pi pi-trash',
      accept: () => {
        this.topicService.delete(this.subjectId, topic.id).subscribe({
          next: () => { this.topics = this.topics.filter(t => t.id !== topic.id); },
          error: () => this.messageService.add({ severity: 'error', summary: 'Erro', detail: 'Erro ao excluir tema' })
        });
      }
    });
  }
}
```

### TopicDetailComponent (tela `/subjects/:subjectId/topics/:id`)

```typescript
// frontend/src/app/features/subjects/topic-detail/topic-detail.component.ts
import { Component, OnInit, OnDestroy, signal } from '@angular/core';
import { ActivatedRoute, RouterLink } from '@angular/router';
import { CommonModule } from '@angular/common';
import { ReactiveFormsModule, FormControl } from '@angular/forms';
import { Subject } from 'rxjs';
import { debounceTime, distinctUntilChanged, takeUntil } from 'rxjs/operators';
import { CardModule } from 'primeng/card';
import { ButtonModule } from 'primeng/button';
import { TextareaModule } from 'primeng/textarea';
import { ToastModule } from 'primeng/toast';
import { MessageService } from 'primeng/api';
import { TopicService } from '../../../core/services/topic.service';
import { Topic } from '../../../core/models/topic.model';

type SaveStatus = 'idle' | 'saving' | 'saved' | 'error';

@Component({
  selector: 'app-topic-detail',
  standalone: true,
  imports: [CommonModule, RouterLink, ReactiveFormsModule,
            CardModule, ButtonModule, TextareaModule, ToastModule],
  providers: [MessageService],
  templateUrl: './topic-detail.component.html'
})
export class TopicDetailComponent implements OnInit, OnDestroy {
  topic: Topic | null = null;
  subjectId!: number;
  topicId!: number;
  notesControl = new FormControl('');
  saveStatus = signal<SaveStatus>('idle');
  private destroy$ = new Subject<void>();

  get isOverdue(): boolean {
    if (!this.topic) return false;
    return new Date(this.topic.next_review) < new Date(new Date().toDateString());
  }

  get nextReviewLabel(): string {
    if (!this.topic) return '—';
    return new Date(this.topic.next_review + 'T00:00:00')
      .toLocaleDateString('pt-BR', { day: 'numeric', month: 'long', year: 'numeric' });
  }

  constructor(
    private route: ActivatedRoute,
    private topicService: TopicService,
    private messageService: MessageService
  ) {}

  ngOnInit() {
    this.subjectId = Number(this.route.snapshot.paramMap.get('subjectId'));
    this.topicId = Number(this.route.snapshot.paramMap.get('id'));
    this.loadTopic();
    this.setupAutoSave();
  }

  loadTopic() {
    this.topicService.getOne(this.subjectId, this.topicId).subscribe({
      next: (topic) => {
        this.topic = topic;
        this.notesControl.setValue(topic.notes ?? '', { emitEvent: false });
      }
    });
  }

  setupAutoSave() {
    this.notesControl.valueChanges.pipe(
      debounceTime(800),
      distinctUntilChanged(),
      takeUntil(this.destroy$)
    ).subscribe(notes => {
      this.saveStatus.set('saving');
      this.topicService.update(this.subjectId, this.topicId, { notes: notes ?? '' }).subscribe({
        next: () => {
          this.saveStatus.set('saved');
          setTimeout(() => this.saveStatus.set('idle'), 2000);
        },
        error: () => {
          this.saveStatus.set('error');
          this.messageService.add({ severity: 'error', summary: 'Erro', detail: 'Erro ao salvar nota' });
        }
      });
    });
  }

  ngOnDestroy() {
    this.destroy$.next();
    this.destroy$.complete();
  }
}
```

### Template TopicDetail (`topic-detail.component.html`)

```html
<div class="topic-detail p-4">
  <p-toast />

  <div class="flex align-items-center gap-2 mb-4">
    <p-button icon="pi pi-arrow-left" [text]="true" [routerLink]="['/subjects', subjectId]" />
    <h2 class="m-0">{{ topic?.name }}</h2>
  </div>

  <!-- Métricas -->
  <div class="grid mb-4">
    <div class="col-6">
      <p-card>
        <div class="text-center">
          <div class="text-3xl font-bold text-color-secondary">—</div>
          <div class="text-sm text-color-secondary mt-1">% de acertos</div>
          <div class="text-xs text-color-secondary">(disponível após sessões)</div>
        </div>
      </p-card>
    </div>
    <div class="col-6">
      <p-card>
        <div class="text-center">
          <div
            class="text-lg font-bold"
            [class.text-red-500]="isOverdue"
            [class.text-color]="!isOverdue"
          >
            {{ nextReviewLabel }}
          </div>
          <div class="text-sm text-color-secondary mt-1">Próxima revisão</div>
          @if (isOverdue) {
            <div class="text-xs text-red-500">Atrasada</div>
          }
        </div>
      </p-card>
    </div>
  </div>

  <!-- Nota rápida -->
  <p-card>
    <div class="flex justify-content-between align-items-center mb-2">
      <span class="font-medium">📝 Nota rápida</span>
      <span class="text-sm text-color-secondary">
        @switch (saveStatus()) {
          @case ('saving') { Salvando... }
          @case ('saved') { Salvo ✓ }
          @case ('error') { Erro ao salvar }
        }
      </span>
    </div>
    <textarea
      pTextarea
      [formControl]="notesControl"
      rows="6"
      class="w-full"
      placeholder="Anote conceitos, fórmulas, dicas ou qualquer coisa relevante para este tema..."
      autoResize="true"
    ></textarea>
  </p-card>

  <!-- Histórico de sessões (placeholder) -->
  <p-card class="mt-4">
    <div class="flex justify-content-between align-items-center mb-3">
      <span class="font-medium">📅 Histórico de Sessões</span>
    </div>
    <div class="text-center py-4 text-color-secondary">
      <i class="pi pi-calendar text-3xl mb-2 block"></i>
      <p class="mb-2">Nenhuma sessão registrada.</p>
      <p-button
        label="Agendar revisão"
        icon="pi pi-plus"
        [text]="true"
        routerLink="/agenda"
      />
    </div>
  </p-card>
</div>
```

### Rotas atualizadas (`app.routes.ts`)

```typescript
// Dentro do shell (privado):
{
  path: 'subjects',
  children: [
    {
      path: '',
      loadComponent: () => import('./features/subjects/subjects.component')
        .then(m => m.SubjectsComponent)
    },
    {
      path: ':id',
      loadComponent: () => import('./features/subjects/subject-detail/subject-detail.component')
        .then(m => m.SubjectDetailComponent)
    },
    {
      path: ':subjectId/topics/:id',
      loadComponent: () => import('./features/subjects/topic-detail/topic-detail.component')
        .then(m => m.TopicDetailComponent)
    }
  ]
}
```

### Spec auto-save com fakeAsync

```typescript
it('auto-salva nota após 800ms de inatividade', fakeAsync(() => {
  component.notesControl.setValue('nova nota');
  tick(800);
  expect(mockTopicService.update).toHaveBeenCalledWith(
    subjectId, topicId, { notes: 'nova nota' }
  );
}));

it('não dispara save antes de 800ms', fakeAsync(() => {
  component.notesControl.setValue('digitando...');
  tick(400);
  expect(mockTopicService.update).not.toHaveBeenCalled();
}));
```

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI não está habilitado em `core-config.yaml`.
> Validação de qualidade usará processo de revisão manual.

---

## File List

| Arquivo | Ação |
|---------|------|
| `frontend/src/app/core/models/topic.model.ts` | Criar |
| `frontend/src/app/core/services/topic.service.ts` | Criar |
| `frontend/src/app/core/services/topic.service.spec.ts` | Criar |
| `frontend/src/app/features/subjects/subject-detail/subject-detail.component.ts` | Criar |
| `frontend/src/app/features/subjects/subject-detail/subject-detail.component.html` | Criar |
| `frontend/src/app/features/subjects/subject-detail/subject-detail.component.spec.ts` | Criar |
| `frontend/src/app/features/subjects/topic-detail/topic-detail.component.ts` | Criar |
| `frontend/src/app/features/subjects/topic-detail/topic-detail.component.html` | Criar |
| `frontend/src/app/features/subjects/topic-detail/topic-detail.component.spec.ts` | Criar |
| `frontend/src/app/app.routes.ts` | Modificar (rotas subject-detail e topic-detail) |

---

## Dev Agent Record

### Completion Notes
_Preenchido pelo @dev ao completar_

### Debug Log
_Referências a logs de debug se necessário_
