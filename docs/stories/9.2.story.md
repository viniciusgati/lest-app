---
id: "9.2"
title: "Backend + Frontend — Perfil do Usuário"
status: Approved
epic: 9
story: 2
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools:
  - manual-review
created_at: "2026-03-22"
updated_at: "2026-03-22"
---

# Story 9.2 — Backend + Frontend — Perfil do Usuário

## Status: Draft

## Story

**Como** usuário do EduTrack,
**Quero** poder atualizar meu nome e senha,
**Para que** eu possa corrigir informações pessoais sem precisar criar uma nova conta.

---

## Contexto

Não existe endpoint nem tela para alterar dados do perfil. O campo `name` e a `password` do usuário estão imutáveis após o cadastro. Esta story implementa um endpoint `PUT /api/v1/profile` e uma tela de configurações no frontend com formulário de perfil.

**Escopo restrito:**
- Alterar `name`
- Alterar `password` (requer `current_password` para validação)
- Alterar `email` está **fora do escopo** (requer confirmação por email — complexidade extra)

---

## Acceptance Criteria

- [ ] **AC1** — `GET /api/v1/profile` retorna dados do usuário autenticado: `{ id, name, email, created_at }`
- [ ] **AC2** — `PUT /api/v1/profile` aceita `name` e atualiza o nome do usuário
- [ ] **AC3** — `PUT /api/v1/profile` aceita `current_password + password + password_confirmation` e atualiza a senha
- [ ] **AC4** — Alterar senha requer `current_password` válida (retorna 422 se incorreta)
- [ ] **AC5** — `password` e `current_password` são campos opcionais — se não fornecidos, apenas `name` é atualizado
- [ ] **AC6** — Frontend: rota `/profile` com `ProfileComponent`
- [ ] **AC7** — Frontend: formulário com campos Nome, Senha Atual, Nova Senha, Confirmar Nova Senha
- [ ] **AC8** — Frontend: campos de senha são opcionais — se em branco, não são enviados
- [ ] **AC9** — Frontend: toast de sucesso após atualização; toast de erro em falha (senha incorreta)
- [ ] **AC10** — Link para `/profile` acessível no header ou menu de navegação
- [ ] **AC11** — RSpec cobre: atualizar nome, atualizar senha com current_password correta, falha com current_password errada

---

## Tasks / Subtasks

- [ ] **Task 1** — Backend: `ProfileController` (AC1, AC2, AC3, AC4, AC5)
  - [ ] Criar `app/controllers/api/v1/profile_controller.rb`
  - [ ] Action `show`: retorna `current_user.as_json(only: [:id, :name, :email, :created_at])`
  - [ ] Action `update`: atualiza `name` sempre; atualiza password apenas se `current_password` + `password` presentes
  - [ ] Usar `current_user.update_with_password` do Devise para mudança de senha (valida current_password)
  - [ ] Usar `current_user.update(name: params[:name])` para mudança apenas de nome

- [ ] **Task 2** — Backend: rotas (AC1, AC2)
  - [ ] Adicionar `resource :profile, only: [:show, :update]` em `config/routes.rb`

- [ ] **Task 3** — Frontend: `ProfileService` (AC1, AC2, AC3)
  - [ ] Criar `frontend/src/app/core/services/profile.service.ts`
  - [ ] `getProfile(): Observable<User>`
  - [ ] `updateProfile(data: UpdateProfileDto): Observable<User>`

- [ ] **Task 4** — Frontend: `ProfileComponent` (AC6, AC7, AC8, AC9)
  - [ ] Gerar componente standalone em `frontend/src/app/features/profile/`
  - [ ] Formulário reativo com campos: `name` (required), `currentPassword`, `password`, `passwordConfirmation`
  - [ ] Validação: `password` e `passwordConfirmation` devem coincidir (custom validator)
  - [ ] Submit: envia apenas campos preenchidos; omite campos de senha se vazios
  - [ ] Toast PrimeNG: sucesso e erro

- [ ] **Task 5** — Frontend: rota e navegação (AC6, AC10)
  - [ ] Adicionar rota `/profile` em `app.routes.ts`
  - [ ] Adicionar link "Perfil" no menu de navegação (bottom-nav ou header)

- [ ] **Task 6** — Testes RSpec (AC11)
  - [ ] `spec/requests/api/v1/profile_spec.rb`
  - [ ] GET /profile → 200 com dados do usuário
  - [ ] PUT /profile com name → 200 com nome atualizado
  - [ ] PUT /profile com current_password correto + password → 200
  - [ ] PUT /profile com current_password errado → 422

---

## Dev Notes

### ProfileController

```ruby
# app/controllers/api/v1/profile_controller.rb
module Api
  module V1
    class ProfileController < BaseController
      def show
        render json: current_user.as_json(only: [:id, :name, :email, :created_at])
      end

      def update
        if password_change_requested?
          if current_user.update_with_password(profile_params_with_password)
            render json: current_user.as_json(only: [:id, :name, :email]), status: :ok
          else
            render json: { errors: current_user.errors.full_messages }, status: :unprocessable_entity
          end
        else
          if current_user.update(name: profile_params[:name])
            render json: current_user.as_json(only: [:id, :name, :email]), status: :ok
          else
            render json: { errors: current_user.errors.full_messages }, status: :unprocessable_entity
          end
        end
      end

      private

      def password_change_requested?
        params[:current_password].present? && params[:password].present?
      end

      def profile_params
        params.permit(:name)
      end

      def profile_params_with_password
        params.permit(:name, :current_password, :password, :password_confirmation)
      end
    end
  end
end
```

### Rotas

```ruby
namespace :api do
  namespace :v1 do
    resource :profile, only: [:show, :update]
  end
end
```

### UpdateProfileDto (TypeScript)

```typescript
interface UpdateProfileDto {
  name?: string;
  current_password?: string;
  password?: string;
  password_confirmation?: string;
}
```

### Validação de senha no formulário Angular

```typescript
// Custom validator: passwords must match
function passwordMatchValidator(group: FormGroup) {
  const pw = group.get('password')?.value;
  const confirm = group.get('passwordConfirmation')?.value;
  if (pw && confirm && pw !== confirm) {
    return { passwordMismatch: true };
  }
  return null;
}
```

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled

---

## File List

| Arquivo | Ação |
|---------|------|
| `backend/app/controllers/api/v1/profile_controller.rb` | Criar |
| `backend/config/routes.rb` | Modificar |
| `backend/spec/requests/api/v1/profile_spec.rb` | Criar |
| `frontend/src/app/core/services/profile.service.ts` | Criar |
| `frontend/src/app/features/profile/profile.component.ts` | Criar |
| `frontend/src/app/features/profile/profile.component.html` | Criar |
| `frontend/src/app/app.routes.ts` | Modificar |
| `frontend/src/app/layout/` (bottom-nav ou header) | Modificar |

---

## Dev Agent Record

### Agent Model Used
_a ser preenchido_

### Completion Notes
_a ser preenchido_

### Debug Log
_a ser preenchido_

### File List
_a ser preenchido_
