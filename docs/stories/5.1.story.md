---
id: "5.1"
title: "Backend — Métricas de Desempenho"
status: Approved
epic: 5
story: 1
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools:
  - manual-review
created_at: "2026-03-10"
updated_at: "2026-03-10"
---

# Story 5.1 — Backend — Métricas de Desempenho

## Status: Ready for Review

## Story

**Como** usuário do EduTrack,
**Quero** que o backend calcule meu desempenho por matéria e tema,
**Para que** eu possa visualizar onde estou indo bem e onde preciso melhorar.

---

## Contexto

Primeira story do Epic 5. Sessões de estudo existem (Epic 4). Esta story cria endpoints que agregam os dados das sessões completadas para calcular métricas de desempenho.

Não há tabela de métricas separada — tudo é calculado sob demanda a partir das `study_sessions` completadas. O endpoint também retorna o progresso real da meta semanal atual.

**Dependência:** Epics 1–4 completos.

---

## Acceptance Criteria

### Métricas por matéria
- [x] **AC1** — `GET /api/v1/metrics/subjects` retorna lista de matérias com `accuracy_percentage` calculado
- [x] **AC2** — `accuracy_percentage` = `SUM(questions_correct) / SUM(questions_done) * 100` das sessões completadas da matéria
- [x] **AC3** — Matérias sem sessões completadas retornam `accuracy_percentage: null`

### Métricas por tema
- [x] **AC4** — `GET /api/v1/metrics/subjects/:subject_id/topics` retorna temas com `accuracy_percentage` por tema
- [x] **AC5** — Inclui histórico de sessões do tema (últimas 10, ordenadas por data desc)

### Progresso semanal
- [x] **AC6** — `GET /api/v1/metrics/weekly_progress` retorna progresso real da semana atual vs meta
- [x] **AC7** — `actual_hours` = soma de `actual_minutes / 60` das sessões completadas na semana
- [x] **AC8** — `actual_questions` = soma de `questions_done` das sessões completadas na semana
- [x] **AC9** — `actual_percentage` = média ponderada de acertos das sessões completadas na semana
- [x] **AC10** — Retorna também a meta atual (`target_*`) para o frontend exibir progresso

### Geral
- [x] **AC11** — Todos os endpoints retornam 401 sem JWT
- [x] **AC12** — RSpec cobre todos os endpoints com dados reais de sessões

---

## Tasks / Subtasks

- [x] **Task 1** — Criar MetricsController (AC1–AC10)
  - [ ] Criar `app/controllers/api/v1/metrics_controller.rb`
  - [ ] Action `subjects`: agrega por matéria
  - [ ] Action `subject_topics`: agrega por tema + histórico de sessões
  - [ ] Action `weekly_progress`: calcula progresso da semana atual

- [x] **Task 2** — Queries de agregação (AC1–AC9)
  - [ ] Query subjects: `GROUP BY subject_id` com `SUM/AVG` de sessions
  - [ ] Query topics: similar + últimas 10 sessões do tema
  - [ ] Query weekly: filtrar sessões por `scheduled_date` na semana atual

- [x] **Task 3** — Configurar rotas (AC1–AC10)
  - [ ] Adicionar em `routes.rb`:
    ```ruby
    namespace :metrics do
      get :subjects
      get 'subjects/:subject_id/topics', action: :subject_topics
      get :weekly_progress
    end
    ```

- [x] **Task 4** — Escrever testes RSpec (AC12)
  - [ ] `spec/requests/api/v1/metrics_spec.rb`
  - [ ] Criar sessões completadas via factories e verificar cálculos

---

## Dev Notes

### MetricsController

```ruby
# app/controllers/api/v1/metrics_controller.rb
module Api
  module V1
    class MetricsController < BaseController
      def subjects
        subjects = current_user.subjects.includes(:topics)

        result = subjects.map do |subject|
          sessions = completed_sessions_for_subject(subject)
          {
            id: subject.id,
            name: subject.name,
            accuracy_percentage: calculate_accuracy(sessions),
            sessions_count: sessions.count
          }
        end

        render json: result
      end

      def subject_topics
        subject = current_user.subjects.find(params[:subject_id])
        topics = subject.topics.includes(:study_sessions)

        result = topics.map do |topic|
          sessions = topic.study_sessions.where(status: 'completed').order(scheduled_date: :desc)
          {
            id: topic.id,
            name: topic.name,
            notes: topic.notes,
            next_review: topic.next_review,
            ease_factor: topic.ease_factor,
            interval: topic.interval,
            accuracy_percentage: calculate_accuracy(sessions),
            sessions_count: sessions.count,
            recent_sessions: sessions.limit(10).map { |s| session_summary(s) }
          }
        end

        render json: result
      rescue ActiveRecord::RecordNotFound
        render json: { error: 'Not found' }, status: :not_found
      end

      def weekly_progress
        week_start = Date.today.beginning_of_week(:monday)
        week_end   = week_start + 6.days

        sessions = completed_sessions_in_range(week_start, week_end)
        goal     = current_user.weekly_goals.find_by(week_start: week_start)

        actual_hours      = sessions.sum('actual_minutes').to_f / 60
        actual_questions  = sessions.sum(:questions_done)
        actual_percentage = calculate_accuracy(sessions)

        render json: {
          week_start: week_start,
          actual_hours: actual_hours.round(1),
          actual_questions: actual_questions,
          actual_percentage: actual_percentage,
          target_hours: goal&.target_hours || 0,
          target_questions: goal&.target_questions || 0,
          target_percentage: goal&.target_percentage || 0
        }
      end

      private

      def completed_sessions_for_subject(subject)
        StudySession.joins(topic: :subject)
                    .where(subjects: { id: subject.id }, status: 'completed')
      end

      def completed_sessions_in_range(from, to)
        StudySession.joins(topic: :subject)
                    .where(subjects: { user_id: current_user.id },
                           status: 'completed',
                           scheduled_date: from..to)
      end

      def calculate_accuracy(sessions)
        total_done    = sessions.sum(:questions_done)
        total_correct = sessions.sum(:questions_correct)
        return nil if total_done.zero?
        (total_correct.to_f / total_done * 100).round(1)
      end

      def session_summary(session)
        {
          id: session.id,
          scheduled_date: session.scheduled_date,
          questions_done: session.questions_done,
          questions_correct: session.questions_correct,
          accuracy: session.questions_done > 0
                    ? (session.questions_correct.to_f / session.questions_done * 100).round(1)
                    : nil
        }
      end
    end
  end
end
```

### Resposta JSON esperada

```json
// GET /api/v1/metrics/subjects
[
  { "id": 1, "name": "Matemática", "accuracy_percentage": 72.5, "sessions_count": 8 },
  { "id": 2, "name": "Português",  "accuracy_percentage": null, "sessions_count": 0 }
]

// GET /api/v1/metrics/weekly_progress
{
  "week_start": "2026-03-09",
  "actual_hours": 4.5,
  "actual_questions": 30,
  "actual_percentage": 68.0,
  "target_hours": 10.0,
  "target_questions": 50,
  "target_percentage": 75.0
}
```

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled

---

## File List

| Arquivo | Ação |
|---------|------|
| `backend/app/controllers/api/v1/metrics_controller.rb` | Criar |
| `backend/config/routes.rb` | Modificar |
| `backend/spec/requests/api/v1/metrics_spec.rb` | Criar |

---

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Completion Notes
- Ternário multi-linha Ruby causa `SyntaxError` — reescrito como `if/else` em `session_summary`
- `namespace :metrics` gera os 3 endpoints corretamente dentro de `api/v1`
- `calculate_accuracy` retorna `nil` (não 0) quando `total_done.zero?` — correto para AC3
- `completed_sessions_for_subject` usa `joins(topic: :subject).where(subjects: { id: subject.id })` para isolamento
- Fixes nos specs: (1) `topic` lazy forçado antes do GET; (2) `:completed` trait com `questions_correct: 8` conflita com `questions_done: 5` — explicitado `questions_correct: 3`
- Suite completa: Tests: 328 passed, 0 failed (+24 testes), exit 0

### Debug Log
- `SyntaxError` em metrics_controller.rb:94 — ternário `? :` multi-linha inválido em Ruby. Reescrito com `if/else`.
- Spec line 81: `body[0]` era nil — `topic` lazy não criado antes do GET. Adicionado `topic` explícito.
- Spec line 204: `questions_correct: 8 > questions_done: 5` → `RecordInvalid`. Corrigido para `questions_correct: 3`.

### File List

| Arquivo | Ação |
|---------|------|
| `backend/app/controllers/api/v1/metrics_controller.rb` | Criado |
| `backend/config/routes.rb` | Modificado |
| `backend/spec/requests/api/v1/metrics_spec.rb` | Criado |

---

## QA Results

### Revisão: Quinn (@qa) — 2026-03-16

**Gate Decision: PASS**
**Quality Score: 95/100**

**Evidências verificadas:**
- `./test.sh` → `Tests: 403 passed, 0 failed`, exit 0
- `bundle exec rspec spec/requests/api/v1/metrics_spec.rb` → `Tests: 24 passed, 0 failed`
- Todos os 12 ACs implementados e cobertos por testes

**Rastreabilidade AC:**

| AC | Resultado | Observação |
|----|-----------|------------|
| AC1 — GET /metrics/subjects | ✅ PASS | 5 testes, accuracy_percentage calculado |
| AC2 — accuracy = SUM(correct)/SUM(done)*100 | ✅ PASS | Agregação de múltiplas sessões verificada |
| AC3 — null para matérias sem sessões | ✅ PASS | `return nil if total_done.zero?` |
| AC4 — GET /metrics/subjects/:id/topics | ✅ PASS | 6 testes, isolamento por current_user |
| AC5 — recent_sessions últimas 10 | ✅ PASS | `sessions.limit(10).order(scheduled_date: :desc)` |
| AC6 — GET /metrics/weekly_progress | ✅ PASS | 10 testes, week_start correto |
| AC7 — actual_hours = sum(actual_minutes)/60 | ✅ PASS | Testado com 90min → 1.5h |
| AC8 — actual_questions = sum(questions_done) | ✅ PASS | Testado e verificado |
| AC9 — actual_percentage = média ponderada | ✅ PASS | Usa calculate_accuracy (weighted) |
| AC10 — target_* do WeeklyGoal | ✅ PASS | Testado com e sem meta definida |
| AC11 — 401 sem JWT | ✅ PASS | Testado em todos os 3 endpoints |
| AC12 — RSpec cobre tudo | ✅ PASS | 24 testes com factories e dados reais |

**Itens Informacionais (sem ação requerida):**

- **N1** — `subjects` action: `includes(:topics)` carregado mas nunca usado no bloco map. Cada `completed_sessions_for_subject` emite query adicional (N+1 latente). Sem impacto no dataset atual.
- **N2** — `subject_topics` action: `includes(:study_sessions)` eager load ignorado — filtered query emitida separadamente. Performance aceitável para escala atual.

**Story aprovada.**
