---
id: "8.3"
title: "Backend — Soft Delete em Matérias e Temas"
status: Approved
epic: 8
story: 3
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools:
  - manual-review
created_at: "2026-03-22"
updated_at: "2026-03-22"
---

# Story 8.3 — Backend — Soft Delete em Matérias e Temas

## Status: Ready for Review

## Story

**Como** usuário do EduTrack,
**Quero** que ao deletar uma matéria ou tema eles sejam marcados como excluídos (não destruídos permanentemente),
**Para que** meu histórico de sessões de estudo seja preservado e eu não perca dados importantes acidentalmente.

---

## Contexto

Atualmente `DELETE /subjects/:id` destrói permanentemente o subject e, por cascade, todos os topics e study_sessions associados. Isso é destrutivo e irreversível. Com a gem `discard` (preferida ao `paranoia` por ser mais simples e não monkey-patching do ActiveRecord), adicionamos uma coluna `discarded_at` e filtramos registros descartados automaticamente via default scope.

**Impacto:**
- `Subject` com soft delete: ao excluir, `discarded_at` é preenchido, topics e sessions permanecem no banco
- `Topic` com soft delete: mesmo comportamento
- `StudySession` NÃO precisa de soft delete (ela tem integridade via `topic_id`)
- Métricas: queries existentes precisam ignorar subjects/topics descartados

---

## Acceptance Criteria

- [ ] **AC1** — `discard` gem adicionada ao `Gemfile`
- [ ] **AC2** — Migration adiciona coluna `discarded_at:datetime` (nullable) em `subjects` e `topics`
- [ ] **AC3** — `DELETE /subjects/:id` seta `discarded_at` ao invés de destruir (retorna 204)
- [ ] **AC4** — `DELETE /subjects/:subject_id/topics/:id` seta `discarded_at` ao invés de destruir (retorna 204)
- [ ] **AC5** — `GET /subjects` e `GET /subjects/:id/topics` retornam apenas registros NÃO descartados
- [ ] **AC6** — Ao descartar um subject, todos os seus topics também são descartados (cascade soft delete)
- [ ] **AC7** — `StudySession` associada a topic descartado permanece no banco (não é afetada)
- [ ] **AC8** — Métricas (`GET /metrics/subjects`, `/metrics/subjects/:id/topics`) excluem subjects/topics descartados
- [ ] **AC9** — RSpec cobre: soft delete de subject (cascata), soft delete de topic, listagens filtradas
- [ ] **AC10** — Todos os testes existentes continuam passando (sem regressão)

---

## Tasks / Subtasks

- [x] **Task 1** — Instalar gem `discard` (AC1)
  - [x] Adicionar `gem 'discard', '~> 1.4'` ao `Gemfile`
  - [x] Executar `bundle install`

- [x] **Task 2** — Migration: adicionar `discarded_at` (AC2)
  - [x] Criar migration `add_discarded_at_to_subjects_and_topics`
  - [x] `add_column :subjects, :discarded_at, :datetime`
  - [x] `add_column :topics, :discarded_at, :datetime`
  - [x] `add_index :subjects, :discarded_at`
  - [x] `add_index :topics, :discarded_at`

- [x] **Task 3** — Incluir `Discard::Model` em Subject e Topic (AC5)
  - [x] Em `app/models/subject.rb`: `include Discard::Model` + `default_scope { kept }`
  - [x] Em `app/models/topic.rb`: `include Discard::Model` + `default_scope { kept }`

- [x] **Task 4** — Atualizar `SubjectsController#destroy` (AC3, AC6)
  - [x] Substituir `@subject.destroy` por `@subject.discard`
  - [x] Antes de descartar o subject, descartar todos os seus topics: `@subject.topics.discard_all`

- [x] **Task 5** — Atualizar `TopicsController#destroy` (AC4)
  - [x] Substituir `@topic.destroy` por `@topic.discard`

- [x] **Task 6** — Verificar e corrigir MetricsController (AC8)
  - [x] `default_scope { kept }` garante que métricas automicamente excluem descartados
  - [x] Sem alterações adicionais necessárias no MetricsController

- [x] **Task 7** — Testes RSpec (AC9, AC10)
  - [x] `spec/requests/api/v1/subjects_spec.rb`: contexto de soft delete adicionado
  - [x] `spec/requests/api/v1/topics_spec.rb`: contexto de soft delete adicionado
  - [x] Verificado que sessions do topic descartado permanecem no banco
  - [x] default_scope { kept } garante filtro automático em listagens e métricas

---

## Dev Notes

### Gemfile

```ruby
gem 'discard', '~> 1.4'
```

### Subject model

```ruby
class Subject < ApplicationRecord
  include Discard::Model
  default_scope { kept }

  belongs_to :user
  has_many :topics, dependent: :destroy  # cascade discard manual no controller
end
```

### Topic model

```ruby
class Topic < ApplicationRecord
  include Discard::Model
  default_scope { kept }

  belongs_to :subject
  has_many :study_sessions, dependent: :nullify  # sessions ficam no banco
end
```

### SubjectsController#destroy

```ruby
def destroy
  ActiveRecord::Base.transaction do
    @subject.topics.discard_all  # cascade soft delete
    @subject.discard
  end
  head :no_content
end
```

### Nota sobre `discard_all`

`discard_all` é um scope method da gem discard que seta `discarded_at` em todos os registros do scope. É equivalente a `update_all(discarded_at: Time.current)`.

### Nota sobre default_scope

Com `default_scope { kept }`, todas as queries automáticas filtrarão registros descartados. Para acessar registros descartados (admin/debug): usar `Subject.with_discarded` ou `Subject.discarded`.

### Impacto em StudySessions

`StudySession.joins(topic: :subject)` ainda funcionará porque as sessions não têm soft delete. As sessions ficam "órfãs" de topics descartados mas permanecem no banco para preservar histórico. As métricas já filtram por `subject_id` do usuário, então topics descartados (via subjects descartados) não aparecerão.

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled

---

## File List

| Arquivo | Ação |
|---------|------|
| `backend/Gemfile` | Modificar |
| `backend/db/migrate/YYYYMMDD_add_discarded_at_to_subjects_and_topics.rb` | Criar |
| `backend/app/models/subject.rb` | Modificar |
| `backend/app/models/topic.rb` | Modificar |
| `backend/app/controllers/api/v1/subjects_controller.rb` | Modificar |
| `backend/app/controllers/api/v1/topics_controller.rb` | Modificar |
| `backend/spec/requests/api/v1/subjects_spec.rb` | Modificar |
| `backend/spec/requests/api/v1/topics_spec.rb` | Modificar |

---

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Completion Notes
- `discard` gem 1.4.0 instalada
- Migration aplicada: `discarded_at` em subjects e topics com índices
- `default_scope { kept }` garante filtro automático em todas as queries (incluindo métricas)
- Cascade soft delete: `topics.discard_all` antes de `subject.discard` na transação
- StudySessions preservadas (has_many :study_sessions, dependent: :destroy em Topic — mas discard NÃO chama callbacks de destroy, sessions ficam no banco)
- Backend: 208/208 RSpec passando

### Debug Log
Nenhum blocker.

### File List
| Arquivo | Ação |
|---------|------|
| `backend/Gemfile` | Modificado |
| `backend/Gemfile.lock` | Modificado |
| `backend/db/migrate/20260324013841_add_discarded_at_to_subjects_and_topics.rb` | Criado |
| `backend/app/models/subject.rb` | Modificado |
| `backend/app/models/topic.rb` | Modificado |
| `backend/app/controllers/api/v1/subjects_controller.rb` | Modificado |
| `backend/app/controllers/api/v1/topics_controller.rb` | Modificado |
| `backend/spec/requests/api/v1/subjects_spec.rb` | Modificado |
| `backend/spec/requests/api/v1/topics_spec.rb` | Modificado |

---

## QA Results

### Revisor: Quinn — 2026-03-23

#### Rastreabilidade de ACs

| AC | Implementado | Evidência |
|----|-------------|-----------|
| AC1 — gem `discard` adicionada | ✅ | `Gemfile` + `discard` 1.4.0 instalada |
| AC2 — Migration `discarded_at` em subjects e topics | ✅ | Migration `20260324013841_add_discarded_at_to_subjects_and_topics.rb` |
| AC3 — DELETE /subjects/:id seta discarded_at | ✅ | `SubjectsController#destroy` usa `@subject.discard` |
| AC4 — DELETE /topics/:id seta discarded_at | ✅ | `TopicsController#destroy` usa `@topic.discard` |
| AC5 — Listagens retornam apenas não-descartados | ✅ | `default_scope { kept }` em Subject e Topic |
| AC6 — Cascade soft delete ao descartar subject | ✅ | `@subject.topics.discard_all` antes de `@subject.discard` em transação |
| AC7 — StudySessions preservadas | ✅ | `discard` não invoca callbacks `dependent: :destroy` — sessions permanecem |
| AC8 — Métricas excluem descartados | ✅ | `default_scope { kept }` aplicado automaticamente nas queries de métricas |
| AC9 — RSpec cobre cenários de soft delete | ✅ | Contextos adicionados em `subjects_spec.rb` e `topics_spec.rb` |
| AC10 — Sem regressão | ✅ | 208/208 RSpec passando |

#### Análise de Qualidade

**Correção:**
- Transação `ActiveRecord::Base.transaction` garante atomicidade no cascade discard ✅
- `discard_all` é `update_all` — operação bulk eficiente sem callbacks ✅
- `with_discarded` disponível para acesso admin/debug (documentado em Dev Notes) ✅

**Risco — StudySessions "órfãs":**
- Topics descartados deixam study_sessions com `topic_id` válido mas topic inacessível pelo default_scope
- Métricas fazem `joins(topic: :subject)` — topics descartados não aparecem por default_scope ✅
- Sem risco de erro em produção — comportamento correto e documentado ✅

**Cobertura:** 208/208 — sem regressão confirmada ✅

#### Decisão

**GATE: ✅ PASS**

Implementação limpa e idiomática com `discard` gem. Cascade soft delete correto, métricas automáticamente filtradas via `default_scope`. Sem regressão.

— Quinn, guardião da qualidade 🛡️
