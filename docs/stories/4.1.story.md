---
id: "4.1"
title: "Backend — Sessões de Estudo"
status: Approved
epic: 4
story: 1
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools:
  - manual-review
created_at: "2026-03-10"
updated_at: "2026-03-10"
---

# Story 4.1 — Backend — Sessões de Estudo

## Status: Ready for Review

## Story

**Como** usuário do EduTrack,
**Quero** que o backend gerencie minhas sessões de estudo,
**Para que** eu possa agendar revisões, registrar resultados e ter meu desempenho calculado automaticamente.

---

## Contexto

Primeira story do Epic 4. Esta story cria o model `StudySession` — o coração do sistema. Cada sessão vincula um tema a uma data, registra o resultado (questões + acertos) e alimenta as métricas e o algoritmo SM-2.

Ao registrar o resultado de uma sessão, o backend:
1. Atualiza `status` de `scheduled` para `completed`
2. Recalcula SM-2 do topic (`ease_factor`, `interval`, `next_review`)
3. Cria automaticamente a próxima sessão de revisão baseada no SM-2

**Dependência:** Epics 1, 2 e 3 completos.

---

## Acceptance Criteria

### CRUD básico
- [x] **AC1** — `GET /api/v1/study_sessions` retorna sessões do usuário (filtros: `?status=`, `?date_from=`, `?date_to=`)
- [x] **AC2** — `POST /api/v1/study_sessions` agenda nova sessão (status: `scheduled`)
- [x] **AC3** — `PUT /api/v1/study_sessions/:id` atualiza dados de uma sessão agendada
- [x] **AC4** — `DELETE /api/v1/study_sessions/:id` remove sessão agendada
- [x] **AC5** — Usuário não acessa sessões de outro usuário (404)

### Registro de resultado
- [x] **AC6** — `PUT /api/v1/study_sessions/:id/complete` registra resultado: `actual_minutes`, `questions_done`, `questions_correct`
- [x] **AC7** — Ao completar, `status` muda para `completed`
- [x] **AC8** — Ao completar, SM-2 do topic é recalculado e salvo (`ease_factor`, `interval`, `next_review`)
- [x] **AC9** — Ao completar, nova sessão futura é criada automaticamente com a data do SM-2 (status: `scheduled`, `auto_generated: true`)
- [x] **AC10** — `questions_correct` não pode ser maior que `questions_done`

### Status automático
- [x] **AC11** — Sessões com `scheduled_date < hoje` e `status: scheduled` são retornadas com `status: late` na resposta JSON (calculado, não persistido)

### Validações
- [x] **AC12** — `scheduled_date` obrigatório; `topic_id` obrigatório e deve pertencer ao usuário
- [x] **AC13** — `expected_minutes` >= 1; `questions_done` >= 0; `questions_correct` >= 0

### Testes
- [x] **AC14** — RSpec cobre todos os endpoints, cálculo SM-2 e criação automática da próxima sessão

---

## Tasks / Subtasks

- [x] **Task 1** — Criar migration e model StudySession (AC1–AC5, AC10–AC13)
  - [x] Criar migration `create_study_sessions`
  - [x] Executar `rails db:migrate`
  - [x] Criar `app/models/study_session.rb` com validações e `status_display`
  - [x] Criar factory `spec/factories/study_sessions.rb` com traits `:completed` e `:late`

- [x] **Task 2** — Implementar SM-2 como service (AC8, AC9)
  - [x] Criar `app/services/sm2_calculator.rb`
  - [x] `quality_score`: < 40% → 1, 40–59% → 2, 60–74% → 3, 75–84% → 4, >= 85% → 5
  - [x] `calculate`: aplica SM-2, retorna `{ ease_factor, interval, next_review }`

- [x] **Task 3** — Criar StudySessionsController (AC1–AC7)
  - [x] Actions: `index` (filtros status/date), `create`, `update`, `destroy`
  - [x] Escopo via `joins(topic: :subject).where(subjects: { user_id: ... })`
  - [x] `session_json` aplica `status_display` para calcular "late" em runtime

- [x] **Task 4** — Action complete (AC6–AC10)
  - [x] `complete` em transação: update session + update topic SM-2 + create next_session
  - [x] Retorna `{ session, next_session }`

- [x] **Task 5** — Rotas (AC1–AC9)
  - [x] `resources :study_sessions` com `member { put :complete }`

- [x] **Task 6** — Testes RSpec (AC14)
  - [x] `spec/services/sm2_calculator_spec.rb` — quality score + calculate
  - [x] `spec/models/study_session_spec.rb` — validações + late? + status_display
  - [x] `spec/requests/api/v1/study_sessions_spec.rb` — CRUD + complete + SM-2 + isolamento
  - [x] `bash test.sh` → Tests: 243 passed, 0 failed, exit 0

---

## Dev Notes

### Model StudySession

```ruby
# app/models/study_session.rb
class StudySession < ApplicationRecord
  belongs_to :topic

  validates :scheduled_date, presence: true
  validates :expected_minutes, numericality: { greater_than: 0 }
  validates :questions_done, numericality: { greater_than_or_equal_to: 0 }
  validates :questions_correct, numericality: { greater_than_or_equal_to: 0 }
  validates :status, inclusion: { in: %w[scheduled completed] }
  validate :questions_correct_lte_done

  delegate :subject, to: :topic

  def late?
    status == 'scheduled' && scheduled_date < Date.today
  end

  def status_display
    late? ? 'late' : status
  end

  private

  def questions_correct_lte_done
    return unless questions_done && questions_correct
    if questions_correct > questions_done
      errors.add(:questions_correct, 'não pode ser maior que questões feitas')
    end
  end
end
```

### SM2Calculator Service

```ruby
# app/services/sm2_calculator.rb
class Sm2Calculator
  MIN_EASE = 1.3

  def self.calculate(topic, questions_done, questions_correct)
    q = quality_score(questions_done, questions_correct)

    ease = topic.ease_factor
    interval = topic.interval

    if q >= 3
      interval = case interval
                 when 1 then 6
                 when 6 then (interval * ease).round
                 else (interval * ease).round
                 end
      ease = [ease + (0.1 - (5 - q) * (0.08 + (5 - q) * 0.02)), MIN_EASE].max
    else
      interval = 1
    end

    {
      ease_factor: ease.round(2),
      interval: interval,
      next_review: Date.today + interval.days
    }
  end

  def self.quality_score(questions_done, questions_correct)
    return 0 if questions_done.zero?
    pct = (questions_correct.to_f / questions_done * 100).round
    case pct
    when 85..100 then 5
    when 75..84  then 4
    when 60..74  then 3
    when 40..59  then 2
    else              1
    end
  end
end
```

### StudySessionsController

```ruby
# app/controllers/api/v1/study_sessions_controller.rb
module Api
  module V1
    class StudySessionsController < BaseController
      before_action :set_session, only: [:update, :destroy, :complete]

      def index
        sessions = user_sessions.order(scheduled_date: :asc)
        sessions = sessions.where(status: params[:status]) if params[:status].present?
        sessions = sessions.where(scheduled_date: params[:date_from]..) if params[:date_from].present?
        sessions = sessions.where(scheduled_date: ..params[:date_to]) if params[:date_to].present?
        render json: sessions.map { |s| session_json(s) }
      end

      def create
        session = StudySession.new(session_params)
        validate_topic_ownership!(session.topic_id)
        if session.save
          render json: session_json(session), status: :created
        else
          render json: { errors: session.errors.full_messages }, status: :unprocessable_entity
        end
      end

      def update
        if @session.update(session_params.except(:topic_id))
          render json: session_json(@session)
        else
          render json: { errors: @session.errors.full_messages }, status: :unprocessable_entity
        end
      end

      def destroy
        @session.destroy
        head :no_content
      end

      def complete
        result_params = params.require(:study_session).permit(
          :actual_minutes, :questions_done, :questions_correct
        )

        sm2 = Sm2Calculator.calculate(
          @session.topic,
          result_params[:questions_done].to_i,
          result_params[:questions_correct].to_i
        )

        ActiveRecord::Base.transaction do
          @session.update!(
            result_params.merge(status: 'completed')
          )
          @session.topic.update!(sm2)

          # Cria próxima sessão automaticamente
          next_session = StudySession.create!(
            topic: @session.topic,
            scheduled_date: sm2[:next_review],
            expected_minutes: @session.expected_minutes,
            auto_generated: true
          )

          render json: {
            session: session_json(@session),
            next_session: session_json(next_session)
          }
        end
      rescue ActiveRecord::RecordInvalid => e
        render json: { errors: [e.message] }, status: :unprocessable_entity
      end

      private

      def user_sessions
        StudySession.joins(topic: :subject).where(subjects: { user_id: current_user.id })
      end

      def set_session
        @session = user_sessions.find(params[:id])
      rescue ActiveRecord::RecordNotFound
        render json: { error: 'Not found' }, status: :not_found
      end

      def validate_topic_ownership!(topic_id)
        unless current_user.subjects.joins(:topics).where(topics: { id: topic_id }).exists?
          render json: { error: 'Topic not found' }, status: :not_found
        end
      end

      def session_json(session)
        session.as_json.merge('status' => session.status_display)
      end

      def session_params
        params.require(:study_session).permit(
          :topic_id, :scheduled_date, :expected_minutes
        )
      end
    end
  end
end
```

### Resposta JSON

```json
// GET /api/v1/study_sessions
[
  {
    "id": 1, "topic_id": 3,
    "scheduled_date": "2026-03-10",
    "expected_minutes": 30,
    "actual_minutes": null,
    "questions_done": 0,
    "questions_correct": 0,
    "status": "scheduled",      // ou "late" se atrasada
    "auto_generated": false
  }
]

// PUT /api/v1/study_sessions/1/complete
{
  "session": { ...sessão completada... },
  "next_session": { ...próxima sessão criada pelo SM-2... }
}
```

### Spec SM2Calculator

```ruby
RSpec.describe Sm2Calculator do
  describe '.quality_score' do
    it { expect(described_class.quality_score(10, 10)).to eq(5) }  # 100%
    it { expect(described_class.quality_score(10, 8)).to eq(4) }   # 80%
    it { expect(described_class.quality_score(10, 6)).to eq(3) }   # 60%
    it { expect(described_class.quality_score(10, 4)).to eq(2) }   # 40%
    it { expect(described_class.quality_score(10, 2)).to eq(1) }   # 20%
    it { expect(described_class.quality_score(0, 0)).to eq(0) }    # sem questões
  end

  describe '.calculate' do
    let(:topic) { build(:topic, ease_factor: 2.5, interval: 1) }

    it 'avança para intervalo 6 após primeira sessão boa' do
      result = described_class.calculate(topic, 10, 9) # 90%
      expect(result[:interval]).to eq(6)
    end

    it 'reseta intervalo para 1 após sessão ruim' do
      result = described_class.calculate(topic, 10, 2) # 20%
      expect(result[:interval]).to eq(1)
    end

    it 'define next_review corretamente' do
      result = described_class.calculate(topic, 10, 9)
      expect(result[:next_review]).to eq(Date.today + 6.days)
    end
  end
end
```

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled

---

## File List

| Arquivo | Ação |
|---------|------|
| `backend/db/migrate/*_create_study_sessions.rb` | Criar |
| `backend/app/models/study_session.rb` | Criar |
| `backend/app/services/sm2_calculator.rb` | Criar |
| `backend/app/controllers/api/v1/study_sessions_controller.rb` | Criar |
| `backend/config/routes.rb` | Modificar |
| `backend/spec/factories/study_sessions.rb` | Criar |
| `backend/spec/services/sm2_calculator_spec.rb` | Criar |
| `backend/spec/models/study_session_spec.rb` | Criar |
| `backend/spec/requests/api/v1/study_sessions_spec.rb` | Criar |

---

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Completion Notes
- `validate_topic_ownership!` do Dev Notes usa `render` sem halt — reescrito com `return render` direto no `create` para evitar double-render
- `Sm2Calculator` em `app/services/` (dir criado); `case interval when 1 then 6 / else (interval * ease).round` — `when 6` era idêntico ao `else`, simplificado
- `session_json` usa `as_json.merge('status' => status_display)` — status "late" calculado sem persistir
- Transação no `complete` garante atomicidade: se a criação da próxima sessão falhar, o update da sessão atual também é revertido
- Test fix: `session` lazy let contava +2 no change block — resolvido com eager `session` antes do expect
- Suite completa: Tests: 243 passed, 0 failed (143 backend RSpec + 103 frontend Vitest), exit 0 (45 novos testes desta story)

### Debug Log
- `change(StudySession, :count).by(1)` → changed by 2: lazy `let(:session)` criado dentro do bloco contava como +1 extra — resolvido forçando criação antes

### File List

| Arquivo | Ação |
|---------|------|
| `backend/db/migrate/20260315222716_create_study_sessions.rb` | Criado |
| `backend/app/models/study_session.rb` | Criado |
| `backend/app/models/topic.rb` | Modificado (`has_many :study_sessions`) |
| `backend/app/services/sm2_calculator.rb` | Criado |
| `backend/app/controllers/api/v1/study_sessions_controller.rb` | Criado |
| `backend/config/routes.rb` | Modificado |
| `backend/spec/factories/study_sessions.rb` | Criado |
| `backend/spec/services/sm2_calculator_spec.rb` | Criado |
| `backend/spec/models/study_session_spec.rb` | Criado |
| `backend/spec/requests/api/v1/study_sessions_spec.rb` | Criado |

---

## QA Results

**Reviewed by:** Quinn (@qa) — 2026-03-15
**Gate:** CONCERNS
**Quality Score:** 92/100

### Sumário

Implementação sólida. SM-2 correto, scoping por usuário correto, transação atômica no `complete`, 243 testes passando. Um concern de comportamento na API: o filtro `?status=late` retorna silenciosamente vazio porque `late` é um status virtual (calculado), não persistido.

### Rastreabilidade AC

| AC | Resultado | Observação |
|----|-----------|------------|
| AC1 — GET com filtros | ✅ PASS | Filtros status/date_from/date_to implementados |
| AC2 — POST agenda sessão | ✅ PASS | Status `scheduled`, ownership validado |
| AC3 — PUT atualiza sessão | ✅ PASS | `topic_id` excluído do update corretamente |
| AC4 — DELETE remove sessão | ✅ PASS | 204 no content |
| AC5 — Isolamento entre usuários | ✅ PASS | `joins(topic: :subject).where(subjects: { user_id: ... })` |
| AC6 — PUT complete registra resultado | ✅ PASS | `actual_minutes`, `questions_done`, `questions_correct` |
| AC7 — Status muda para completed | ✅ PASS | Verificado no spec e transação |
| AC8 — SM-2 recalculado ao completar | ✅ PASS | `ease_factor`, `interval`, `next_review` atualizados |
| AC9 — Próxima sessão criada automaticamente | ✅ PASS | `auto_generated: true`, data do SM-2 |
| AC10 — questions_correct <= questions_done | ✅ PASS | Validação customizada no model + teste de rejeição |
| AC11 — Status late calculado em runtime | ✅ PASS | `session_json` injeta corretamente; filtro `?status=late` corrigido (C1 resolvido) |
| AC12 — scheduled_date/topic_id obrigatórios | ✅ PASS | Validações presentes |
| AC13 — Limites numéricos | ✅ PASS | `greater_than: 0` e `greater_than_or_equal_to: 0` |
| AC14 — RSpec cobre tudo | ✅ PASS | 45 novos testes; 243 total, 0 falhas |

### Concerns

~~**C1 — Filtro `?status=late` silenciosamente vazio**~~ — **RESOLVIDO**
- Controller atualizado: apenas `completed` usa filtro DB; `scheduled` e `late` são filtrados em memória via `status_display`
- Testes adicionados: `status=late` retorna apenas atrasadas, `status=scheduled` exclui atrasadas
- 401 testes passando

### Notas Informacionais

**N1 — `render json:` dentro do bloco `ActiveRecord::Base.transaction`**
- Em Rails, `render` dentro de uma transaction é code smell
- Neste caso é seguro: `render` é a última instrução do bloco (todas as operações DB precedem)
- Sem risco de double render ou response enviada antes do commit
- Sugestão futura: mover `render` para fora do bloco, atribuindo resultado a variável local

**N2 — `:unprocessable_entity` nos specs vs `:unprocessable_content` no controller**
- Spec linha 151: `expect(response).to have_http_status(:unprocessable_entity)`
- Controller usa `:unprocessable_content` (Rack 3.x)
- Ambos mapeiam para HTTP 422 — cosmético, nenhum impacto funcional

### Evidências

- `bundle exec rspec spec/services/sm2_calculator_spec.rb` → 14 examples, 0 failures
- `bash test.sh` → Tests: 401 passed, 0 failed, exit 0
- Todos os ACs verificados via leitura de código e specs correspondentes

### Decisão

**GATE: ✅ PASS**

C1 (filtro `?status=late`) resolvido. SM-2 correto, isolamento por usuário validado, 401/401 testes passando. N1 (render dentro de transaction) e N2 (alias HTTP 422) registrados como debt informacional sem impacto funcional.

— Quinn, guardião da qualidade 🛡️
