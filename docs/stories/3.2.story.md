---
id: "3.2"
title: "Frontend — Tela de Metas Semanais"
status: Draft
epic: 3
story: 2
created_at: "2026-03-10"
updated_at: "2026-03-10"
---

# Story 3.2 — Frontend — Tela de Metas Semanais

## Status: Draft

## Story

**Como** usuário do EduTrack,
**Quero** definir e acompanhar minhas metas semanais e configurar minha disponibilidade,
**Para que** eu tenha clareza sobre o que preciso estudar na semana e o sistema possa gerar minha programação automaticamente.

---

## Contexto

Continuação da Story 3.1. Backend de metas e configuração prontos. Esta story implementa a tela `/goals` com duas seções:

1. **Meta da semana** — Formulário para definir horas, questões e % alvo + barras de progresso (placeholder até Epic 4/5)
2. **Configurações de programação** — Dias disponíveis e estratégia de agendamento automático

O progresso real (horas estudadas, questões feitas, % alcançado) será preenchido em Epic 5 quando as sessões existirem. Por ora as barras mostram apenas o alvo com progresso 0.

**Dependência:** Stories 3.1 concluída.

---

## Acceptance Criteria

### Meta da semana
- [ ] **AC1** — Tela `/goals` exibe a meta da semana atual com período (ex: "9 a 15 de março")
- [ ] **AC2** — Três campos editáveis: horas de estudo, questões a realizar, % alvo
- [ ] **AC3** — Salvar meta via botão "Salvar meta" chama `PUT /api/v1/weekly_goals/current`
- [ ] **AC4** — Três barras de progresso (`p-progressBar`) mostram meta vs realizado (realizado = 0 por enquanto)
- [ ] **AC5** — Feedback visual: barra verde se meta atingida, amarela se parcial, cinza se sem progresso
- [ ] **AC6** — Meta carregada automaticamente ao abrir a tela (GET current)
- [ ] **AC7** — Sucesso ao salvar exibe toast "Meta salva com sucesso"
- [ ] **AC8** — Erro exibe toast com mensagem da API

### Configurações de programação
- [ ] **AC9** — Seção "Programação automática" com seleção de dias disponíveis (checkboxes: Seg a Dom)
- [ ] **AC10** — Seleção de estratégia via radio buttons: "Revisão espaçada (SM-2)", "Pontos fracos", "Balanceado"
- [ ] **AC11** — Salvar configuração via botão "Salvar configurações" chama `PUT /api/v1/user_config`
- [ ] **AC12** — Configuração carregada automaticamente ao abrir a tela
- [ ] **AC13** — Ao menos 1 dia deve estar selecionado (validação client-side)

### Geral
- [ ] **AC14** — Testes Jest cobrem: carregamento, salvar meta, salvar config, validação de dias

---

## Tasks / Subtasks

- [ ] **Task 1** — Criar services e models (AC2, AC6, AC12)
  - [ ] Criar `frontend/src/app/core/models/weekly-goal.model.ts`
  - [ ] Criar `frontend/src/app/core/models/user-config.model.ts`
  - [ ] Criar `frontend/src/app/core/services/weekly-goal.service.ts`
    - Métodos: `getCurrent()`, `updateCurrent(goal)`, `getHistory()`
  - [ ] Criar `frontend/src/app/core/services/user-config.service.ts`
    - Métodos: `get()`, `update(config)`

- [ ] **Task 2** — Implementar tela de metas (AC1–AC8)
  - [ ] Atualizar `frontend/src/app/features/goals/goals.component.ts`
  - [ ] Carregar meta atual no `ngOnInit`
  - [ ] Formulário reativo com campos: `target_hours`, `target_questions`, `target_percentage`
  - [ ] Validações: todos numéricos, `target_percentage` entre 0 e 100, todos `>= 0`
  - [ ] Calcular label do período da semana: "9 a 15 de março de 2026"
  - [ ] Exibir três `p-progressBar` com valor 0 e label do alvo

- [ ] **Task 3** — Barras de progresso com status visual (AC4, AC5)
  - [ ] Criar helper `progressColor(current, target): string`
    - `current >= target` → `'success'` (verde)
    - `current > 0` → `'warning'` (amarelo)
    - `current === 0` → `''` (cinza/padrão)
  - [ ] Aplicar via propriedade `[styleClass]` do `p-progressBar`
  - [ ] Label abaixo de cada barra: "0 / 10h estudadas", "0 / 50 questões", "0% / 75% alvo"

- [ ] **Task 4** — Seção de configurações de programação (AC9–AC13)
  - [ ] Adicionar seção abaixo da meta no mesmo componente ou em tab separada
  - [ ] Checkboxes para dias: Seg, Ter, Qua, Qui, Sex, Sáb, Dom (mapeados para mon–sun)
  - [ ] Radio group para estratégia com `p-radioButton` do PrimeNG
  - [ ] Validação: ao desmarcar todos os dias, botão "Salvar configurações" fica desabilitado
  - [ ] Chamar `UserConfigService.update()` ao salvar

- [ ] **Task 5** — Escrever testes Jest (AC14)
  - [ ] `weekly-goal.service.spec.ts` — testa GET e PUT com URLs corretas
  - [ ] `user-config.service.spec.ts` — testa GET e PUT
  - [ ] `goals.component.spec.ts`:
    - Carrega meta ao iniciar
    - Salvar meta chama service com params corretos
    - Salvar config chama service com params corretos
    - Botão desabilitado quando nenhum dia selecionado

- [ ] **Task 6** — Verificação final (todos os ACs)
  - [ ] Testar fluxo: abrir tela → definir meta → salvar → verificar toast
  - [ ] Testar configuração: selecionar dias e estratégia → salvar → reabrir e verificar persistência
  - [ ] Testar validação: desmarcar todos os dias → botão desabilitado
  - [ ] Testar em viewport mobile (375px)

---

## Dev Notes

### Interfaces

```typescript
// frontend/src/app/core/models/weekly-goal.model.ts
export interface WeeklyGoal {
  id?: number;
  week_start: string;       // 'YYYY-MM-DD' (segunda-feira)
  target_hours: number;
  target_questions: number;
  target_percentage: number;
}

// Estendida com progresso real (Epic 5 vai popular)
export interface WeeklyGoalWithProgress extends WeeklyGoal {
  actual_hours: number;
  actual_questions: number;
  actual_percentage: number;
}

// frontend/src/app/core/models/user-config.model.ts
export type WeekDay = 'mon' | 'tue' | 'wed' | 'thu' | 'fri' | 'sat' | 'sun';
export type ScheduleStrategy = 'sm2' | 'weak_points' | 'balanced';

export interface UserConfig {
  id?: number;
  available_days: WeekDay[];
  schedule_strategy: ScheduleStrategy;
}
```

### WeeklyGoalService

```typescript
// frontend/src/app/core/services/weekly-goal.service.ts
import { Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
import { WeeklyGoal } from '../models/weekly-goal.model';

@Injectable({ providedIn: 'root' })
export class WeeklyGoalService {
  private readonly url = '/api/v1/weekly_goals';

  constructor(private http: HttpClient) {}

  getCurrent(): Observable<WeeklyGoal> {
    return this.http.get<WeeklyGoal>(`${this.url}/current`);
  }

  updateCurrent(goal: Partial<WeeklyGoal>): Observable<WeeklyGoal> {
    return this.http.put<WeeklyGoal>(`${this.url}/current`, { weekly_goal: goal });
  }

  getHistory(): Observable<WeeklyGoal[]> {
    return this.http.get<WeeklyGoal[]>(this.url);
  }
}
```

### UserConfigService

```typescript
// frontend/src/app/core/services/user-config.service.ts
import { Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
import { UserConfig } from '../models/user-config.model';

@Injectable({ providedIn: 'root' })
export class UserConfigService {
  private readonly url = '/api/v1/user_config';

  constructor(private http: HttpClient) {}

  get(): Observable<UserConfig> {
    return this.http.get<UserConfig>(this.url);
  }

  update(config: Partial<UserConfig>): Observable<UserConfig> {
    return this.http.put<UserConfig>(this.url, { user_config: config });
  }
}
```

### GoalsComponent

```typescript
// frontend/src/app/features/goals/goals.component.ts
import { Component, OnInit } from '@angular/core';
import { CommonModule } from '@angular/common';
import { ReactiveFormsModule, FormBuilder, Validators } from '@angular/forms';
import { ButtonModule } from 'primeng/button';
import { CardModule } from 'primeng/card';
import { ProgressBarModule } from 'primeng/progressbar';
import { ToastModule } from 'primeng/toast';
import { InputNumberModule } from 'primeng/inputnumber';
import { CheckboxModule } from 'primeng/checkbox';
import { RadioButtonModule } from 'primeng/radiobutton';
import { DividerModule } from 'primeng/divider';
import { MessageService } from 'primeng/api';
import { WeeklyGoalService } from '../../core/services/weekly-goal.service';
import { UserConfigService } from '../../core/services/user-config.service';
import { WeeklyGoal } from '../../core/models/weekly-goal.model';
import { UserConfig, WeekDay, ScheduleStrategy } from '../../core/models/user-config.model';
import { forkJoin } from 'rxjs';

@Component({
  selector: 'app-goals',
  standalone: true,
  imports: [
    CommonModule, ReactiveFormsModule,
    ButtonModule, CardModule, ProgressBarModule, ToastModule,
    InputNumberModule, CheckboxModule, RadioButtonModule, DividerModule
  ],
  providers: [MessageService],
  templateUrl: './goals.component.html'
})
export class GoalsComponent implements OnInit {
  loading = false;
  weekLabel = '';

  goalForm = this.fb.group({
    target_hours: [0, [Validators.required, Validators.min(0)]],
    target_questions: [0, [Validators.required, Validators.min(0)]],
    target_percentage: [0, [Validators.required, Validators.min(0), Validators.max(100)]]
  });

  // Progresso real (placeholder até Epic 5)
  actualHours = 0;
  actualQuestions = 0;
  actualPercentage = 0;

  readonly days: { label: string; value: WeekDay }[] = [
    { label: 'Seg', value: 'mon' }, { label: 'Ter', value: 'tue' },
    { label: 'Qua', value: 'wed' }, { label: 'Qui', value: 'thu' },
    { label: 'Sex', value: 'fri' }, { label: 'Sáb', value: 'sat' },
    { label: 'Dom', value: 'sun' }
  ];

  readonly strategies: { label: string; value: ScheduleStrategy; description: string }[] = [
    { label: 'Revisão espaçada (SM-2)', value: 'sm2', description: 'Prioriza temas com revisão vencida ou vencendo' },
    { label: 'Pontos fracos', value: 'weak_points', description: 'Prioriza temas com menor % de acertos' },
    { label: 'Balanceado', value: 'balanced', description: 'Distribui igualmente entre todas as matérias' }
  ];

  selectedDays: WeekDay[] = [];
  selectedStrategy: ScheduleStrategy = 'sm2';

  get noDaySelected(): boolean {
    return this.selectedDays.length === 0;
  }

  constructor(
    private fb: FormBuilder,
    private weeklyGoalService: WeeklyGoalService,
    private userConfigService: UserConfigService,
    private messageService: MessageService
  ) {}

  ngOnInit() {
    this.weekLabel = this.buildWeekLabel();
    this.loading = true;

    forkJoin({
      goal: this.weeklyGoalService.getCurrent(),
      config: this.userConfigService.get()
    }).subscribe({
      next: ({ goal, config }) => {
        this.goalForm.patchValue({
          target_hours: goal.target_hours,
          target_questions: goal.target_questions,
          target_percentage: goal.target_percentage
        });
        this.selectedDays = config.available_days;
        this.selectedStrategy = config.schedule_strategy;
        this.loading = false;
      },
      error: () => { this.loading = false; }
    });
  }

  saveGoal() {
    if (this.goalForm.invalid) return;
    this.weeklyGoalService.updateCurrent(this.goalForm.value as Partial<WeeklyGoal>).subscribe({
      next: () => this.messageService.add({ severity: 'success', summary: 'Sucesso', detail: 'Meta salva com sucesso' }),
      error: (err) => {
        const msg = err.error?.errors?.[0] ?? 'Erro ao salvar meta';
        this.messageService.add({ severity: 'error', summary: 'Erro', detail: msg });
      }
    });
  }

  saveConfig() {
    if (this.noDaySelected) return;
    this.userConfigService.update({
      available_days: this.selectedDays,
      schedule_strategy: this.selectedStrategy
    }).subscribe({
      next: () => this.messageService.add({ severity: 'success', summary: 'Sucesso', detail: 'Configurações salvas' }),
      error: () => this.messageService.add({ severity: 'error', summary: 'Erro', detail: 'Erro ao salvar configurações' })
    });
  }

  progressColor(current: number, target: number): string {
    if (target === 0) return '';
    if (current >= target) return 'success';
    if (current > 0) return 'warning';
    return '';
  }

  progressValue(current: number, target: number): number {
    if (target === 0) return 0;
    return Math.min(Math.round((current / target) * 100), 100);
  }

  private buildWeekLabel(): string {
    const today = new Date();
    const day = today.getDay();
    const diff = today.getDate() - day + (day === 0 ? -6 : 1);
    const monday = new Date(today.setDate(diff));
    const sunday = new Date(monday);
    sunday.setDate(monday.getDate() + 6);
    const fmt = (d: Date) => d.toLocaleDateString('pt-BR', { day: 'numeric', month: 'long' });
    return `${fmt(monday)} a ${fmt(sunday)} de ${sunday.getFullYear()}`;
  }
}
```

### Template (`goals.component.html`)

```html
<div class="goals-page p-4">
  <p-toast />

  <h2 class="mb-1">Metas da Semana</h2>
  <p class="text-color-secondary mb-4">{{ weekLabel }}</p>

  <!-- Progresso atual -->
  <p-card class="mb-4">
    <h3 class="mt-0 mb-3">Progresso atual</h3>

    <div class="mb-4">
      <div class="flex justify-content-between mb-1">
        <span>⏱ Horas de estudo</span>
        <span class="text-color-secondary">
          {{ actualHours }}h / {{ goalForm.value.target_hours }}h
        </span>
      </div>
      <p-progressBar
        [value]="progressValue(actualHours, goalForm.value.target_hours || 0)"
        [styleClass]="progressColor(actualHours, goalForm.value.target_hours || 0)"
      />
    </div>

    <div class="mb-4">
      <div class="flex justify-content-between mb-1">
        <span>📝 Questões</span>
        <span class="text-color-secondary">
          {{ actualQuestions }} / {{ goalForm.value.target_questions }} questões
        </span>
      </div>
      <p-progressBar
        [value]="progressValue(actualQuestions, goalForm.value.target_questions || 0)"
        [styleClass]="progressColor(actualQuestions, goalForm.value.target_questions || 0)"
      />
    </div>

    <div>
      <div class="flex justify-content-between mb-1">
        <span>🎯 Percentual de acertos</span>
        <span class="text-color-secondary">
          {{ actualPercentage }}% / {{ goalForm.value.target_percentage }}% alvo
        </span>
      </div>
      <p-progressBar
        [value]="progressValue(actualPercentage, goalForm.value.target_percentage || 0)"
        [styleClass]="progressColor(actualPercentage, goalForm.value.target_percentage || 0)"
      />
    </div>
  </p-card>

  <!-- Definir metas -->
  <p-card class="mb-4">
    <h3 class="mt-0 mb-3">Definir metas</h3>
    <form [formGroup]="goalForm">
      <div class="field mb-3">
        <label>Horas de estudo</label>
        <p-inputNumber
          formControlName="target_hours"
          [min]="0" [max]="168"
          suffix="h"
          class="w-full mt-1"
        />
      </div>
      <div class="field mb-3">
        <label>Questões a realizar</label>
        <p-inputNumber
          formControlName="target_questions"
          [min]="0"
          suffix=" questões"
          class="w-full mt-1"
        />
      </div>
      <div class="field mb-3">
        <label>% alvo de acertos</label>
        <p-inputNumber
          formControlName="target_percentage"
          [min]="0" [max]="100"
          suffix="%"
          class="w-full mt-1"
        />
        @if (goalForm.get('target_percentage')?.errors?.['max']) {
          <small class="p-error">Máximo 100%</small>
        }
      </div>
      <p-button
        label="Salvar meta"
        icon="pi pi-check"
        class="w-full"
        [disabled]="goalForm.invalid"
        (onClick)="saveGoal()"
      />
    </form>
  </p-card>

  <p-divider />

  <!-- Configuração de programação automática -->
  <p-card class="mt-4">
    <h3 class="mt-0 mb-3">Programação automática</h3>

    <div class="mb-4">
      <label class="block mb-2 font-medium">Dias disponíveis para estudo</label>
      <div class="flex flex-wrap gap-3">
        @for (day of days; track day.value) {
          <div class="flex align-items-center gap-2">
            <p-checkbox
              [value]="day.value"
              [(ngModel)]="selectedDays"
              [inputId]="day.value"
            />
            <label [for]="day.value">{{ day.label }}</label>
          </div>
        }
      </div>
      @if (noDaySelected) {
        <small class="p-error block mt-1">Selecione ao menos um dia</small>
      }
    </div>

    <div class="mb-4">
      <label class="block mb-2 font-medium">Estratégia de geração da semana</label>
      <div class="flex flex-column gap-3">
        @for (strategy of strategies; track strategy.value) {
          <div class="flex align-items-start gap-2">
            <p-radioButton
              [value]="strategy.value"
              [(ngModel)]="selectedStrategy"
              [inputId]="strategy.value"
              class="mt-1"
            />
            <div>
              <label [for]="strategy.value" class="font-medium cursor-pointer">
                {{ strategy.label }}
              </label>
              <p class="text-sm text-color-secondary m-0">{{ strategy.description }}</p>
            </div>
          </div>
        }
      </div>
    </div>

    <p-button
      label="Salvar configurações"
      icon="pi pi-cog"
      class="w-full"
      [disabled]="noDaySelected"
      (onClick)="saveConfig()"
    />
  </p-card>
</div>
```

### Spec do GoalsComponent

```typescript
describe('GoalsComponent', () => {
  let mockGoalService: jest.Mocked<WeeklyGoalService>;
  let mockConfigService: jest.Mocked<UserConfigService>;

  const mockGoal: WeeklyGoal = {
    week_start: '2026-03-09',
    target_hours: 10, target_questions: 50, target_percentage: 75
  };
  const mockConfig: UserConfig = {
    available_days: ['mon', 'tue', 'wed'],
    schedule_strategy: 'sm2'
  };

  beforeEach(async () => {
    mockGoalService = {
      getCurrent: jest.fn().mockReturnValue(of(mockGoal)),
      updateCurrent: jest.fn().mockReturnValue(of(mockGoal))
    } as any;
    mockConfigService = {
      get: jest.fn().mockReturnValue(of(mockConfig)),
      update: jest.fn().mockReturnValue(of(mockConfig))
    } as any;
    // ... TestBed setup
  });

  it('carrega meta e config ao iniciar', () => {
    expect(mockGoalService.getCurrent).toHaveBeenCalled();
    expect(mockConfigService.get).toHaveBeenCalled();
    expect(component.goalForm.value.target_hours).toBe(10);
    expect(component.selectedDays).toContain('mon');
  });

  it('salva meta ao clicar em Salvar meta', () => {
    component.goalForm.patchValue({ target_hours: 12 });
    component.saveGoal();
    expect(mockGoalService.updateCurrent).toHaveBeenCalledWith(
      expect.objectContaining({ target_hours: 12 })
    );
  });

  it('desabilita salvar config quando nenhum dia selecionado', () => {
    component.selectedDays = [];
    expect(component.noDaySelected).toBe(true);
  });
});
```

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI não está habilitado em `core-config.yaml`.
> Validação de qualidade usará processo de revisão manual.

---

## File List

| Arquivo | Ação |
|---------|------|
| `frontend/src/app/core/models/weekly-goal.model.ts` | Criar |
| `frontend/src/app/core/models/user-config.model.ts` | Criar |
| `frontend/src/app/core/services/weekly-goal.service.ts` | Criar |
| `frontend/src/app/core/services/weekly-goal.service.spec.ts` | Criar |
| `frontend/src/app/core/services/user-config.service.ts` | Criar |
| `frontend/src/app/core/services/user-config.service.spec.ts` | Criar |
| `frontend/src/app/features/goals/goals.component.ts` | Modificar |
| `frontend/src/app/features/goals/goals.component.html` | Criar |
| `frontend/src/app/features/goals/goals.component.spec.ts` | Criar |

---

## Dev Agent Record

### Completion Notes
_Preenchido pelo @dev ao completar_

### Debug Log
_Referências a logs de debug se necessário_
