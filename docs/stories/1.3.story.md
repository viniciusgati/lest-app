---
id: "1.3"
title: "Autenticação — Devise + Devise-JWT"
status: Draft
epic: 1
story: 3
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools:
  - manual-review
created_at: "2026-03-10"
updated_at: "2026-03-10"
---

# Story 1.3 — Autenticação — Devise + Devise-JWT

## Status: Draft

## Story

**Como** usuário do EduTrack,
**Quero** poder me cadastrar e fazer login com email e senha,
**Para que** meus dados de estudo sejam pessoais e protegidos.

---

## Contexto

Continuação da Story 1.2. Rails e Angular estão configurados. Esta story implementa autenticação completa:
- Backend: Devise + Devise-JWT (tokens JWT stateless)
- Frontend: Telas de login e cadastro em Angular + serviço de auth + interceptor JWT

Todos os endpoints de API futuros serão protegidos por autenticação JWT. O token é armazenado no `localStorage` e enviado via header `Authorization: Bearer <token>` em todas as requisições.

**Dependência:** Stories 1.1 e 1.2 concluídas.

---

## Acceptance Criteria

- [ ] **AC1** — `POST /api/v1/auth/signup` cria usuário e retorna token JWT
- [ ] **AC2** — `POST /api/v1/auth/login` autentica usuário e retorna token JWT
- [ ] **AC3** — `DELETE /api/v1/auth/logout` invalida o token (JWT denylist)
- [ ] **AC4** — Endpoints protegidos retornam `401 Unauthorized` sem token válido
- [ ] **AC5** — Token JWT enviado no header `Authorization: Bearer <token>`
- [ ] **AC6** — Tela de login Angular (`/auth/login`) com campos email e senha funcionando
- [ ] **AC7** — Tela de cadastro Angular (`/auth/signup`) com campos nome, email e senha funcionando
- [ ] **AC8** — Após login bem-sucedido, usuário é redirecionado para Home (`/`)
- [ ] **AC9** — Token armazenado no `localStorage` e enviado automaticamente em todas as requisições via interceptor
- [ ] **AC10** — Guard Angular protege rotas privadas: usuário sem token é redirecionado para `/auth/login`
- [ ] **AC11** — Erro de credenciais inválidas exibe mensagem amigável na tela
- [ ] **AC12** — Logout limpa o token e redireciona para `/auth/login`

---

## Tasks / Subtasks

- [ ] **Task 1** — Instalar e configurar Devise no Rails (AC1, AC2, AC3)
  - [ ] Adicionar gems ao `Gemfile`: `devise`, `devise-jwt`
  - [ ] Executar `bundle install`
  - [ ] Executar `rails generate devise:install`
  - [ ] Configurar `config/initializers/devise.rb` para API mode (sem flash, sem cookies)
  - [ ] Executar `rails generate devise User`
  - [ ] Adicionar campo `name` à migration do User
  - [ ] Executar `rails db:migrate`

- [ ] **Task 2** — Configurar Devise-JWT (AC3, AC4, AC5)
  - [ ] Criar migration para tabela `jwt_denylist`
  - [ ] Executar `rails db:migrate`
  - [ ] Configurar model `User` com estratégia JWT e denylist
  - [ ] Configurar `config/initializers/devise.rb` com `secret_key` JWT
  - [ ] Adicionar `JWT_SECRET_KEY` ao `.env`

- [ ] **Task 3** — Criar controllers de autenticação (AC1, AC2, AC3)
  - [ ] Criar `app/controllers/api/v1/auth/registrations_controller.rb`
  - [ ] Criar `app/controllers/api/v1/auth/sessions_controller.rb`
  - [ ] Criar `app/controllers/api/v1/auth/logout_controller.rb` (ou usar sessions#destroy)
  - [ ] Criar `app/controllers/api/v1/base_controller.rb` com `before_action :authenticate_user!`

- [ ] **Task 4** — Configurar rotas da API (AC1, AC2, AC3)
  - [ ] Adicionar namespace `/api/v1` em `routes.rb`
  - [ ] Mapear rotas Devise dentro do namespace

- [ ] **Task 5** — Configurar CORS (AC5)
  - [ ] ⚠️ `rack-cors` já foi adicionado ao `Gemfile` na Story 1.2 — verificar antes de adicionar novamente
  - [ ] **Modificar** `config/initializers/cors.rb` (criado na Story 1.2) adicionando `expose: ['Authorization']` para que o Angular receba o token JWT no header de resposta

- [ ] **Task 6** — Serviço de Auth no Angular (AC6, AC7, AC8, AC9, AC12)
  - [ ] Criar `frontend/src/app/core/services/auth.service.ts`
  - [ ] Implementar métodos: `login()`, `signup()`, `logout()`, `isAuthenticated()`, `currentUser()`
  - [ ] Armazenar/remover token no `localStorage`

- [ ] **Task 7** — Interceptor JWT no Angular (AC9)
  - [ ] Criar `frontend/src/app/core/interceptors/auth.interceptor.ts`
  - [ ] Injetar header `Authorization: Bearer <token>` em todas as requisições
  - [ ] Tratar respostas `401`: limpar token e redirecionar para login
  - [ ] Registrar interceptor em `app.config.ts`

- [ ] **Task 8** — Auth Guard no Angular (AC10)
  - [ ] Criar `frontend/src/app/core/guards/auth.guard.ts`
  - [ ] Verificar `AuthService.isAuthenticated()` e redirecionar se não autenticado
  - [ ] Aplicar guard nas rotas privadas em `app.routes.ts`

- [ ] **Task 9** — Tela de Login Angular (AC6, AC8, AC11)
  - [ ] Criar componente `frontend/src/app/features/auth/login/login.component.ts`
  - [ ] Formulário reativo com campos: email, senha
  - [ ] Botão de submit chama `AuthService.login()`
  - [ ] Exibir erro com `p-message` do PrimeNG em caso de falha
  - [ ] Redirecionar para `/` em caso de sucesso
  - [ ] Link para tela de cadastro

- [ ] **Task 10** — Tela de Cadastro Angular (AC7, AC11)
  - [ ] Criar componente `frontend/src/app/features/auth/signup/signup.component.ts`
  - [ ] Formulário reativo com campos: nome, email, senha, confirmação de senha
  - [ ] Validação client-side: campos obrigatórios, email válido, senhas iguais
  - [ ] Exibir erro em caso de falha (email já cadastrado, etc.)
  - [ ] Redirecionar para `/` após cadastro bem-sucedido

- [ ] **Task 11** — Testes automatizados RSpec (AC1, AC2, AC3, AC4)
  - [ ] Criar `spec/requests/api/v1/auth/registrations_spec.rb`
    - [ ] `POST /api/v1/auth/signup` com dados válidos → 201 + token
    - [ ] `POST /api/v1/auth/signup` com email duplicado → 422
    - [ ] `POST /api/v1/auth/signup` com dados inválidos → 422
  - [ ] Criar `spec/requests/api/v1/auth/sessions_spec.rb`
    - [ ] `POST /api/v1/auth/login` com credenciais válidas → 200 + token
    - [ ] `POST /api/v1/auth/login` com credenciais inválidas → 401
    - [ ] `DELETE /api/v1/auth/logout` com token válido → 204
    - [ ] `DELETE /api/v1/auth/logout` sem token → 401
  - [ ] Criar `spec/requests/api/v1/protected_endpoint_spec.rb`
    - [ ] Endpoint protegido sem token → 401
    - [ ] Endpoint protegido com token válido → 200
    - [ ] Endpoint protegido com token da denylist → 401
  - [ ] Executar `bundle exec rspec spec/requests/api/` sem erros

- [ ] **Task 12** — Verificação final (todos os ACs)
  - [ ] Testar fluxo completo no Angular (signup → home → logout → login)
  - [ ] Verificar token no `localStorage` via DevTools
  - [ ] Verificar header `Authorization` nas requisições via DevTools → Network
  - [ ] Testar guard: acessar rota privada sem login → redireciona para `/auth/login`

---

## Dev Notes

### Gems necessárias (`Gemfile`)

```ruby
gem 'devise'
gem 'devise-jwt'
gem 'rack-cors'
```

### Model User (`app/models/user.rb`)

```ruby
class User < ApplicationRecord
  devise :database_authenticatable, :registerable, :validatable,
         :jwt_authenticatable, jwt_revocation_strategy: JwtDenylist

  validates :name, presence: true
end
```

### Model JwtDenylist (`app/models/jwt_denylist.rb`)

```ruby
class JwtDenylist < ApplicationRecord
  include Devise::JWT::RevocationStrategies::Denylist
  self.table_name = 'jwt_denylist'
end
```

### Migration JwtDenylist

```ruby
class CreateJwtDenylist < ActiveRecord::Migration[7.1]
  def change
    create_table :jwt_denylist do |t|
      t.string :jti, null: false
      t.datetime :exp, null: false
    end
    add_index :jwt_denylist, :jti
  end
end
```

### Migration User (campos adicionais)

```ruby
# Adicionar à migration gerada pelo Devise:
t.string :name, null: false, default: ''
```

### `config/initializers/devise.rb` (trechos relevantes)

```ruby
Devise.setup do |config|
  config.navigational_formats = []  # API mode: sem navegação HTML

  config.jwt do |jwt|
    jwt.secret = ENV['JWT_SECRET_KEY']
    jwt.dispatch_requests = [
      ['POST', %r{^/api/v1/auth/login$}]
    ]
    jwt.revocation_requests = [
      ['DELETE', %r{^/api/v1/auth/logout$}]
    ]
    jwt.expiration_time = 24.hours.to_i
  end
end
```

### `config/routes.rb` (auth namespace)

```ruby
Rails.application.routes.draw do
  get '/health', to: 'health#index'

  namespace :api do
    namespace :v1 do
      devise_for :users,
                 path: 'auth',
                 path_names: {
                   sign_in: 'login',
                   sign_out: 'logout',
                   registration: 'signup'
                 },
                 controllers: {
                   sessions: 'api/v1/auth/sessions',
                   registrations: 'api/v1/auth/registrations'
                 }
    end
  end

  get '*path',
      to: 'application#fallback_index_html',
      constraints: ->(req) { !req.xhr? && req.format.html? }
end
```

### `BaseController` para rotas protegidas

```ruby
# app/controllers/api/v1/base_controller.rb
module Api
  module V1
    class BaseController < ApplicationController
      before_action :authenticate_user!

      def current_user_response
        {
          id: current_user.id,
          name: current_user.name,
          email: current_user.email
        }
      end
    end
  end
end
```

### `config/initializers/cors.rb`

```ruby
Rails.application.config.middleware.insert_before 0, Rack::Cors do
  allow do
    origins ENV.fetch('ALLOWED_ORIGINS', 'http://localhost:4200')
    resource '*',
             headers: :any,
             methods: [:get, :post, :put, :patch, :delete, :options, :head],
             expose: ['Authorization']
  end
end
```

### `AuthService` Angular

```typescript
// frontend/src/app/core/services/auth.service.ts
import { Injectable, signal } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Router } from '@angular/router';
import { tap } from 'rxjs/operators';

@Injectable({ providedIn: 'root' })
export class AuthService {
  private readonly TOKEN_KEY = 'edutrack_token';
  isAuthenticated = signal<boolean>(this.hasToken());

  constructor(private http: HttpClient, private router: Router) {}

  signup(name: string, email: string, password: string) {
    return this.http.post('/api/v1/auth/signup', {
      user: { name, email, password, password_confirmation: password }
    }).pipe(tap((res: any) => this.setToken(res.token)));
  }

  login(email: string, password: string) {
    return this.http.post('/api/v1/auth/login', {
      user: { email, password }
    }).pipe(tap((res: any) => this.setToken(res.token)));
  }

  logout() {
    return this.http.delete('/api/v1/auth/logout').pipe(
      tap(() => this.clearToken())
    );
  }

  private setToken(token: string) {
    localStorage.setItem(this.TOKEN_KEY, token);
    this.isAuthenticated.set(true);
  }

  private clearToken() {
    localStorage.removeItem(this.TOKEN_KEY);
    this.isAuthenticated.set(false);
    this.router.navigate(['/auth/login']);
  }

  getToken(): string | null {
    return localStorage.getItem(this.TOKEN_KEY);
  }

  private hasToken(): boolean {
    return !!localStorage.getItem(this.TOKEN_KEY);
  }
}
```

### Interceptor JWT Angular

```typescript
// frontend/src/app/core/interceptors/auth.interceptor.ts
import { HttpInterceptorFn } from '@angular/common/http';
import { inject } from '@angular/core';
import { AuthService } from '../services/auth.service';
import { catchError, throwError } from 'rxjs';
import { Router } from '@angular/router';

export const authInterceptor: HttpInterceptorFn = (req, next) => {
  const auth = inject(AuthService);
  const router = inject(Router);
  const token = auth.getToken();

  const authReq = token
    ? req.clone({ setHeaders: { Authorization: `Bearer ${token}` } })
    : req;

  return next(authReq).pipe(
    catchError(err => {
      if (err.status === 401) {
        localStorage.removeItem('edutrack_token');
        router.navigate(['/auth/login']);
      }
      return throwError(() => err);
    })
  );
};
```

### Auth Guard Angular

```typescript
// frontend/src/app/core/guards/auth.guard.ts
import { inject } from '@angular/core';
import { CanActivateFn, Router } from '@angular/router';
import { AuthService } from '../services/auth.service';

export const authGuard: CanActivateFn = () => {
  const auth = inject(AuthService);
  const router = inject(Router);

  if (auth.isAuthenticated()) return true;

  router.navigate(['/auth/login']);
  return false;
};
```

### Variáveis de ambiente adicionais (`.env`)

```
JWT_SECRET_KEY=sua_chave_secreta_longa_aqui
ALLOWED_ORIGINS=http://localhost:4200
```

### Endpoints da API

| Método | Rota | Body | Resposta |
|--------|------|------|---------|
| POST | `/api/v1/auth/signup` | `{ user: { name, email, password } }` | `{ token }` + header Authorization |
| POST | `/api/v1/auth/login` | `{ user: { email, password } }` | `{ token }` + header Authorization |
| DELETE | `/api/v1/auth/logout` | — | `204 No Content` |

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI não está habilitado em `core-config.yaml`.
> Validação de qualidade usará processo de revisão manual.

---

## File List

| Arquivo | Ação |
|---------|------|
| `backend/Gemfile` | Modificar (devise, devise-jwt) — rack-cors já presente da Story 1.2 |
| `backend/config/initializers/devise.rb` | Modificar |
| `backend/config/initializers/cors.rb` | Modificar (adicionar `expose: ['Authorization']`) |
| `backend/config/routes.rb` | Modificar |
| `backend/app/models/user.rb` | Modificar |
| `backend/app/models/jwt_denylist.rb` | Criar |
| `backend/db/migrate/*_devise_create_users.rb` | Modificar (adicionar name) |
| `backend/db/migrate/*_create_jwt_denylist.rb` | Criar |
| `backend/app/controllers/api/v1/base_controller.rb` | Criar |
| `backend/app/controllers/api/v1/auth/sessions_controller.rb` | Criar |
| `backend/app/controllers/api/v1/auth/registrations_controller.rb` | Criar |
| `frontend/src/app/core/services/auth.service.ts` | Criar |
| `frontend/src/app/core/interceptors/auth.interceptor.ts` | Criar |
| `frontend/src/app/core/guards/auth.guard.ts` | Criar |
| `frontend/src/app/features/auth/login/login.component.ts` | Criar |
| `frontend/src/app/features/auth/signup/signup.component.ts` | Criar |
| `frontend/src/app/app.routes.ts` | Modificar (guards + rotas auth) |
| `frontend/src/app/app.config.ts` | Modificar (interceptor) |
| `backend/.env` | Modificar (JWT_SECRET_KEY) |
| `spec/requests/api/v1/auth/registrations_spec.rb` | Criar |
| `spec/requests/api/v1/auth/sessions_spec.rb` | Criar |
| `spec/requests/api/v1/protected_endpoint_spec.rb` | Criar |

---

## Dev Agent Record

### Completion Notes
_Preenchido pelo @dev ao completar_

### Debug Log
_Referências a logs de debug se necessário_
