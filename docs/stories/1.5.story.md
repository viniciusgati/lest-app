---
id: "1.5"
title: "Suite de Testes — RSpec + Jest com Relatório Minimalista"
status: Approved
epic: 1
story: 5
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools:
  - manual-review
created_at: "2026-03-10"
updated_at: "2026-03-10"
---

# Story 1.5 — Suite de Testes — RSpec + Jest com Relatório Minimalista

## Status: Approved

## Story

**Como** desenvolvedor do EduTrack,
**Quero** ter uma suite de testes funcional no backend e frontend com relatório enxuto,
**Para que** eu possa validar o sistema rapidamente com o mínimo de contexto necessário para análise.

---

## Contexto

Última story do Epic 1. Backend Rails e frontend Angular estão configurados com auth e layout. Esta story instala e configura os frameworks de teste com formatadores customizados que exibem **apenas falhas e contagem total** — suprimindo testes que passaram para reduzir o ruído no contexto de análise.

### Formato de saída esperado

```
Tests: 47 passed, 0 failed

# Quando há falhas:
Tests: 44 passed, 3 failed

FAILED:
  [backend] spec/requests/health_spec.rb:8 — expected 200, got 500
  [backend] spec/models/user_spec.rb:15 — User is invalid without name
  [frontend] src/app/core/services/auth.service.spec.ts:22 — should store token on login
```

**Dependência:** Stories 1.1 a 1.4 concluídas.

---

## Acceptance Criteria

- [ ] **AC1** — RSpec instalado e configurado no backend Rails
- [ ] **AC2** — Jest instalado e configurado no frontend Angular (substituindo Karma/Jasmine)
- [ ] **AC3** — Formatter customizado do RSpec exibe apenas: contagem total e falhas (suprime testes que passaram)
- [ ] **AC4** — Reporter customizado do Jest exibe apenas: contagem total e falhas (suprime testes que passaram)
- [ ] **AC5** — Script unificado `test.sh` na raiz executa backend + frontend e exibe relatório consolidado
- [ ] **AC6** — `test.sh` retorna exit code `0` se todos passam, `1` se há falhas
- [ ] **AC7** — Testes iniciais do backend cobrem: `GET /health`, modelo User (validações), autenticação (signup/login/logout)
- [ ] **AC8** — Testes iniciais do frontend cobrem: `AuthService` (login, logout, token), `AuthGuard`, `AuthInterceptor`
- [ ] **AC9** — `bundle exec rspec` funciona standalone com o formatter minimalista
- [ ] **AC10** — `npm test` no frontend funciona standalone com o reporter minimalista

---

## Tasks / Subtasks

- [ ] **Task 1** — Instalar e configurar RSpec no backend (AC1, AC9)
  - [ ] Adicionar ao `Gemfile`:
    ```ruby
    group :development, :test do
      gem 'rspec-rails'
      gem 'factory_bot_rails'
      gem 'faker'
    end
    group :test do
      gem 'shoulda-matchers'
    end
    ```
  - [ ] Executar `bundle install`
  - [ ] Executar `rails generate rspec:install`
  - [ ] Configurar `spec/rails_helper.rb` com FactoryBot e Shoulda Matchers
  - [ ] Verificar `bundle exec rspec` executa sem erros (0 exemplos)

- [ ] **Task 2** — Criar formatter minimalista do RSpec (AC3, AC9)
  - [ ] Criar `backend/spec/support/minimal_formatter.rb`
  - [ ] Implementar formatter que:
    - Suprime output de testes individuais que passam
    - Ao final exibe: `Tests: X passed, Y failed`
    - Lista cada falha com arquivo, linha e mensagem
  - [ ] Criar `backend/.rspec` configurando o formatter como padrão
  - [ ] Testar o formatter com um teste de sucesso e um de falha proposital

- [ ] **Task 3** — Escrever testes iniciais do backend (AC7)
  - [ ] Criar `spec/factories/users.rb` com factory do User
  - [ ] Criar `spec/requests/health_spec.rb` — testa `GET /health`
  - [ ] Criar `spec/models/user_spec.rb` — testa validações do model User
  - [ ] Criar `spec/requests/auth/registrations_spec.rb` — testa signup
  - [ ] Criar `spec/requests/auth/sessions_spec.rb` — testa login e logout
  - [ ] Executar todos e confirmar que passam

- [ ] **Task 4** — Instalar e configurar Jest no frontend (AC2, AC10)
  - [ ] Remover Karma/Jasmine (se presentes):
    ```bash
    npm uninstall karma karma-chrome-launcher karma-coverage karma-jasmine karma-jasmine-html-reporter
    ```
  - [ ] Instalar Jest:
    ```bash
    npm install --save-dev jest @jest/globals jest-environment-jsdom ts-jest @types/jest
    npm install --save-dev jest-preset-angular
    ```
  - [ ] Criar `frontend/jest.config.js`
  - [ ] Criar `frontend/setup-jest.ts`
  - [ ] Atualizar `tsconfig.spec.json` para Jest
  - [ ] Atualizar script `test` em `package.json`: `"test": "jest"`

- [ ] **Task 5** — Criar reporter minimalista do Jest (AC4, AC10)
  - [ ] Criar `frontend/jest-minimal-reporter.js`
  - [ ] Implementar reporter que:
    - Suprime output de testes que passam
    - Ao final exibe: `Tests: X passed, Y failed`
    - Lista cada falha com arquivo, linha e mensagem de erro
  - [ ] Configurar reporter em `jest.config.js`
  - [ ] Testar com um teste de sucesso e um de falha proposital

- [ ] **Task 6** — Escrever testes iniciais do frontend (AC8)
  - [ ] Criar `src/app/core/services/auth.service.spec.ts`
    - Testa `login()` armazena token no localStorage
    - Testa `logout()` remove token
    - Testa `isAuthenticated()` retorna correto
  - [ ] Criar `src/app/core/guards/auth.guard.spec.ts`
    - Testa redirecionamento quando não autenticado
    - Testa acesso permitido quando autenticado
  - [ ] Criar `src/app/core/interceptors/auth.interceptor.spec.ts`
    - Testa que header Authorization é injetado quando há token
    - Testa que requisição passa sem header quando não há token
  - [ ] Executar todos e confirmar que passam

- [ ] **Task 7** — Criar script unificado `test.sh` (AC5, AC6)
  - [ ] Criar `test.sh` na raiz do monorepo
  - [ ] Script executa backend (RSpec) e frontend (Jest) sequencialmente
  - [ ] Consolida saída no formato minimalista definido
  - [ ] Retorna exit code 1 se qualquer suite falhar
  - [ ] Dar permissão: `chmod +x test.sh`

- [ ] **Task 8** — Verificação final (todos os ACs)
  - [ ] Executar `./test.sh` e confirmar saída minimalista
  - [ ] Introduzir uma falha proposital no backend e confirmar que aparece no relatório
  - [ ] Introduzir uma falha proposital no frontend e confirmar que aparece no relatório
  - [ ] Remover falhas proposital e confirmar `Tests: X passed, 0 failed`
  - [ ] Confirmar exit code correto: `echo $?` após `./test.sh`

---

## Dev Notes

### Formatter minimalista do RSpec

```ruby
# backend/spec/support/minimal_formatter.rb
class MinimalFormatter
  RSpec::Core::Formatters.register self,
    :start, :example_failed, :dump_summary

  def initialize(output)
    @output = output
    @failures = []
  end

  def start(notification)
    # Silencioso no início
  end

  def example_failed(notification)
    example = notification.example
    @failures << {
      location: example.location,
      description: example.full_description,
      message: notification.message_lines.first&.strip
    }
  end

  def dump_summary(notification)
    passed = notification.example_count - notification.failure_count
    failed = notification.failure_count

    @output.puts "\nTests: #{passed} passed, #{failed} failed"

    if @failures.any?
      @output.puts "\nFAILED:"
      @failures.each do |f|
        @output.puts "  [#{f[:location]}] #{f[:message]}"
      end
    end
  end
end
```

### `.rspec` na raiz do backend

```
--require spec_helper
--format MinimalFormatter
--require support/minimal_formatter
```

### `spec/rails_helper.rb` (trechos relevantes)

```ruby
require 'spec_helper'
ENV['RAILS_ENV'] ||= 'test'
require_relative '../config/environment'
require 'rspec/rails'
require 'factory_bot_rails'

Dir[Rails.root.join('spec/support/**/*.rb')].sort.each { |f| require f }

RSpec.configure do |config|
  config.include FactoryBot::Syntax::Methods
  config.use_transactional_fixtures = true
end

Shoulda::Matchers.configure do |config|
  config.integrate { |with| with.test_framework(:rspec).and.library(:rails) }
end
```

### Factory do User

```ruby
# spec/factories/users.rb
FactoryBot.define do
  factory :user do
    name { Faker::Name.name }
    email { Faker::Internet.unique.email }
    password { 'password123' }
    password_confirmation { 'password123' }
  end
end
```

### Specs do backend

```ruby
# spec/requests/health_spec.rb
require 'rails_helper'

RSpec.describe 'Health', type: :request do
  describe 'GET /health' do
    it 'retorna status ok' do
      get '/health'
      expect(response).to have_http_status(:ok)
      expect(JSON.parse(response.body)).to eq({ 'status' => 'ok' })
    end
  end
end

# spec/models/user_spec.rb
require 'rails_helper'

RSpec.describe User, type: :model do
  it { should validate_presence_of(:name) }
  it { should validate_presence_of(:email) }
  it { should validate_uniqueness_of(:email).case_insensitive }
  it { should validate_presence_of(:password) }

  it 'é válido com atributos válidos' do
    expect(build(:user)).to be_valid
  end
end

# spec/requests/auth/registrations_spec.rb
require 'rails_helper'

RSpec.describe 'Auth::Registrations', type: :request do
  describe 'POST /api/v1/auth/signup' do
    let(:valid_params) do
      { user: { name: 'João', email: 'joao@test.com',
                password: 'password123', password_confirmation: 'password123' } }
    end

    it 'cria usuário e retorna token' do
      post '/api/v1/auth/signup', params: valid_params, as: :json
      expect(response).to have_http_status(:ok)
      expect(response.headers['Authorization']).to be_present
    end

    it 'retorna erro com email duplicado' do
      create(:user, email: 'joao@test.com')
      post '/api/v1/auth/signup', params: valid_params, as: :json
      expect(response).to have_http_status(:unprocessable_entity)
    end
  end
end

# spec/requests/auth/sessions_spec.rb
require 'rails_helper'

RSpec.describe 'Auth::Sessions', type: :request do
  let(:user) { create(:user, email: 'test@test.com', password: 'password123') }

  describe 'POST /api/v1/auth/login' do
    it 'autentica e retorna token' do
      post '/api/v1/auth/login',
           params: { user: { email: user.email, password: 'password123' } },
           as: :json
      expect(response).to have_http_status(:ok)
      expect(response.headers['Authorization']).to be_present
    end

    it 'retorna 401 com credenciais inválidas' do
      post '/api/v1/auth/login',
           params: { user: { email: user.email, password: 'wrong' } },
           as: :json
      expect(response).to have_http_status(:unauthorized)
    end
  end
end
```

### `jest.config.js` do frontend

```javascript
module.exports = {
  preset: 'jest-preset-angular',
  setupFilesAfterFramework: ['<rootDir>/setup-jest.ts'],
  testEnvironment: 'jsdom',
  reporters: [
    '<rootDir>/jest-minimal-reporter.js'
  ],
  collectCoverageFrom: ['src/app/**/*.ts', '!src/app/**/*.module.ts'],
  moduleNameMapper: {
    '^@/(.*)$': '<rootDir>/src/$1'
  }
};
```

### Reporter minimalista do Jest

```javascript
// frontend/jest-minimal-reporter.js
class MinimalReporter {
  constructor(globalConfig, options) {
    this._globalConfig = globalConfig;
    this._failures = [];
  }

  onRunComplete(contexts, results) {
    const passed = results.numPassedTests;
    const failed = results.numFailedTests;

    console.log(`\nTests: ${passed} passed, ${failed} failed`);

    if (failed > 0) {
      console.log('\nFAILED:');
      results.testResults.forEach(suite => {
        suite.testResults
          .filter(t => t.status === 'failed')
          .forEach(test => {
            const file = suite.testFilePath.replace(process.cwd(), '');
            const msg = test.failureMessages[0]?.split('\n')[0] ?? '';
            console.log(`  [${file}] ${test.fullName} — ${msg}`);
          });
      });
    }
  }
}

module.exports = MinimalReporter;
```

### `setup-jest.ts`

```typescript
import 'jest-preset-angular/setup-jest';
```

### Script `test.sh` (raiz do monorepo)

```bash
#!/bin/bash
set -e

BACKEND_EXIT=0
FRONTEND_EXIT=0

echo "=== Backend (RSpec) ==="
cd backend && bundle exec rspec || BACKEND_EXIT=$?
cd ..

echo ""
echo "=== Frontend (Jest) ==="
cd frontend && npm test || FRONTEND_EXIT=$?
cd ..

echo ""
if [ $BACKEND_EXIT -eq 0 ] && [ $FRONTEND_EXIT -eq 0 ]; then
  echo "✅ All tests passed"
  exit 0
else
  echo "❌ Tests failed (backend: $BACKEND_EXIT, frontend: $FRONTEND_EXIT)"
  exit 1
fi
```

### `tsconfig.spec.json` para Jest

```json
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/spec",
    "types": ["jest"]
  },
  "include": ["src/**/*.spec.ts", "src/**/*.d.ts", "setup-jest.ts"]
}
```

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI não está habilitado em `core-config.yaml`.
> Validação de qualidade usará processo de revisão manual.

---

## File List

| Arquivo | Ação |
|---------|------|
| `backend/Gemfile` | Modificar (rspec-rails, factory_bot, faker, shoulda-matchers) |
| `backend/.rspec` | Criar |
| `backend/spec/rails_helper.rb` | Modificar |
| `backend/spec/spec_helper.rb` | Gerado pelo rspec:install |
| `backend/spec/support/minimal_formatter.rb` | Criar |
| `backend/spec/factories/users.rb` | Criar |
| `backend/spec/requests/health_spec.rb` | Criar |
| `backend/spec/models/user_spec.rb` | Criar |
| `backend/spec/requests/auth/registrations_spec.rb` | Criar |
| `backend/spec/requests/auth/sessions_spec.rb` | Criar |
| `frontend/jest.config.js` | Criar |
| `frontend/setup-jest.ts` | Criar |
| `frontend/jest-minimal-reporter.js` | Criar |
| `frontend/tsconfig.spec.json` | Modificar |
| `frontend/package.json` | Modificar (script test + dependências Jest) |
| `frontend/src/app/core/services/auth.service.spec.ts` | Criar |
| `frontend/src/app/core/guards/auth.guard.spec.ts` | Criar |
| `frontend/src/app/core/interceptors/auth.interceptor.spec.ts` | Criar |
| `test.sh` | Criar |

---

## Dev Agent Record

### Completion Notes
_Preenchido pelo @dev ao completar_

### Debug Log
_Referências a logs de debug se necessário_
