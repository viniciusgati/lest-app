---
id: "6.2"
title: "Frontend — Botão Gerar Semana"
status: Draft
epic: 6
story: 2
created_at: "2026-03-10"
updated_at: "2026-03-10"
---

# Story 6.2 — Frontend — Botão Gerar Semana

## Status: Draft

## Story

**Como** usuário do EduTrack,
**Quero** gerar minha programação semanal com um clique,
**Para que** o sistema distribua automaticamente minhas revisões sem eu precisar agendar cada uma manualmente.

---

## Contexto

Última story do projeto. Backend de agendamento automático pronto (Story 6.1). Esta story adiciona o botão "Gerar semana" na Home e na Agenda, implementa a chamada ao backend e atualiza o calendário com as sessões geradas.

**Dependência:** Stories 5.2 e 6.1 concluídas.

---

## Acceptance Criteria

- [ ] **AC1** — Botão "Gerar semana" disponível na Home e na Agenda
- [ ] **AC2** — Ao clicar, exibe dialog de confirmação: "Isso substituirá sessões agendadas automaticamente. Sessões concluídas não serão afetadas."
- [ ] **AC3** — Ao confirmar, chama `POST /api/v1/schedule/generate`
- [ ] **AC4** — Loading indicator durante a geração
- [ ] **AC5** — Após sucesso, calendário da Agenda é atualizado com as novas sessões
- [ ] **AC6** — Mini-calendário da Home também é atualizado
- [ ] **AC7** — Toast de sucesso: "X sessões geradas para esta semana"
- [ ] **AC8** — Se não há temas cadastrados, exibe mensagem orientando a cadastrar matérias/temas primeiro
- [ ] **AC9** — Se não há meta semanal definida, exibe aviso: "Defina sua meta semanal para uma programação mais precisa" (mas permite gerar mesmo assim)
- [ ] **AC10** — Testes Jest cobrem: confirmação, chamada ao service, atualização do calendário

---

## Tasks / Subtasks

- [ ] **Task 1** — Criar ScheduleService (AC3)
  - [ ] Criar `frontend/src/app/core/services/schedule.service.ts`
  - [ ] Método `generate(): Observable<StudySession[]>`
  - [ ] Criar `schedule.service.spec.ts`

- [ ] **Task 2** — Adicionar botão na Home (AC1, AC2, AC8, AC9)
  - [ ] Adicionar botão "Gerar semana" no `home.component.html`
  - [ ] Ao clicar: verificar se há temas cadastrados (via `subjectsMetrics`)
  - [ ] Se sem temas: exibir `p-message` orientando cadastrar
  - [ ] Se sem meta: exibir aviso via `p-message` (não bloqueia)
  - [ ] Exibir `p-confirmDialog` de confirmação

- [ ] **Task 3** — Adicionar botão na Agenda (AC1, AC5)
  - [ ] Adicionar botão "Gerar semana" no `agenda.component.html`
  - [ ] Após geração: recarregar sessões e atualizar eventos do calendário

- [ ] **Task 4** — Implementar geração e atualização (AC3–AC7)
  - [ ] Após confirmação: chamar `ScheduleService.generate()`
  - [ ] Em sucesso: atualizar `weekSessions` na Home e eventos no calendário da Agenda
  - [ ] Toast: `"${sessions.length} sessões geradas para esta semana"`
  - [ ] Em erro: toast de erro com mensagem da API

- [ ] **Task 5** — Escrever testes Jest (AC10)
  - [ ] `schedule.service.spec.ts` — testa POST para URL correta
  - [ ] `home.component.spec.ts` — testa fluxo de geração, aviso sem temas, atualização do calendário
  - [ ] `agenda.component.spec.ts` — testa atualização dos eventos após geração

- [ ] **Task 6** — Verificação final (todos os ACs)
  - [ ] Testar com temas cadastrados: gerar → verificar sessões no calendário
  - [ ] Testar sem temas: verificar mensagem orientativa
  - [ ] Testar sem meta: verificar aviso não bloqueante
  - [ ] Gerar segunda vez: verificar que sessões antigas foram substituídas
  - [ ] Verificar que sessões concluídas não foram afetadas
  - [ ] Testar em mobile (375px)

---

## Dev Notes

### ScheduleService

```typescript
// frontend/src/app/core/services/schedule.service.ts
import { Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
import { StudySession } from '../models/study-session.model';

@Injectable({ providedIn: 'root' })
export class ScheduleService {
  constructor(private http: HttpClient) {}

  generate(): Observable<StudySession[]> {
    return this.http.post<StudySession[]>('/api/v1/schedule/generate', {});
  }
}
```

### Botão na Home

```html
<!-- Em home.component.html, após o mini-calendário -->
<div class="flex justify-content-end mb-2">
  <p-button
    label="Gerar semana"
    icon="pi pi-magic"
    severity="secondary"
    [outlined]="true"
    (onClick)="confirmGenerate()"
  />
</div>
```

### Método `confirmGenerate` na Home

```typescript
confirmGenerate() {
  // Verificar se há temas
  const hasTopics = this.subjectsMetrics.some(s => s.sessions_count >= 0);
  if (!hasTopics) {
    this.messageService.add({
      severity: 'warn',
      summary: 'Sem temas',
      detail: 'Cadastre matérias e temas antes de gerar a programação.',
      life: 5000
    });
    return;
  }

  this.confirmationService.confirm({
    message: 'Isso substituirá sessões agendadas automaticamente. Sessões concluídas não serão afetadas.',
    header: 'Gerar programação semanal?',
    icon: 'pi pi-calendar-plus',
    accept: () => this.generateSchedule()
  });
}

generateSchedule() {
  this.generating = true;
  this.scheduleService.generate().subscribe({
    next: (sessions) => {
      this.generating = false;
      this.weekSessions = [
        ...this.weekSessions.filter(s => s.status === 'completed'),
        ...sessions
      ];
      this.messageService.add({
        severity: 'success',
        summary: 'Semana gerada!',
        detail: `${sessions.length} sessões geradas para esta semana`
      });
    },
    error: (err) => {
      this.generating = false;
      this.messageService.add({
        severity: 'error',
        summary: 'Erro',
        detail: err.error?.error ?? 'Erro ao gerar programação'
      });
    }
  });
}
```

### Aviso sobre meta não definida

```typescript
// No ngOnInit, após carregar weeklyProgress:
this.hasGoal = (this.weeklyProgress?.target_hours ?? 0) > 0;

// No template:
@if (!hasGoal) {
  <p-message
    severity="info"
    text="Defina sua meta semanal em Metas para uma programação mais precisa."
    class="block mb-3"
  />
}
```

### Atualização do calendário na Agenda após geração

```typescript
// agenda.component.ts
generateSchedule() {
  this.confirmationService.confirm({
    message: 'Isso substituirá sessões agendadas automaticamente. Sessões concluídas não serão afetadas.',
    header: 'Gerar programação semanal?',
    icon: 'pi pi-calendar-plus',
    accept: () => {
      this.scheduleService.generate().subscribe({
        next: (newSessions) => {
          // Remove eventos auto_generated+scheduled, mantém o resto
          const kept = this.allSessions.filter(
            s => s.status === 'completed' || !s.auto_generated
          );
          this.allSessions = [...kept, ...newSessions];
          this.rebuildCalendarEvents();
          this.messageService.add({
            severity: 'success',
            summary: 'Semana gerada!',
            detail: `${newSessions.length} sessões criadas`
          });
        }
      });
    }
  });
}
```

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled

---

## File List

| Arquivo | Ação |
|---------|------|
| `frontend/src/app/core/services/schedule.service.ts` | Criar |
| `frontend/src/app/core/services/schedule.service.spec.ts` | Criar |
| `frontend/src/app/features/home/home.component.ts` | Modificar |
| `frontend/src/app/features/home/home.component.html` | Modificar |
| `frontend/src/app/features/agenda/agenda.component.ts` | Modificar |
| `frontend/src/app/features/agenda/agenda.component.html` | Modificar |

---

## Dev Agent Record

### Completion Notes
_Preenchido pelo @dev ao completar_

### Debug Log
_Referências a logs de debug se necessário_
