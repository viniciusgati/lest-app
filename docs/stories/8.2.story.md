---
id: "8.2"
title: "Backend + Frontend — Refresh Token"
status: Approved
epic: 8
story: 2
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools:
  - manual-review
created_at: "2026-03-22"
updated_at: "2026-03-22"
---

# Story 8.2 — Backend + Frontend — Refresh Token

## Status: Ready for Review

## Story

**Como** usuário do EduTrack,
**Quero** que minha sessão seja renovada automaticamente enquanto estou usando o app,
**Para que** não seja deslogado abruptamente após 24 horas de uso.

---

## Contexto

O JWT atual expira em 24 horas sem mecanismo de renovação. Quando expira, o usuário recebe erro 401 e precisa fazer login novamente — sem aviso. Esta story implementa um refresh token de longa duração (7 dias) no backend e um interceptor Angular que detecta o token expirado e o renova silenciosamente antes de redirecionar para login.

**Estratégia:**
- Access token: 24h (mantém atual)
- Refresh token: 7 dias (novo, armazenado em `httpOnly cookie` OU `localStorage`)
- Interceptor: captura 401, tenta refresh, repete a request original
- Se refresh falhar: redireciona para `/auth/login`

---

## Acceptance Criteria

- [ ] **AC1** — `POST /api/v1/auth/refresh` aceita um refresh token válido e retorna novo access token
- [ ] **AC2** — Refresh token é gerado no login junto com o access token
- [ ] **AC3** — Refresh token armazenado em tabela `refresh_tokens` (user_id, token, expires_at, revoked_at)
- [ ] **AC4** — Refresh token expira em 7 dias
- [ ] **AC5** — Refresh token é revogado no logout
- [ ] **AC6** — Frontend: `AuthInterceptor` captura respostas 401 e tenta `POST /auth/refresh` automaticamente
- [ ] **AC7** — Frontend: após refresh bem-sucedido, a request original é repetida transparentemente
- [ ] **AC8** — Frontend: se refresh falhar (token expirado/revogado), usuário é redirecionado para `/auth/login`
- [ ] **AC9** — Refresh token armazenado em `localStorage` no frontend (consistente com JWT atual)
- [ ] **AC10** — RSpec cobre: geração, uso, expiração e revogação do refresh token

---

## Tasks / Subtasks

- [x] **Task 1** — Migration: tabela `refresh_tokens` (AC3)
  - [x] Criar migration com campos: `user_id:references`, `token:string:uniq`, `expires_at:datetime`, `revoked_at:datetime`
  - [x] Adicionar índice em `token` (unique) e `user_id`

- [x] **Task 2** — Model `RefreshToken` (AC3, AC4, AC5)
  - [x] Criar `app/models/refresh_token.rb`
  - [x] `belongs_to :user`
  - [x] Scopes: `valid` (não expirado e não revogado), `expired`, `revoked`
  - [x] Método `revoke!` que seta `revoked_at = Time.current`

- [x] **Task 3** — Endpoint `POST /api/v1/auth/refresh` (AC1, AC2)
  - [x] Criar `app/controllers/api/v1/auth/refresh_tokens_controller.rb`
  - [x] Busca refresh token válido, gera novo JWT access token, retorna `{ token: "...", expires_in: 86400 }`
  - [x] Retorna 401 se token inválido/expirado/revogado

- [x] **Task 4** — Integrar geração de refresh token no login (AC2)
  - [x] Sobrescrever `Devise::SessionsController` para gerar e retornar `refresh_token` junto com JWT
  - [x] Resposta do login: `{ token: "...", refresh_token: "...", user: { ... } }`

- [x] **Task 5** — Revogar refresh token no logout (AC5)
  - [x] Sobrescrever `Devise::SessionsController#destroy` para revogar refresh token ativo do usuário

- [x] **Task 6** — Adicionar rota de refresh (AC1)
  - [x] `post '/auth/refresh', to: 'auth/refresh_tokens#create'` em `config/routes.rb`

- [x] **Task 7** — Frontend: `AuthInterceptor` (AC6, AC7, AC8)
  - [x] Atualizar `frontend/src/app/core/interceptors/auth.interceptor.ts`
  - [x] Intercepta respostas 401
  - [x] Chama `AuthService#refreshToken()` com token do localStorage
  - [x] Em sucesso: atualiza token e repete request original
  - [x] Em falha: chama `AuthService#clearSession()` e navega para `/auth/login`
  - [x] Interceptor já registrado em `app.config.ts`

- [x] **Task 8** — Frontend: `AuthService` atualizado (AC9)
  - [x] `AuthService#login()`: salva `refresh_token` em localStorage junto com JWT
  - [x] `AuthService#refreshToken()`: `POST /auth/refresh` com refresh token
  - [x] `AuthService#logout()`: remove ambos os tokens do localStorage

- [x] **Task 9** — Testes RSpec (AC10)
  - [x] `spec/requests/api/v1/auth/refresh_tokens_spec.rb`
  - [x] Testa: token válido → 200 com novo JWT; token expirado → 401; token revogado → 401; token inválido → 401

---

## Dev Notes

### Tabela refresh_tokens

```ruby
# migration
create_table :refresh_tokens do |t|
  t.references :user, null: false, foreign_key: true
  t.string :token, null: false
  t.datetime :expires_at, null: false
  t.datetime :revoked_at
  t.timestamps
end
add_index :refresh_tokens, :token, unique: true
```

### RefreshToken model

```ruby
class RefreshToken < ApplicationRecord
  belongs_to :user

  scope :valid, -> { where(revoked_at: nil).where('expires_at > ?', Time.current) }

  def revoke!
    update!(revoked_at: Time.current)
  end

  def self.generate_for(user)
    create!(
      user: user,
      token: SecureRandom.hex(32),
      expires_at: 7.days.from_now
    )
  end
end
```

### AuthInterceptor (Angular)

```typescript
// frontend/src/app/core/interceptors/auth.interceptor.ts
import { HttpInterceptorFn, HttpErrorResponse } from '@angular/common/http';
import { inject } from '@angular/core';
import { catchError, switchMap, throwError } from 'rxjs';
import { AuthService } from '../services/auth.service';
import { Router } from '@angular/router';

export const authInterceptor: HttpInterceptorFn = (req, next) => {
  const auth = inject(AuthService);
  const router = inject(Router);

  return next(req).pipe(
    catchError((error: HttpErrorResponse) => {
      if (error.status === 401 && !req.url.includes('/auth/')) {
        return auth.refreshToken().pipe(
          switchMap(({ token }) => {
            auth.setToken(token);
            const retried = req.clone({
              setHeaders: { Authorization: `Bearer ${token}` }
            });
            return next(retried);
          }),
          catchError(() => {
            auth.logout();
            router.navigate(['/auth/login']);
            return throwError(() => error);
          })
        );
      }
      return throwError(() => error);
    })
  );
};
```

### Rota

```ruby
namespace :api do
  namespace :v1 do
    post '/auth/refresh', to: 'auth/refresh_tokens#create'
  end
end
```

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled

---

## File List

| Arquivo | Ação |
|---------|------|
| `backend/db/migrate/YYYYMMDD_create_refresh_tokens.rb` | Criar |
| `backend/app/models/refresh_token.rb` | Criar |
| `backend/app/controllers/api/v1/auth/refresh_tokens_controller.rb` | Criar |
| `backend/app/controllers/api/v1/auth/sessions_controller.rb` | Criar ou Modificar |
| `backend/config/routes.rb` | Modificar |
| `backend/spec/requests/api/v1/auth/refresh_tokens_spec.rb` | Criar |
| `frontend/src/app/core/interceptors/auth.interceptor.ts` | Criar |
| `frontend/src/app/core/services/auth.service.ts` | Modificar |
| `frontend/src/app/app.config.ts` | Modificar |

---

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Completion Notes
- Migration `create_refresh_tokens` criada e aplicada
- `RefreshToken` model com scope `valid`, método `revoke!` e `generate_for`
- `POST /api/v1/auth/refresh` endpoint sem autenticação JWT (herda ApplicationController)
- SessionsController: gera refresh token no login, revoga no logout
- Frontend: AuthInterceptor tenta refresh em 401, retenta request original; falha → clearSession
- `AuthService` salva/remove `refresh_token` do localStorage; novos métodos: `setToken`, `refreshToken`
- Backend: 203/203 | Frontend: 240/240 | TypeScript: 0 erros

### Debug Log
Nenhum blocker.

### File List
| Arquivo | Ação |
|---------|------|
| `backend/db/migrate/20260324013154_create_refresh_tokens.rb` | Criado |
| `backend/app/models/refresh_token.rb` | Criado |
| `backend/app/controllers/api/v1/auth/refresh_tokens_controller.rb` | Criado |
| `backend/app/controllers/api/v1/auth/sessions_controller.rb` | Modificado |
| `backend/config/routes.rb` | Modificado |
| `backend/spec/factories/refresh_tokens.rb` | Criado |
| `backend/spec/requests/api/v1/auth/refresh_tokens_spec.rb` | Criado |
| `frontend/src/app/core/interceptors/auth.interceptor.ts` | Modificado |
| `frontend/src/app/core/interceptors/auth.interceptor.spec.ts` | Modificado |
| `frontend/src/app/core/services/auth.service.ts` | Modificado |
| `frontend/src/app/core/services/auth.service.spec.ts` | Modificado |
