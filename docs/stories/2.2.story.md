---
id: "2.2"
title: "Frontend — CRUD de Matérias"
status: Draft
epic: 2
story: 2
created_at: "2026-03-10"
updated_at: "2026-03-10"
---

# Story 2.2 — Frontend — CRUD de Matérias

## Status: Draft

## Story

**Como** usuário do EduTrack,
**Quero** gerenciar minhas matérias no app,
**Para que** eu possa organizar meu conteúdo de estudo e navegar para ver os temas de cada uma.

---

## Contexto

Continuação da Story 2.1. Os endpoints de subjects estão prontos no backend. Esta story implementa a tela de matérias no Angular: listagem, criação, edição e exclusão — tudo com PrimeNG e consumindo a API REST.

A tela de matérias (`/subjects`) já existe como placeholder (Story 1.4). Aqui ela ganha funcionalidade completa.

**Dependência:** Stories 1.1–1.5 e 2.1 concluídas.

---

## Acceptance Criteria

- [ ] **AC1** — Tela `/subjects` exibe lista de matérias do usuário
- [ ] **AC2** — Estado vazio exibido quando não há matérias cadastradas (com CTA para criar)
- [ ] **AC3** — Botão "Nova Matéria" abre dialog/modal com campo de nome
- [ ] **AC4** — Criar matéria atualiza a lista imediatamente sem recarregar a página
- [ ] **AC5** — Cada matéria exibe botão de editar (altera o nome via dialog)
- [ ] **AC6** — Cada matéria exibe botão de excluir com confirmação antes de remover
- [ ] **AC7** — Excluir matéria remove da lista imediatamente sem recarregar a página
- [ ] **AC8** — Clicar no nome da matéria navega para `/subjects/:id` (placeholder — tela de temas, Story 2.3)
- [ ] **AC9** — Erros da API (ex: nome vazio) são exibidos como toast/mensagem na tela
- [ ] **AC10** — Loading state exibido durante chamadas à API
- [ ] **AC11** — Testes Jest cobrem: carregamento da lista, criação, edição e exclusão

---

## Tasks / Subtasks

- [ ] **Task 1** — Criar SubjectService (AC1, AC4, AC7)
  - [ ] Criar `frontend/src/app/core/services/subject.service.ts`
  - [ ] Implementar métodos: `getAll()`, `create(name)`, `update(id, name)`, `delete(id)`
  - [ ] Usar `HttpClient` com tipagem correta (interface `Subject`)
  - [ ] Definir interface em `frontend/src/app/core/models/subject.model.ts`

- [ ] **Task 2** — Implementar tela de listagem (AC1, AC2, AC8, AC10)
  - [ ] Atualizar `frontend/src/app/features/subjects/subjects.component.ts`
  - [ ] Carregar matérias no `ngOnInit` via `SubjectService.getAll()`
  - [ ] Usar `p-progressSpinner` do PrimeNG durante loading
  - [ ] Renderizar lista com `p-card` ou `p-listbox` para cada matéria
  - [ ] Exibir estado vazio com `p-empty` ou template customizado quando lista vazia
  - [ ] Clicar no nome navega para `/subjects/:id` via `RouterLink`

- [ ] **Task 3** — Dialog de criação (AC3, AC4, AC9)
  - [ ] Usar `p-dialog` do PrimeNG para o modal de criação
  - [ ] Formulário reativo com campo `name` (obrigatório, máx 100 chars)
  - [ ] Botão "Salvar" chama `SubjectService.create()` e fecha dialog em sucesso
  - [ ] Em erro: exibe mensagem via `p-toast` (PrimeNG)
  - [ ] Adicionar `ToastModule` e `MessageService` ao componente

- [ ] **Task 4** — Dialog de edição (AC5, AC9)
  - [ ] Reusar `p-dialog` com modo edição
  - [ ] Pré-preencher campo com nome atual da matéria
  - [ ] Botão "Salvar" chama `SubjectService.update()` e atualiza item na lista
  - [ ] Em erro: exibe toast com mensagem da API

- [ ] **Task 5** — Confirmação de exclusão (AC6, AC7)
  - [ ] Usar `p-confirmDialog` do PrimeNG com `ConfirmationService`
  - [ ] Mensagem: "Excluir '[nome]'? Todos os temas serão removidos."
  - [ ] Confirmar chama `SubjectService.delete()` e remove da lista local
  - [ ] Cancelar fecha sem ação

- [ ] **Task 6** — Escrever testes Jest (AC11)
  - [ ] Criar `frontend/src/app/features/subjects/subjects.component.spec.ts`
  - [ ] Mock de `SubjectService` com `jest.fn()`
  - [ ] Testar: lista renderizada corretamente, estado vazio, criação adiciona à lista, exclusão remove da lista
  - [ ] Criar `frontend/src/app/core/services/subject.service.spec.ts`
  - [ ] Testar: cada método HTTP é chamado com URL e params corretos

- [ ] **Task 7** — Verificação final (todos os ACs)
  - [ ] Testar fluxo completo: criar → editar → excluir matéria
  - [ ] Testar estado vazio
  - [ ] Testar mensagem de erro com API offline
  - [ ] Testar em viewport mobile (375px)

---

## Dev Notes

### Interface Subject

```typescript
// frontend/src/app/core/models/subject.model.ts
export interface Subject {
  id: number;
  name: string;
  created_at: string;
}
```

### SubjectService

```typescript
// frontend/src/app/core/services/subject.service.ts
import { Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
import { Subject } from '../models/subject.model';

@Injectable({ providedIn: 'root' })
export class SubjectService {
  private readonly url = '/api/v1/subjects';

  constructor(private http: HttpClient) {}

  getAll(): Observable<Subject[]> {
    return this.http.get<Subject[]>(this.url);
  }

  create(name: string): Observable<Subject> {
    return this.http.post<Subject>(this.url, { subject: { name } });
  }

  update(id: number, name: string): Observable<Subject> {
    return this.http.put<Subject>(`${this.url}/${id}`, { subject: { name } });
  }

  delete(id: number): Observable<void> {
    return this.http.delete<void>(`${this.url}/${id}`);
  }
}
```

### SubjectsComponent

```typescript
// frontend/src/app/features/subjects/subjects.component.ts
import { Component, OnInit } from '@angular/core';
import { CommonModule } from '@angular/common';
import { RouterLink } from '@angular/router';
import { FormsModule, ReactiveFormsModule, FormBuilder, Validators } from '@angular/forms';
import { ButtonModule } from 'primeng/button';
import { CardModule } from 'primeng/card';
import { DialogModule } from 'primeng/dialog';
import { ConfirmDialogModule } from 'primeng/confirmdialog';
import { ToastModule } from 'primeng/toast';
import { ProgressSpinnerModule } from 'primeng/progressspinner';
import { InputTextModule } from 'primeng/inputtext';
import { MessageService, ConfirmationService } from 'primeng/api';
import { SubjectService } from '../../core/services/subject.service';
import { Subject } from '../../core/models/subject.model';

@Component({
  selector: 'app-subjects',
  standalone: true,
  imports: [
    CommonModule, RouterLink, FormsModule, ReactiveFormsModule,
    ButtonModule, CardModule, DialogModule, ConfirmDialogModule,
    ToastModule, ProgressSpinnerModule, InputTextModule
  ],
  providers: [MessageService, ConfirmationService],
  templateUrl: './subjects.component.html'
})
export class SubjectsComponent implements OnInit {
  subjects: Subject[] = [];
  loading = false;
  dialogVisible = false;
  editingSubject: Subject | null = null;

  form = this.fb.group({
    name: ['', [Validators.required, Validators.maxLength(100)]]
  });

  constructor(
    private subjectService: SubjectService,
    private messageService: MessageService,
    private confirmationService: ConfirmationService,
    private fb: FormBuilder
  ) {}

  ngOnInit() {
    this.loadSubjects();
  }

  loadSubjects() {
    this.loading = true;
    this.subjectService.getAll().subscribe({
      next: (data) => { this.subjects = data; this.loading = false; },
      error: () => { this.loading = false; }
    });
  }

  openCreate() {
    this.editingSubject = null;
    this.form.reset();
    this.dialogVisible = true;
  }

  openEdit(subject: Subject) {
    this.editingSubject = subject;
    this.form.patchValue({ name: subject.name });
    this.dialogVisible = true;
  }

  save() {
    if (this.form.invalid) return;
    const name = this.form.value.name!;

    const request$ = this.editingSubject
      ? this.subjectService.update(this.editingSubject.id, name)
      : this.subjectService.create(name);

    request$.subscribe({
      next: (saved) => {
        if (this.editingSubject) {
          const idx = this.subjects.findIndex(s => s.id === saved.id);
          if (idx > -1) this.subjects[idx] = saved;
        } else {
          this.subjects = [...this.subjects, saved];
        }
        this.dialogVisible = false;
      },
      error: (err) => {
        const msg = err.error?.errors?.[0] ?? 'Erro ao salvar matéria';
        this.messageService.add({ severity: 'error', summary: 'Erro', detail: msg });
      }
    });
  }

  confirmDelete(subject: Subject) {
    this.confirmationService.confirm({
      message: `Excluir "${subject.name}"? Todos os temas serão removidos.`,
      header: 'Confirmar exclusão',
      icon: 'pi pi-trash',
      accept: () => this.delete(subject)
    });
  }

  private delete(subject: Subject) {
    this.subjectService.delete(subject.id).subscribe({
      next: () => {
        this.subjects = this.subjects.filter(s => s.id !== subject.id);
      },
      error: () => {
        this.messageService.add({ severity: 'error', summary: 'Erro', detail: 'Erro ao excluir matéria' });
      }
    });
  }
}
```

### Template HTML (`subjects.component.html`)

```html
<div class="subjects-page p-4">
  <p-toast />
  <p-confirmDialog />

  <div class="flex justify-content-between align-items-center mb-4">
    <h2 class="m-0">Matérias</h2>
    <p-button label="Nova Matéria" icon="pi pi-plus" (onClick)="openCreate()" />
  </div>

  @if (loading) {
    <div class="flex justify-content-center mt-6">
      <p-progressSpinner />
    </div>
  } @else if (subjects.length === 0) {
    <div class="text-center mt-6">
      <i class="pi pi-book text-4xl text-color-secondary mb-3 block"></i>
      <p class="text-color-secondary mb-3">Nenhuma matéria cadastrada ainda.</p>
      <p-button label="Criar primeira matéria" icon="pi pi-plus" (onClick)="openCreate()" />
    </div>
  } @else {
    <div class="flex flex-column gap-2">
      @for (subject of subjects; track subject.id) {
        <p-card>
          <div class="flex align-items-center justify-content-between">
            <a [routerLink]="['/subjects', subject.id]" class="font-medium text-lg no-underline text-color">
              {{ subject.name }}
            </a>
            <div class="flex gap-2">
              <p-button icon="pi pi-pencil" [text]="true" severity="secondary" (onClick)="openEdit(subject)" />
              <p-button icon="pi pi-trash" [text]="true" severity="danger" (onClick)="confirmDelete(subject)" />
            </div>
          </div>
        </p-card>
      }
    </div>
  }

  <p-dialog
    [(visible)]="dialogVisible"
    [header]="editingSubject ? 'Editar Matéria' : 'Nova Matéria'"
    [modal]="true"
    [style]="{ width: '90vw', maxWidth: '400px' }"
  >
    <form [formGroup]="form" (ngSubmit)="save()">
      <div class="field">
        <label for="name">Nome</label>
        <input
          id="name"
          type="text"
          pInputText
          formControlName="name"
          placeholder="Ex: Matemática"
          class="w-full mt-1"
          autofocus
        />
        @if (form.get('name')?.invalid && form.get('name')?.touched) {
          <small class="p-error">Nome é obrigatório (máx. 100 caracteres)</small>
        }
      </div>
    </form>
    <ng-template pTemplate="footer">
      <p-button label="Cancelar" [text]="true" severity="secondary" (onClick)="dialogVisible = false" />
      <p-button label="Salvar" icon="pi pi-check" (onClick)="save()" [disabled]="form.invalid" />
    </ng-template>
  </p-dialog>
</div>
```

### Spec do SubjectsComponent

```typescript
// frontend/src/app/features/subjects/subjects.component.spec.ts
import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SubjectsComponent } from './subjects.component';
import { SubjectService } from '../../core/services/subject.service';
import { of, throwError } from 'rxjs';
import { provideRouter } from '@angular/router';
import { provideAnimationsAsync } from '@angular/platform-browser/animations/async';

describe('SubjectsComponent', () => {
  let fixture: ComponentFixture<SubjectsComponent>;
  let component: SubjectsComponent;
  let mockSubjectService: jest.Mocked<SubjectService>;

  const mockSubjects = [
    { id: 1, name: 'Matemática', created_at: '' },
    { id: 2, name: 'Português', created_at: '' }
  ];

  beforeEach(async () => {
    mockSubjectService = {
      getAll: jest.fn().mockReturnValue(of(mockSubjects)),
      create: jest.fn(),
      update: jest.fn(),
      delete: jest.fn()
    } as any;

    await TestBed.configureTestingModule({
      imports: [SubjectsComponent],
      providers: [
        provideRouter([]),
        provideAnimationsAsync(),
        { provide: SubjectService, useValue: mockSubjectService }
      ]
    }).compileComponents();

    fixture = TestBed.createComponent(SubjectsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('carrega matérias ao iniciar', () => {
    expect(mockSubjectService.getAll).toHaveBeenCalled();
    expect(component.subjects.length).toBe(2);
  });

  it('adiciona matéria à lista ao criar', () => {
    const nova = { id: 3, name: 'História', created_at: '' };
    mockSubjectService.create.mockReturnValue(of(nova));
    component.form.patchValue({ name: 'História' });
    component.save();
    expect(component.subjects.length).toBe(3);
  });

  it('remove matéria da lista ao excluir', () => {
    mockSubjectService.delete.mockReturnValue(of(undefined as any));
    component['delete'](mockSubjects[0]);
    expect(component.subjects.length).toBe(1);
  });
});
```

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI não está habilitado em `core-config.yaml`.
> Validação de qualidade usará processo de revisão manual.

---

## File List

| Arquivo | Ação |
|---------|------|
| `frontend/src/app/core/models/subject.model.ts` | Criar |
| `frontend/src/app/core/services/subject.service.ts` | Criar |
| `frontend/src/app/core/services/subject.service.spec.ts` | Criar |
| `frontend/src/app/features/subjects/subjects.component.ts` | Modificar |
| `frontend/src/app/features/subjects/subjects.component.html` | Criar |
| `frontend/src/app/features/subjects/subjects.component.spec.ts` | Criar |

---

## Dev Agent Record

### Completion Notes
_Preenchido pelo @dev ao completar_

### Debug Log
_Referências a logs de debug se necessário_
