---
id: "2.2"
title: "Frontend — CRUD de Matérias"
status: Approved
epic: 2
story: 2
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools:
  - manual-review
created_at: "2026-03-10"
updated_at: "2026-03-10"
---

# Story 2.2 — Frontend — CRUD de Matérias

## Status: Ready for Review

## Story

**Como** usuário do EduTrack,
**Quero** gerenciar minhas matérias no app,
**Para que** eu possa organizar meu conteúdo de estudo e navegar para ver os temas de cada uma.

---

## Contexto

Continuação da Story 2.1. Os endpoints de subjects estão prontos no backend. Esta story implementa a tela de matérias no Angular: listagem, criação, edição e exclusão — tudo com PrimeNG e consumindo a API REST.

A tela de matérias (`/subjects`) já existe como placeholder (Story 1.4). Aqui ela ganha funcionalidade completa.

**Dependência:** Stories 1.1–1.5 e 2.1 concluídas.

---

## Acceptance Criteria

- [x] **AC1** — Tela `/subjects` exibe lista de matérias do usuário
- [x] **AC2** — Estado vazio exibido quando não há matérias cadastradas (com CTA para criar)
- [x] **AC3** — Botão "Nova Matéria" abre dialog/modal com campo de nome
- [x] **AC4** — Criar matéria atualiza a lista imediatamente sem recarregar a página
- [x] **AC5** — Cada matéria exibe botão de editar (altera o nome via dialog)
- [x] **AC6** — Cada matéria exibe botão de excluir com confirmação antes de remover
- [x] **AC7** — Excluir matéria remove da lista imediatamente sem recarregar a página
- [x] **AC8** — Clicar no nome da matéria navega para `/subjects/:id` (placeholder — tela de temas, Story 2.3)
- [x] **AC9** — Erros da API (ex: nome vazio) são exibidos como toast/mensagem na tela
- [x] **AC10** — Loading state exibido durante chamadas à API
- [x] **AC11** — Testes Vitest cobrem: carregamento da lista, criação, edição e exclusão

---

## Tasks / Subtasks

- [x] **Task 1** — Criar SubjectService (AC1, AC4, AC7)
  - [x] Criar `frontend/src/app/core/services/subject.service.ts`
  - [x] Implementar métodos: `getAll()`, `create(name)`, `update(id, name)`, `delete(id)`
  - [x] Usar `HttpClient` com tipagem correta (interface `Subject`)
  - [x] Definir interface em `frontend/src/app/core/models/subject.model.ts`

- [x] **Task 2** — Implementar tela de listagem (AC1, AC2, AC8, AC10)
  - [x] Atualizar `frontend/src/app/features/subjects/subjects/subjects.ts`
  - [x] Carregar matérias no `ngOnInit` via `SubjectService.getAll()`
  - [x] Usar `p-progressSpinner` do PrimeNG durante loading
  - [x] Renderizar lista com `p-card` para cada matéria
  - [x] Exibir estado vazio com template customizado quando lista vazia
  - [x] Clicar no nome navega para `/subjects/:id` via `RouterLink`

- [x] **Task 3** — Dialog de criação (AC3, AC4, AC9)
  - [x] Usar `p-dialog` do PrimeNG para o modal de criação
  - [x] Formulário reativo com campo `name` (obrigatório, máx 100 chars)
  - [x] Botão "Salvar" chama `SubjectService.create()` e fecha dialog em sucesso
  - [x] Em erro: exibe mensagem via `p-toast` (PrimeNG)
  - [x] Adicionar `ToastModule` e `MessageService` ao componente

- [x] **Task 4** — Dialog de edição (AC5, AC9)
  - [x] Reusar `p-dialog` com modo edição
  - [x] Pré-preencher campo com nome atual da matéria
  - [x] Botão "Salvar" chama `SubjectService.update()` e atualiza item na lista
  - [x] Em erro: exibe toast com mensagem da API

- [x] **Task 5** — Confirmação de exclusão (AC6, AC7)
  - [x] Usar `p-confirmDialog` do PrimeNG com `ConfirmationService`
  - [x] Mensagem: "Excluir '[nome]'? Todos os temas serão removidos."
  - [x] Confirmar chama `SubjectService.delete()` e remove da lista local
  - [x] Cancelar fecha sem ação

- [x] **Task 6** — Escrever testes Vitest (AC11)
  - [x] Criar `frontend/src/app/features/subjects/subjects/subjects.spec.ts`
  - [x] Mock de `SubjectService` com `vi.fn()` (Vitest)
  - [x] Testar: lista renderizada corretamente, estado vazio, criação adiciona à lista, exclusão remove da lista
  - [x] Criar `frontend/src/app/core/services/subject.service.spec.ts`
  - [x] Testar: cada método HTTP é chamado com URL e params corretos

- [x] **Task 7** — Verificação final (todos os ACs)
  - [x] Fluxo CRUD coberto pelos testes unitários
  - [x] Estado vazio coberto por teste
  - [x] `bash test.sh` → Tests: 107 passed, 0 failed, exit 0

---

## Dev Notes

### Interface Subject

```typescript
// frontend/src/app/core/models/subject.model.ts
export interface Subject {
  id: number;
  name: string;
  created_at: string;
}
```

### SubjectService

```typescript
// frontend/src/app/core/services/subject.service.ts
import { Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
import { Subject } from '../models/subject.model';

@Injectable({ providedIn: 'root' })
export class SubjectService {
  private readonly url = '/api/v1/subjects';

  constructor(private http: HttpClient) {}

  getAll(): Observable<Subject[]> {
    return this.http.get<Subject[]>(this.url);
  }

  create(name: string): Observable<Subject> {
    return this.http.post<Subject>(this.url, { subject: { name } });
  }

  update(id: number, name: string): Observable<Subject> {
    return this.http.put<Subject>(`${this.url}/${id}`, { subject: { name } });
  }

  delete(id: number): Observable<void> {
    return this.http.delete<void>(`${this.url}/${id}`);
  }
}
```

### SubjectsComponent

```typescript
// frontend/src/app/features/subjects/subjects.component.ts
import { Component, OnInit } from '@angular/core';
import { CommonModule } from '@angular/common';
import { RouterLink } from '@angular/router';
import { FormsModule, ReactiveFormsModule, FormBuilder, Validators } from '@angular/forms';
import { ButtonModule } from 'primeng/button';
import { CardModule } from 'primeng/card';
import { DialogModule } from 'primeng/dialog';
import { ConfirmDialogModule } from 'primeng/confirmdialog';
import { ToastModule } from 'primeng/toast';
import { ProgressSpinnerModule } from 'primeng/progressspinner';
import { InputTextModule } from 'primeng/inputtext';
import { MessageService, ConfirmationService } from 'primeng/api';
import { SubjectService } from '../../core/services/subject.service';
import { Subject } from '../../core/models/subject.model';

@Component({
  selector: 'app-subjects',
  standalone: true,
  imports: [
    CommonModule, RouterLink, FormsModule, ReactiveFormsModule,
    ButtonModule, CardModule, DialogModule, ConfirmDialogModule,
    ToastModule, ProgressSpinnerModule, InputTextModule
  ],
  providers: [MessageService, ConfirmationService],
  templateUrl: './subjects.component.html'
})
export class SubjectsComponent implements OnInit {
  subjects: Subject[] = [];
  loading = false;
  dialogVisible = false;
  editingSubject: Subject | null = null;

  form = this.fb.group({
    name: ['', [Validators.required, Validators.maxLength(100)]]
  });

  constructor(
    private subjectService: SubjectService,
    private messageService: MessageService,
    private confirmationService: ConfirmationService,
    private fb: FormBuilder
  ) {}

  ngOnInit() {
    this.loadSubjects();
  }

  loadSubjects() {
    this.loading = true;
    this.subjectService.getAll().subscribe({
      next: (data) => { this.subjects = data; this.loading = false; },
      error: () => { this.loading = false; }
    });
  }

  openCreate() {
    this.editingSubject = null;
    this.form.reset();
    this.dialogVisible = true;
  }

  openEdit(subject: Subject) {
    this.editingSubject = subject;
    this.form.patchValue({ name: subject.name });
    this.dialogVisible = true;
  }

  save() {
    if (this.form.invalid) return;
    const name = this.form.value.name!;

    const request$ = this.editingSubject
      ? this.subjectService.update(this.editingSubject.id, name)
      : this.subjectService.create(name);

    request$.subscribe({
      next: (saved) => {
        if (this.editingSubject) {
          const idx = this.subjects.findIndex(s => s.id === saved.id);
          if (idx > -1) this.subjects[idx] = saved;
        } else {
          this.subjects = [...this.subjects, saved];
        }
        this.dialogVisible = false;
      },
      error: (err) => {
        const msg = err.error?.errors?.[0] ?? 'Erro ao salvar matéria';
        this.messageService.add({ severity: 'error', summary: 'Erro', detail: msg });
      }
    });
  }

  confirmDelete(subject: Subject) {
    this.confirmationService.confirm({
      message: `Excluir "${subject.name}"? Todos os temas serão removidos.`,
      header: 'Confirmar exclusão',
      icon: 'pi pi-trash',
      accept: () => this.delete(subject)
    });
  }

  private delete(subject: Subject) {
    this.subjectService.delete(subject.id).subscribe({
      next: () => {
        this.subjects = this.subjects.filter(s => s.id !== subject.id);
      },
      error: () => {
        this.messageService.add({ severity: 'error', summary: 'Erro', detail: 'Erro ao excluir matéria' });
      }
    });
  }
}
```

### Template HTML (`subjects.component.html`)

```html
<div class="subjects-page p-4">
  <p-toast />
  <p-confirmDialog />

  <div class="flex justify-content-between align-items-center mb-4">
    <h2 class="m-0">Matérias</h2>
    <p-button label="Nova Matéria" icon="pi pi-plus" (onClick)="openCreate()" />
  </div>

  @if (loading) {
    <div class="flex justify-content-center mt-6">
      <p-progressSpinner />
    </div>
  } @else if (subjects.length === 0) {
    <div class="text-center mt-6">
      <i class="pi pi-book text-4xl text-color-secondary mb-3 block"></i>
      <p class="text-color-secondary mb-3">Nenhuma matéria cadastrada ainda.</p>
      <p-button label="Criar primeira matéria" icon="pi pi-plus" (onClick)="openCreate()" />
    </div>
  } @else {
    <div class="flex flex-column gap-2">
      @for (subject of subjects; track subject.id) {
        <p-card>
          <div class="flex align-items-center justify-content-between">
            <a [routerLink]="['/subjects', subject.id]" class="font-medium text-lg no-underline text-color">
              {{ subject.name }}
            </a>
            <div class="flex gap-2">
              <p-button icon="pi pi-pencil" [text]="true" severity="secondary" (onClick)="openEdit(subject)" />
              <p-button icon="pi pi-trash" [text]="true" severity="danger" (onClick)="confirmDelete(subject)" />
            </div>
          </div>
        </p-card>
      }
    </div>
  }

  <p-dialog
    [(visible)]="dialogVisible"
    [header]="editingSubject ? 'Editar Matéria' : 'Nova Matéria'"
    [modal]="true"
    [style]="{ width: '90vw', maxWidth: '400px' }"
  >
    <form [formGroup]="form" (ngSubmit)="save()">
      <div class="field">
        <label for="name">Nome</label>
        <input
          id="name"
          type="text"
          pInputText
          formControlName="name"
          placeholder="Ex: Matemática"
          class="w-full mt-1"
          autofocus
        />
        @if (form.get('name')?.invalid && form.get('name')?.touched) {
          <small class="p-error">Nome é obrigatório (máx. 100 caracteres)</small>
        }
      </div>
    </form>
    <ng-template pTemplate="footer">
      <p-button label="Cancelar" [text]="true" severity="secondary" (onClick)="dialogVisible = false" />
      <p-button label="Salvar" icon="pi pi-check" (onClick)="save()" [disabled]="form.invalid" />
    </ng-template>
  </p-dialog>
</div>
```

### Spec do SubjectsComponent

```typescript
// frontend/src/app/features/subjects/subjects.component.spec.ts
import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SubjectsComponent } from './subjects.component';
import { SubjectService } from '../../core/services/subject.service';
import { of, throwError } from 'rxjs';
import { provideRouter } from '@angular/router';
import { provideAnimationsAsync } from '@angular/platform-browser/animations/async';

describe('SubjectsComponent', () => {
  let fixture: ComponentFixture<SubjectsComponent>;
  let component: SubjectsComponent;
  let mockSubjectService: jest.Mocked<SubjectService>;

  const mockSubjects = [
    { id: 1, name: 'Matemática', created_at: '' },
    { id: 2, name: 'Português', created_at: '' }
  ];

  beforeEach(async () => {
    mockSubjectService = {
      getAll: jest.fn().mockReturnValue(of(mockSubjects)),
      create: jest.fn(),
      update: jest.fn(),
      delete: jest.fn()
    } as any;

    await TestBed.configureTestingModule({
      imports: [SubjectsComponent],
      providers: [
        provideRouter([]),
        provideAnimationsAsync(),
        { provide: SubjectService, useValue: mockSubjectService }
      ]
    }).compileComponents();

    fixture = TestBed.createComponent(SubjectsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('carrega matérias ao iniciar', () => {
    expect(mockSubjectService.getAll).toHaveBeenCalled();
    expect(component.subjects.length).toBe(2);
  });

  it('adiciona matéria à lista ao criar', () => {
    const nova = { id: 3, name: 'História', created_at: '' };
    mockSubjectService.create.mockReturnValue(of(nova));
    component.form.patchValue({ name: 'História' });
    component.save();
    expect(component.subjects.length).toBe(3);
  });

  it('remove matéria da lista ao excluir', () => {
    mockSubjectService.delete.mockReturnValue(of(undefined as any));
    component['delete'](mockSubjects[0]);
    expect(component.subjects.length).toBe(1);
  });
});
```

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI não está habilitado em `core-config.yaml`.
> Validação de qualidade usará processo de revisão manual.

---

## File List

| Arquivo | Ação |
|---------|------|
| `frontend/src/app/core/models/subject.model.ts` | Criar |
| `frontend/src/app/core/services/subject.service.ts` | Criar |
| `frontend/src/app/core/services/subject.service.spec.ts` | Criar |
| `frontend/src/app/features/subjects/subjects.component.ts` | Modificar |
| `frontend/src/app/features/subjects/subjects.component.html` | Criar |
| `frontend/src/app/features/subjects/subjects.component.spec.ts` | Criar |

---

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Completion Notes
- Arquivo do componente é `subjects.ts` (classe `Subjects`) — naming convention herdada da Story 1.4, não `subjects.component.ts`
- `inject(FormBuilder)` usado como field initializer para evitar TS2729 ("used before initialization") com constructor injection
- Story Dev Notes usavam `jest.fn()` — adaptado para `vi.fn()` do Vitest (projeto usa `@angular/build:unit-test` com Vitest 4.x)
- Testes isolados com `of([...BASE_SUBJECTS])` (spread) para evitar mutação de dados compartilhados entre testes
- `deleteSubject()` público (em vez de `private delete()`) para viabilizar teste direto sem invocar `ConfirmationService`
- Suite completa: Tests: 107 passed, 0 failed (58 backend RSpec + 49 frontend Vitest), exit 0

### Debug Log
- TS2729 "fb used before initialization": resolvido com `inject(FormBuilder)` como field initializer
- NG0100 ExpressionChangedAfterChecked: removida chamada de `fixture.detectChanges()` em testes que modificam estado após a verificação inicial
- Mutação de `mockSubjects` entre testes: resolvido retornando `of([...BASE_SUBJECTS])` no mock do `getAll`

### File List

| Arquivo | Ação |
|---------|------|
| `frontend/src/app/core/models/subject.model.ts` | Criado |
| `frontend/src/app/core/services/subject.service.ts` | Criado |
| `frontend/src/app/core/services/subject.service.spec.ts` | Criado |
| `frontend/src/app/features/subjects/subjects/subjects.ts` | Modificado |
| `frontend/src/app/features/subjects/subjects/subjects.html` | Modificado |
| `frontend/src/app/features/subjects/subjects/subjects.spec.ts` | Criado |

---

## QA Results

### Review Date: 2026-03-15

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Implementação limpa e consistente com os padrões do projeto. O componente `Subjects` usa corretamente o padrão `inject()` do Angular moderno, gerencia estado imutável com spread array, e trata erros via toast. O `SubjectService` é simples e bem tipado. Template usa control flow syntax moderna do Angular 17+ (`@if`, `@for`, `@else`).

### Refactoring Performed

Nenhum. A implementação está limpa e não requer refatoração.

### Compliance Check

- Coding Standards: ✓ `inject()` pattern, imutabilidade de arrays, tipagem correta
- Project Structure: ✓ Arquivos em `features/subjects/subjects/` conforme convenção da Story 1.4
- Testing Strategy: ✓ Vitest com `vi.fn()`, `HttpTestingController` para service, mocks isolados
- All ACs Met: ✓ Todos os 11 ACs implementados e verificados

### Improvements Checklist

- [x] Todos os ACs cobertos por implementação
- [x] Testes unitários cobrindo: load, estado vazio, criar, editar, excluir, form inválido, dialog open/close
- [x] `HttpTestingController` com `httpMock.verify()` no afterEach — sem requests pendentes

### Security Review

Sem preocupações. Autenticação gerenciada pelo `AuthInterceptor` (Epic 1). Nenhum dado sensível exposto no componente. XSS protegido pelo binding Angular nativo (`{{ }}`).

### Performance Considerations

Sem preocupações. Array imutável com spread garante change detection correto. Chamadas HTTP completam em uma emissão (sem leak de subscription). Ordenação delegada ao backend.

### Files Modified During Review

Nenhum arquivo modificado. Review apenas.

### Gate Status

Gate: PASS → docs/qa/gates/2.2-frontend-crud-materias.yml

### Recommended Status

✓ Ready for Done
(Observações informativas — sem ação requerida pelo @dev)

**N1:** `constructor() {}` vazio no componente — cosmético, sem impacto funcional.
**N2:** Dev Notes e File List do corpo da story usam naming antigo (`subjects.component.*`) — desvio documentado no Dev Agent Record, sem impacto funcional.
**N3:** Frontmatter `status: Approved` conflita com body `## Status: Ready for Review` — alinhar após gate.
