---
id: "1.4"
title: "Layout Base + Navegação"
status: Approved
epic: 1
story: 4
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools:
  - manual-review
created_at: "2026-03-10"
updated_at: "2026-03-10"
---

# Story 1.4 — Layout Base + Navegação

## Status: Approved

## Story

**Como** usuário do EduTrack,
**Quero** ter uma estrutura visual consistente com navegação entre as seções do app,
**Para que** eu possa me mover facilmente entre Home, Matérias, Agenda e Metas.

---

## Contexto

Continuação da Story 1.3. Auth está funcionando. Esta story cria o shell do app: layout principal com bottom navigation (estilo mobile), header contextual e as páginas placeholder de cada seção. É a fundação visual sobre a qual todas as features serão construídas.

O layout deve ser mobile-first, adequado para uso como PWA em iOS e Android.

**Dependência:** Stories 1.1, 1.2 e 1.3 concluídas.

---

## Acceptance Criteria

- [ ] **AC1** — Shell component criado com slot para conteúdo e bottom navigation fixo
- [ ] **AC2** — Bottom navigation exibe 4 itens: Home (🏠), Matérias (📚), Agenda (📅), Metas (🎯)
- [ ] **AC3** — Item ativo no bottom nav é destacado visualmente
- [ ] **AC4** — Rotas privadas (`/`, `/subjects`, `/agenda`, `/goals`) usam o shell layout
- [ ] **AC5** — Rotas públicas (`/auth/login`, `/auth/signup`) não exibem bottom nav
- [ ] **AC6** — Header contextual exibe o título da seção atual
- [ ] **AC7** — Página placeholder criada para cada seção: Home, Matérias, Agenda, Metas
- [ ] **AC8** — Layout responsivo: conteúdo não fica sob o bottom nav (padding correto)
- [ ] **AC9** — Botão de logout acessível no header ou menu
- [ ] **AC10** — Transição suave entre rotas (sem flash de tela branca)
- [ ] **AC11** — Testes Jest cobrem: ShellComponent renderiza outlet, BottomNavComponent destaca item ativo, HeaderComponent exibe título correto e botão logout

---

## Tasks / Subtasks

- [x] **Task 1** — Criar Shell Component (AC1, AC4, AC5)
  - [x] Criar `frontend/src/app/layout/shell/shell.component.ts`
  - [x] Template com `<router-outlet>` para conteúdo e `<app-bottom-nav>` fixo no rodapé
  - [x] Configurar rotas: privadas usam shell como layout pai, públicas ficam fora

- [x] **Task 2** — Criar Bottom Navigation Component (AC2, AC3)
  - [x] Criar `frontend/src/app/layout/bottom-nav/bottom-nav.component.ts`
  - [x] Usar `RouterLink` e `RouterLinkActive` do Angular para destacar item ativo
  - [x] Ícones via PrimeIcons: `pi-home`, `pi-book`, `pi-calendar`, `pi-flag`
  - [x] Estilo fixo no rodapé com `position: fixed; bottom: 0`
  - [x] Altura do nav: 60px; padding inferior para iOS safe area (`env(safe-area-inset-bottom)`)

- [x] **Task 3** — Criar Header Component (AC6, AC9)
  - [x] Criar `frontend/src/app/layout/header/header.component.ts`
  - [x] Exibir título dinâmico baseado na rota atual (via `Router` + `toSignal`)
  - [x] Botão de logout no canto direito que chama `AuthService.logout()`
  - [x] Usar componente `p-toolbar` do PrimeNG

- [x] **Task 4** — Configurar rotas com layout (AC4, AC5)
  - [x] Atualizar `app.routes.ts` com estrutura de rotas aninhadas:
    - Rota pai: `ShellComponent` (com guard `authGuard`)
    - Filhas: `home`, `subjects`, `agenda`, `goals`
    - Rotas públicas separadas: `auth/login`, `auth/signup`

- [x] **Task 5** — Criar páginas placeholder (AC7)
  - [x] `frontend/src/app/features/home/home/home.html` — texto "Home (em breve)"
  - [x] `frontend/src/app/features/subjects/subjects/subjects.html` — texto "Matérias (em breve)"
  - [x] `frontend/src/app/features/agenda/agenda/agenda.html` — texto "Agenda (em breve)"
  - [x] `frontend/src/app/features/goals/goals/goals.html` — texto "Metas (em breve)"

- [x] **Task 6** — Ajustes de estilo global (AC8, AC10)
  - [x] Adicionar em `styles.scss`: `html, body { height: 100%; }` + `.placeholder-page`
  - [x] Configurar animação de rota em `app.config.ts` com `withViewTransitions()`
  - [x] `index.html` atualizado com `viewport-fit=cover` para iOS safe area

- [x] **Task 7** — Escrever testes Vitest (AC11)
  - [x] Criar `frontend/src/app/layout/shell/shell.component.spec.ts`
    - Testa que `<router-outlet>` e `<app-bottom-nav>` são renderizados
  - [x] Criar `frontend/src/app/layout/bottom-nav/bottom-nav.component.spec.ts`
    - Testa que os 4 itens de nav são renderizados
    - Testa rotas corretas em navItems
  - [x] Criar `frontend/src/app/layout/header/header.component.spec.ts`
    - Testa que `pageTitle()` retorna "EduTrack" para rota raiz
    - Testa que botão logout chama `AuthService.logout()`
  - [x] Executar `npm test` — 15 testes passando (4 suites)

- [ ] **Task 8** — Verificação final (todos os ACs)
  - [ ] Navegar entre todas as seções e confirmar item ativo no bottom nav
  - [ ] Confirmar que `/auth/login` não exibe bottom nav
  - [ ] Testar logout e redirecionamento para login
  - [ ] Testar em viewport mobile (375px de largura) via DevTools
  - [ ] Confirmar padding correto: conteúdo não fica atrás do bottom nav

---

## Dev Notes

### Estrutura de rotas com shell layout (`app.routes.ts`)

```typescript
import { Routes } from '@angular/router';
import { authGuard } from './core/guards/auth.guard';

export const routes: Routes = [
  // Rotas públicas (sem shell)
  {
    path: 'auth',
    children: [
      {
        path: 'login',
        loadComponent: () => import('./features/auth/login/login.component')
          .then(m => m.LoginComponent)
      },
      {
        path: 'signup',
        loadComponent: () => import('./features/auth/signup/signup.component')
          .then(m => m.SignupComponent)
      }
    ]
  },

  // Rotas privadas (com shell)
  {
    path: '',
    loadComponent: () => import('./layout/shell/shell.component')
      .then(m => m.ShellComponent),
    canActivate: [authGuard],
    children: [
      {
        path: '',
        loadComponent: () => import('./features/home/home.component')
          .then(m => m.HomeComponent)
      },
      {
        path: 'subjects',
        loadComponent: () => import('./features/subjects/subjects.component')
          .then(m => m.SubjectsComponent)
      },
      {
        path: 'agenda',
        loadComponent: () => import('./features/agenda/agenda.component')
          .then(m => m.AgendaComponent)
      },
      {
        path: 'goals',
        loadComponent: () => import('./features/goals/goals.component')
          .then(m => m.GoalsComponent)
      }
    ]
  },

  { path: '**', redirectTo: '' }
];
```

### Shell Component

```typescript
// frontend/src/app/layout/shell/shell.component.ts
import { Component } from '@angular/core';
import { RouterOutlet } from '@angular/router';
import { BottomNavComponent } from '../bottom-nav/bottom-nav.component';
import { HeaderComponent } from '../header/header.component';

@Component({
  selector: 'app-shell',
  standalone: true,
  imports: [RouterOutlet, BottomNavComponent, HeaderComponent],
  template: `
    <div class="shell">
      <app-header />
      <main class="shell__content">
        <router-outlet />
      </main>
      <app-bottom-nav />
    </div>
  `,
  styles: [`
    .shell {
      display: flex;
      flex-direction: column;
      height: 100dvh;
    }
    .shell__content {
      flex: 1;
      overflow-y: auto;
      padding-bottom: calc(60px + env(safe-area-inset-bottom));
    }
  `]
})
export class ShellComponent {}
```

### Bottom Navigation Component

```typescript
// frontend/src/app/layout/bottom-nav/bottom-nav.component.ts
import { Component } from '@angular/core';
import { RouterLink, RouterLinkActive } from '@angular/router';

interface NavItem {
  label: string;
  icon: string;
  route: string;
}

@Component({
  selector: 'app-bottom-nav',
  standalone: true,
  imports: [RouterLink, RouterLinkActive],
  template: `
    <nav class="bottom-nav">
      @for (item of navItems; track item.route) {
        <a
          class="bottom-nav__item"
          [routerLink]="item.route"
          routerLinkActive="bottom-nav__item--active"
          [routerLinkActiveOptions]="{ exact: item.route === '/' }"
        >
          <i class="pi {{ item.icon }}"></i>
          <span>{{ item.label }}</span>
        </a>
      }
    </nav>
  `,
  styles: [`
    .bottom-nav {
      position: fixed;
      bottom: 0;
      left: 0;
      right: 0;
      height: 60px;
      display: flex;
      background: var(--surface-card);
      border-top: 1px solid var(--surface-border);
      padding-bottom: env(safe-area-inset-bottom);
      z-index: 1000;
    }
    .bottom-nav__item {
      flex: 1;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: 2px;
      color: var(--text-color-secondary);
      text-decoration: none;
      font-size: 0.65rem;
      transition: color 0.2s;
    }
    .bottom-nav__item i {
      font-size: 1.2rem;
    }
    .bottom-nav__item--active {
      color: var(--primary-color);
    }
  `]
})
export class BottomNavComponent {
  navItems: NavItem[] = [
    { label: 'Home', icon: 'pi-home', route: '/' },
    { label: 'Matérias', icon: 'pi-book', route: '/subjects' },
    { label: 'Agenda', icon: 'pi-calendar', route: '/agenda' },
    { label: 'Metas', icon: 'pi-flag', route: '/goals' }
  ];
}
```

### Header Component

```typescript
// frontend/src/app/layout/header/header.component.ts
import { Component, computed } from '@angular/core';
import { Router, NavigationEnd } from '@angular/router';
import { filter, map } from 'rxjs/operators';
import { toSignal } from '@angular/core/rxjs-interop';
import { AuthService } from '../../core/services/auth.service';
import { ToolbarModule } from 'primeng/toolbar';
import { ButtonModule } from 'primeng/button';

@Component({
  selector: 'app-header',
  standalone: true,
  imports: [ToolbarModule, ButtonModule],
  template: `
    <p-toolbar>
      <div class="p-toolbar-group-start">
        <span class="font-bold text-lg">{{ pageTitle() }}</span>
      </div>
      <div class="p-toolbar-group-end">
        <p-button
          icon="pi pi-sign-out"
          [text]="true"
          severity="secondary"
          (onClick)="logout()"
          pTooltip="Sair"
        />
      </div>
    </p-toolbar>
  `
})
export class HeaderComponent {
  private routeTitles: Record<string, string> = {
    '/': 'EduTrack',
    '/subjects': 'Matérias',
    '/agenda': 'Agenda',
    '/goals': 'Metas'
  };

  private currentUrl = toSignal(
    this.router.events.pipe(
      filter(e => e instanceof NavigationEnd),
      map((e: any) => e.urlAfterRedirects)
    ),
    { initialValue: this.router.url }
  );

  pageTitle = computed(() => this.routeTitles[this.currentUrl()] ?? 'EduTrack');

  constructor(private router: Router, private auth: AuthService) {}

  logout() {
    this.auth.logout().subscribe();
  }
}
```

### `styles.scss` global atualizado

```scss
@import "primeicons/primeicons.css";

*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html, body {
  height: 100%;
  font-family: var(--font-family);
  background-color: var(--surface-ground);
  color: var(--text-color);
}
```

### `app.config.ts` com view transitions

```typescript
import { withViewTransitions } from '@angular/router';

providers: [
  provideRouter(routes, withViewTransitions()),
  ...
]
```

### iOS Safe Area

O `env(safe-area-inset-bottom)` garante que o bottom nav não fica atrás do indicador home do iPhone. Requer meta tag no `index.html`:

```html
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
```

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI não está habilitado em `core-config.yaml`.
> Validação de qualidade usará processo de revisão manual.

---

## File List

| Arquivo | Ação |
|---------|------|
| `frontend/src/app/layout/shell/shell.component.ts` | Criado |
| `frontend/src/app/layout/bottom-nav/bottom-nav.component.ts` | Criado |
| `frontend/src/app/layout/header/header.component.ts` | Criado |
| `frontend/src/app/features/home/home/home.html` | Modificado (placeholder) |
| `frontend/src/app/features/subjects/subjects/subjects.html` | Modificado (placeholder) |
| `frontend/src/app/features/agenda/agenda/agenda.html` | Modificado (placeholder) |
| `frontend/src/app/features/goals/goals/goals.html` | Modificado (placeholder) |
| `frontend/src/app/app.routes.ts` | Modificado (estrutura com shell) |
| `frontend/src/app/app.config.ts` | Modificado (withViewTransitions) |
| `frontend/src/styles.scss` | Modificado (html/body height, placeholder-page) |
| `frontend/src/index.html` | Modificado (viewport-fit=cover) |
| `frontend/src/app/layout/shell/shell.component.spec.ts` | Criado |
| `frontend/src/app/layout/bottom-nav/bottom-nav.component.spec.ts` | Criado |
| `frontend/src/app/layout/header/header.component.spec.ts` | Criado |

---

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Completion Notes
- Shell component criado com `inject()` para evitar uso de `this` antes da inicialização
- Rotas refatoradas para estrutura aninhada com ShellComponent como layout pai
- Features já existiam (`home/home/home.ts`, etc.) com estrutura de subpastas — placeholders atualizados nos HTMLs
- Test runner é Vitest (não Jest/Jasmine) — specs usam `vi.fn()` para mocks
- 15 testes passando em 4 suites
- Task 8 (verificação manual em browser) pendente para validação do usuário

### Debug Log
- HeaderComponent: erro TS2729 (`router used before initialization`) corrigido usando `inject()` em vez de `constructor` DI
- Spec do Header: `jasmine.SpyObj` não disponível (não é Jasmine) — corrigido para `vi.fn()` do Vitest
