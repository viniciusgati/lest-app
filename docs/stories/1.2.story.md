---
id: "1.2"
title: "Setup Angular PWA + PrimeNG"
status: Draft
epic: 1
story: 2
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools:
  - manual-review
created_at: "2026-03-10"
updated_at: "2026-03-10"
---

# Story 1.2 — Setup Angular PWA + PrimeNG

## Status: Draft

## Story

**Como** desenvolvedor do projeto,
**Quero** ter o frontend Angular configurado como PWA com PrimeNG e integrado ao Rails,
**Para que** o app possa ser instalado em iOS/Android e o time tenha a base visual pronta para desenvolver as telas.

---

## Contexto

Continuação da Story 1.1. O backend Rails já está rodando. Esta story configura o frontend Angular 17+ como PWA, instala o PrimeNG como biblioteca de componentes e configura o build para gerar os estáticos em `backend/public/`, onde o Rails os servirá.

**Dependência:** Story 1.1 concluída.

---

## Acceptance Criteria

- [ ] **AC1** — Aplicação Angular 17+ criada em `/frontend`
- [ ] **AC2** — PWA configurado via `@angular/pwa` (manifesto, service worker, ícones placeholder)
- [ ] **AC3** — PrimeNG instalado e tema padrão aplicado (Lara Dark ou Aura)
- [ ] **AC4** — `angular.json` com `outputPath` apontando para `../backend/public`
- [ ] **AC5** — `ng build` executa sem erros e gera arquivos em `backend/public/`
- [ ] **AC6** — Rails serve o Angular: acessar `http://localhost:3000` exibe a tela inicial Angular
- [ ] **AC7** — Rota catch-all configurada no Rails para suportar navegação client-side do Angular (`*path → public/index.html`)
- [ ] **AC8** — Script de build combinado disponível na raiz: `build.sh` que executa `ng build` e garante que `backend/public/` está atualizado
- [ ] **AC9** — App instalável como PWA: manifesto válido verificado via DevTools do Chrome (aba Application)
- [ ] **AC10** — Estrutura de módulos/pastas do Angular definida e documentada

---

## Tasks / Subtasks

- [ ] **Task 1** — Criar aplicação Angular em `/frontend` (AC1)
  - [ ] Executar `ng new frontend --routing --style=scss --ssr=false`
  - [ ] Remover conteúdo de demonstração padrão do Angular
  - [ ] Verificar `ng serve` funciona localmente na porta 4200

- [ ] **Task 2** — Configurar PWA (AC2, AC9)
  - [ ] Executar `ng add @angular/pwa` dentro de `/frontend`
  - [ ] Editar `manifest.webmanifest`: nome "EduTrack", short_name "EduTrack", theme_color, background_color
  - [ ] Adicionar ícones placeholder (512x512, 192x192) em `frontend/src/assets/icons/`
  - [ ] Verificar `ngsw-config.json` gerado

- [ ] **Task 3** — Instalar e configurar PrimeNG (AC3)
  - [ ] Executar `npm install primeng @primeng/themes primeicons`
    - [ ] Importar apenas `primeicons/primeicons.css` em `styles.scss` (tema Aura é injetado via `providePrimeNG`, não via CSS import)
  - [ ] Adicionar `providePrimeNG` com tema configurado em `app.config.ts`
  - [ ] Testar com um componente PrimeNG simples (ex: `p-button`) na `AppComponent`

- [ ] **Task 4** — Configurar outputPath para `backend/public/` (AC4, AC5)
  - [ ] Editar `angular.json`: `"outputPath": "../backend/public"`
  - [ ] Garantir que `backend/public/` está no `.gitignore` (verificar Story 1.1)
  - [ ] Executar `ng build` e confirmar arquivos gerados em `backend/public/`

- [ ] **Task 5** — Configurar catch-all no Rails (AC6, AC7)
  - [ ] Adicionar em `backend/config/routes.rb`:
    ```ruby
    get '*path', to: 'application#fallback_index_html', constraints: ->(request) { !request.xhr? && request.format.html? }
    ```
  - [ ] Adicionar método `fallback_index_html` em `ApplicationController`:
    ```ruby
    def fallback_index_html
      render file: Rails.root.join('public', 'index.html')
    end
  ```
  - [ ] Testar: `rails server` + acessar `http://localhost:3000` exibe Angular

- [ ] **Task 6** — Criar script de build combinado (AC8)
  - [ ] Criar `build.sh` na raiz:
    ```bash
    #!/bin/bash
    echo "Building Angular..."
    cd frontend && npm run build
    echo "Build complete. Files in backend/public/"
    ```
  - [ ] Dar permissão de execução: `chmod +x build.sh`

- [ ] **Task 7** — Definir estrutura de pastas do Angular (AC10)
  - [ ] Criar estrutura base em `frontend/src/app/`:
    ```
    app/
    ├── core/           # Serviços singleton, guards, interceptors
    │   ├── services/
    │   └── interceptors/
    ├── shared/         # Componentes e pipes reutilizáveis
    │   └── components/
    ├── features/       # Módulos de funcionalidade
    │   ├── home/
    │   ├── subjects/
    │   ├── agenda/
    │   ├── goals/
    │   └── auth/
    └── layout/         # Shell, nav, header
    ```
  - [ ] Criar arquivo `frontend/src/app/app.routes.ts` com rotas placeholder para cada feature
  - [ ] Documentar estrutura em Dev Notes desta story

- [ ] **Task 8** — Testes unitários Angular (AC1)
  - [ ] Executar `ng test --watch=false --browsers=ChromeHeadless`
  - [ ] Confirmar todos os testes do scaffold passam sem erros
  - [ ] Adicionar `"ChromeHeadless"` a `browsers` no `karma.conf.js` se necessário

- [ ] **Task 9** — Verificação final (AC6, AC9)
  - [ ] Confirmar `http://localhost:3000` serve Angular via Rails
  - [ ] Verificar PWA via Chrome DevTools > Application > Manifest
  - [ ] Confirmar navegação client-side funciona (ex: `/subjects` não quebra ao recarregar)

---

## Dev Notes

### Versões
- Angular CLI: 17+
- Node: 18+
- PrimeNG: 17+ (compatível com Angular 17)

### Comando de criação do projeto

```bash
cd /path/to/monorepo
ng new frontend --routing --style=scss --ssr=false
cd frontend
ng add @angular/pwa
npm install primeng @primeng/themes primeicons
```

### Configuração do PrimeNG em `app.config.ts`

```typescript
import { ApplicationConfig } from '@angular/core';
import { provideRouter } from '@angular/router';
import { provideAnimationsAsync } from '@angular/platform-browser/animations/async';
import { providePrimeNG } from 'primeng/config';
import Aura from '@primeng/themes/aura';
import { routes } from './app.routes';

export const appConfig: ApplicationConfig = {
  providers: [
    provideRouter(routes),
    provideAnimationsAsync(),
    providePrimeNG({
      theme: {
        preset: Aura,
        options: { darkModeSelector: '.app-dark' }
      }
    })
  ]
};
```

### `styles.scss` base

> **Nota PrimeNG 17+ com Aura:** O tema é injetado programaticamente via `providePrimeNG` em `app.config.ts`. NÃO importar `primeng/resources/themes/*.css` (legado v16). Importar apenas primeicons.

```scss
/* Apenas primeicons — tema Aura é injetado via providePrimeNG() */
@import "primeicons/primeicons.css";

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  font-family: var(--font-family);
  background-color: var(--surface-ground);
  color: var(--text-color);
}
```

### `angular.json` — outputPath

```json
"build": {
  "options": {
    "outputPath": "../backend/public",
    ...
  }
}
```

### Catch-all no Rails (`config/routes.rb`)

```ruby
Rails.application.routes.draw do
  get '/health', to: 'health#index'

  # API routes virão aqui (namespaced /api/v1)

  # Catch-all: delega para Angular (deve ser o ÚLTIMO)
  get '*path',
      to: 'application#fallback_index_html',
      constraints: ->(req) { !req.xhr? && req.format.html? }
end
```

### `ApplicationController` com fallback

```ruby
class ApplicationController < ActionController::API
  def fallback_index_html
    render file: Rails.root.join('public', 'index.html'), layout: false
  end
end
```

### Estrutura de rotas Angular (`app.routes.ts`)

```typescript
export const routes: Routes = [
  { path: '', loadComponent: () => import('./features/home/home.component').then(m => m.HomeComponent) },
  { path: 'subjects', loadComponent: () => import('./features/subjects/subjects.component').then(m => m.SubjectsComponent) },
  { path: 'agenda', loadComponent: () => import('./features/agenda/agenda.component').then(m => m.AgendaComponent) },
  { path: 'goals', loadComponent: () => import('./features/goals/goals.component').then(m => m.GoalsComponent) },
  { path: 'auth/login', loadComponent: () => import('./features/auth/login/login.component').then(m => m.LoginComponent) },
  { path: '**', redirectTo: '' }
];
```

### Estrutura de pastas Angular ao final desta story

```
frontend/src/app/
├── core/
│   ├── services/        # AuthService, ApiService (story 1.3+)
│   └── interceptors/    # JWT interceptor (story 1.3)
├── shared/
│   └── components/      # Componentes reutilizáveis
├── features/
│   ├── home/            # Dashboard (story 5.x)
│   ├── subjects/        # Matérias & Temas (story 2.x)
│   ├── agenda/          # Calendário (story 4.x)
│   ├── goals/           # Metas semanais (story 3.x)
│   └── auth/            # Login/Cadastro (story 1.3)
└── layout/
    └── shell/           # Bottom nav + layout principal
```

### Importante: API calls durante desenvolvimento

Durante o desenvolvimento local, o Angular em `ng serve` (porta 4200) precisa fazer proxy para o Rails (porta 3000). Criar `frontend/proxy.conf.json`:

```json
{
  "/api": {
    "target": "http://localhost:3000",
    "secure": false
  },
  "/health": {
    "target": "http://localhost:3000",
    "secure": false
  }
}
```

E configurar em `angular.json`:
```json
"serve": {
  "options": {
    "proxyConfig": "proxy.conf.json"
  }
}
```

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI não está habilitado em `core-config.yaml`.
> Validação de qualidade usará processo de revisão manual.

---

## File List

| Arquivo | Ação |
|---------|------|
| `frontend/` (app Angular completa) | Criar |
| `frontend/src/app/app.config.ts` | Modificar |
| `frontend/src/app/app.routes.ts` | Modificar |
| `frontend/src/styles.scss` | Modificar |
| `frontend/src/manifest.webmanifest` | Modificar |
| `frontend/ngsw-config.json` | Criar (via ng add) |
| `frontend/proxy.conf.json` | Criar |
| `frontend/angular.json` | Modificar (outputPath) |
| `backend/config/routes.rb` | Modificar (catch-all) |
| `backend/app/controllers/application_controller.rb` | Modificar (fallback) |
| `build.sh` | Criar |

---

## Dev Agent Record

### Completion Notes
_Preenchido pelo @dev ao completar_

### Debug Log
_Referências a logs de debug se necessário_
