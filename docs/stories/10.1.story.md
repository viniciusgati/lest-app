---
id: "10.1"
title: "Backend + Frontend — Streak e Gamificação Básica"
status: Approved
epic: 10
story: 1
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools:
  - manual-review
created_at: "2026-03-22"
updated_at: "2026-03-22"
---

# Story 10.1 — Backend + Frontend — Streak e Gamificação Básica

## Status: Ready for Review

## Story

**Como** usuário do EduTrack,
**Quero** ver minha sequência de dias consecutivos de estudo (streak),
**Para que** me sinta motivado a manter a consistência e não quebrar a sequência.

---

## Contexto

Streak é um dos mecanismos de engajamento mais efetivos em apps de estudo (Duolingo, Anki). Um "dia de estudo" é contabilizado quando o usuário tem pelo menos 1 sessão com `status: 'completed'` naquele dia. O streak é calculado a partir de ontem para trás (hoje pode ainda não ter sessão, mas a sequência não deve quebrar).

**Escopo desta story:**
- Cálculo de streak no backend via endpoint dedicado
- Exibição do streak no Dashboard (Home) com ícone de fogo e número
- Exibição de melhor streak histórico
- Mensagens motivacionais baseadas no streak atual

**Fora de escopo:**
- Notificações de streak prestes a quebrar (Epic futuro)
- Achievements/badges (Epic futuro)
- Streak freeze (Epic futuro)

---

## Acceptance Criteria

- [ ] **AC1** — `GET /api/v1/metrics/streak` retorna `{ current_streak, longest_streak, last_study_date, studied_today }`
- [ ] **AC2** — `current_streak`: número de dias consecutivos com sessão completada, contando desde ontem para trás (se hoje já tem sessão, conta hoje também)
- [ ] **AC3** — `longest_streak`: maior sequência histórica já registrada pelo usuário
- [ ] **AC4** — `last_study_date`: data da última sessão completada (ISO 8601 ou null)
- [ ] **AC5** — `studied_today`: boolean indicando se há sessão completada hoje
- [ ] **AC6** — Dias sem sessão completada quebram o streak
- [ ] **AC7** — Frontend: widget de streak na tela Home exibindo `🔥 N dias` (ícone de fogo + número)
- [ ] **AC8** — Frontend: se `current_streak == 0`, exibir "Comece hoje!" incentivando o usuário
- [ ] **AC9** — Frontend: tooltip ou linha abaixo com "Recorde: X dias"
- [ ] **AC10** — Frontend: cor do widget muda conforme streak (cinza=0, azul=1-6, laranja=7-29, dourado=30+)
- [ ] **AC11** — RSpec cobre: streak de 0, streak de N dias, quebra de streak, studied_today

---

## Tasks / Subtasks

- [x] **Task 1** — Backend: action `streak` em `MetricsController` (AC1–AC6)
  - [x] Adicionar action `streak` ao `MetricsController`
  - [x] Calcular `current_streak`: iterar dias para trás a partir de hoje/ontem até encontrar dia sem sessão
  - [x] Calcular `longest_streak`: janela deslizante sobre todos os dias com sessão completada
  - [x] Calcular `last_study_date`: data mais recente com sessão completada
  - [x] Calcular `studied_today`: verifica se `study_dates.first == today`
  - [x] Rota: `get :streak` no namespace metrics

- [x] **Task 2** — Backend: algoritmo de streak (AC2, AC3, AC6)
  - [x] `pluck("DISTINCT DATE(scheduled_date)")` com `Arel.sql` para evitar warning
  - [x] `current_streak`: iteração com `Set#include?` decrescendo `check_date`
  - [x] `longest_streak`: sliding window sobre array ascendente

- [x] **Task 3** — Frontend: `MetricsService#getStreak()` (AC7)
  - [x] `getStreak(): Observable<StreakData>` em `MetricsService`
  - [x] Interface `StreakData` em `metrics.model.ts`

- [x] **Task 4** — Frontend: `StreakWidget` standalone (AC7–AC10)
  - [x] Componente em `frontend/src/app/shared/components/streak-widget/`
  - [x] `@Input({ required: true }) streak!: StreakData`
  - [x] `🔥 N dias` ou `📚 Comece hoje!`
  - [x] `streakClass`: zero/low/medium/high + cores CSS
  - [x] Tooltip PrimeNG: "Recorde: X dias"

- [x] **Task 5** — Frontend: integrar widget na Home (AC7)
  - [x] `<app-streak-widget>` no `HomeComponent` junto ao botão "Gerar semana"
  - [x] `getStreak()` no `forkJoin` do `ngOnInit`

- [x] **Task 6** — Testes RSpec (AC11)
  - [x] 5 casos: streak 0, 3 consecutivos, gap quebra streak, longest_streak, studied_today

---

## Dev Notes

### Algoritmo de streak (Ruby)

```ruby
def streak
  # Datas únicas com sessão completada, ordenadas desc
  study_dates = StudySession
    .joins(topic: :subject)
    .where(subjects: { user_id: current_user.id })
    .where(status: 'completed')
    .where('questions_done > 0')
    .pluck("DISTINCT DATE(scheduled_date)")
    .map(&:to_date)
    .sort
    .reverse

  today = Date.today
  studied_today = study_dates.first == today
  last_study_date = study_dates.first

  # current_streak: contar a partir de hoje (ou ontem se hoje não estudou)
  current_streak = 0
  check_date = studied_today ? today : today - 1
  study_set = study_dates.to_set

  while study_set.include?(check_date)
    current_streak += 1
    check_date -= 1
  end

  # longest_streak: percorrer todas as datas
  longest = 0
  current = 0
  prev_date = nil

  study_dates.reverse.each do |date|
    if prev_date.nil? || date == prev_date + 1
      current += 1
      longest = [longest, current].max
    else
      current = 1
    end
    prev_date = date
  end

  render json: {
    current_streak: current_streak,
    longest_streak: longest,
    last_study_date: last_study_date&.iso8601,
    studied_today: studied_today
  }
end
```

### Interface TypeScript

```typescript
interface StreakData {
  currentStreak: number;
  longestStreak: number;
  lastStudyDate: string | null;
  studiedToday: boolean;
}
```

### CSS classes para cor do streak

```scss
// streak-widget.component.scss
.streak-zero   { color: var(--text-color-secondary); }
.streak-low    { color: #6366f1; }   // 1-6 dias: azul
.streak-medium { color: #f97316; }   // 7-29 dias: laranja
.streak-high   { color: #eab308; }   // 30+ dias: dourado
```

### Rota

```ruby
get 'metrics/streak', to: 'metrics#streak'
```

### Nota sobre "dia de estudo"

Um dia conta se tem `questions_done > 0` — garante que sessões vazias (sem questões respondidas) não contam como estudo real. Este critério pode ser relaxado futuramente para `status: 'completed'` sem filtro de questões.

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled

---

## File List

| Arquivo | Ação |
|---------|------|
| `backend/app/controllers/api/v1/metrics_controller.rb` | Modificar |
| `backend/config/routes.rb` | Modificar |
| `backend/spec/requests/api/v1/metrics_spec.rb` | Modificar |
| `frontend/src/app/core/services/metrics.service.ts` | Modificar |
| `frontend/src/app/core/models/metrics.model.ts` | Modificar |
| `frontend/src/app/shared/components/streak-widget/streak-widget.component.ts` | Criar |
| `frontend/src/app/shared/components/streak-widget/streak-widget.component.html` | Criar |
| `frontend/src/app/shared/components/streak-widget/streak-widget.component.scss` | Criar |
| `frontend/src/app/features/home/home.component.ts` | Modificar |
| `frontend/src/app/features/home/home.component.html` | Modificar |

---

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Completion Notes
- `MetricsController#streak`: algoritmo com Set para O(1) lookup por data
- `studied_today`: verifica `study_dates.first == Date.today`
- `longest_streak`: sliding window sobre array ascendente
- `StreakWidget` standalone com `streakClass` getter e tooltip PrimeNG
- `StreakData` interface com snake_case (alinhado com JSON da API)
- Home integra streak no `forkJoin` e exibe widget ao lado de "Gerar semana"
- 5 RSpec novos; 231/231 backend total; 253/253 frontend total

### Debug Log
- `pluck("DISTINCT DATE(...)")` gerava deprecation warning — corrigido com `Arel.sql(...)`

### File List
| Arquivo | Ação |
|---------|------|
| `backend/app/controllers/api/v1/metrics_controller.rb` | Modificado |
| `backend/config/routes.rb` | Modificado |
| `backend/spec/requests/api/v1/metrics_spec.rb` | Modificado |
| `frontend/src/app/core/services/metrics.service.ts` | Modificado |
| `frontend/src/app/core/models/metrics.model.ts` | Modificado |
| `frontend/src/app/shared/components/streak-widget/streak-widget.ts` | Criado |
| `frontend/src/app/features/home/home/home.ts` | Modificado |
| `frontend/src/app/features/home/home/home.html` | Modificado |
| `frontend/src/app/features/home/home/home.spec.ts` | Modificado |
