---
id: "8.4"
title: "Backend — Índices de Banco + Rate Limiting"
status: Approved
epic: 8
story: 4
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools:
  - manual-review
created_at: "2026-03-22"
updated_at: "2026-03-22"
---

# Story 8.4 — Backend — Índices de Banco + Rate Limiting

## Status: Draft

## Story

**Como** operador do EduTrack,
**Quero** que as queries mais frequentes usem índices otimizados e que a API tenha proteção contra abuso,
**Para que** o sistema mantenha performance e segurança conforme a base de usuários cresce.

---

## Contexto

Duas melhorias de infraestrutura backend agrupadas por serem de baixo risco e alta eficiência:

**1. Índices:** Queries frequentes sem índice composto identificadas:
- `study_sessions` filtradas por `status + scheduled_date` (agenda, schedule generator)
- `topics` filtradas por `subject_id + next_review` (SM-2 strategy)
- `study_sessions` filtradas por `topic_id + status` (métricas por topic)

**2. Rate Limiting com `rack-attack`:** Endpoints críticos sem proteção:
- Auth endpoints (`/auth/login`, `/auth/signup`) — vulneráveis a brute force e enumeração
- API geral — sem throttle por usuário autenticado

---

## Acceptance Criteria

- [ ] **AC1** — `rack-attack` gem adicionada ao `Gemfile`
- [ ] **AC2** — Migration adiciona índices compostos em `study_sessions` e `topics`
- [ ] **AC3** — Índice composto `[topic_id, status, scheduled_date]` em `study_sessions`
- [ ] **AC4** — Índice composto `[subject_id, next_review]` em `topics`
- [ ] **AC5** — Rate limit: `POST /auth/login` e `POST /auth/signup` limitados a 10 req/min por IP
- [ ] **AC6** — Rate limit: API geral limitada a 300 req/min por usuário autenticado (via JWT user_id)
- [ ] **AC7** — Respostas com rate limit retornam HTTP 429 com body `{ error: "Rate limit exceeded. Try again in X seconds." }`
- [ ] **AC8** — `rack-attack` configurado para usar cache Rails (memory_store em dev, pode usar Redis em prod)
- [ ] **AC9** — Testes RSpec para rate limiting: simular burst de requests e verificar 429
- [ ] **AC10** — Todos os testes existentes continuam passando (sem regressão)

---

## Tasks / Subtasks

- [ ] **Task 1** — Instalar `rack-attack` (AC1)
  - [ ] Adicionar `gem 'rack-attack', '~> 6.7'` ao `Gemfile`
  - [ ] Executar `bundle install`

- [ ] **Task 2** — Migration: índices compostos (AC2, AC3, AC4)
  - [ ] Criar migration `add_performance_indexes`
  - [ ] `add_index :study_sessions, [:topic_id, :status, :scheduled_date]`
  - [ ] `add_index :study_sessions, [:status, :scheduled_date]` (para ScheduleGenerator)
  - [ ] `add_index :topics, [:subject_id, :next_review]`

- [ ] **Task 3** — Configurar Rack::Attack (AC5, AC6, AC7, AC8)
  - [ ] Criar `config/initializers/rack_attack.rb`
  - [ ] Throttle `/auth/login` e `/auth/signup`: 10 req/60s por IP
  - [ ] Throttle API geral (`/api/`): 300 req/60s por IP (fallback sem autenticação)
  - [ ] Customizar resposta 429: JSON com mensagem e `Retry-After` header
  - [ ] Configurar `Rack::Attack.cache.store = Rails.cache`

- [ ] **Task 4** — Montar Rack::Attack no middleware (AC8)
  - [ ] Verificar se `config/application.rb` já inclui `config.middleware.use Rack::Attack`
  - [ ] Se não, adicionar após `ActionDispatch::Flash`

- [ ] **Task 5** — Testes RSpec (AC9, AC10)
  - [ ] `spec/requests/rack_attack_spec.rb`
  - [ ] Simular 11 requests para `/auth/login` e verificar 429 no 11º
  - [ ] Verificar body e headers da resposta 429

---

## Dev Notes

### Gemfile

```ruby
gem 'rack-attack', '~> 6.7'
```

### Migration de índices

```ruby
class AddPerformanceIndexes < ActiveRecord::Migration[7.1]
  def change
    add_index :study_sessions, [:topic_id, :status, :scheduled_date],
              name: 'idx_sessions_topic_status_date'
    add_index :study_sessions, [:status, :scheduled_date],
              name: 'idx_sessions_status_date'
    add_index :topics, [:subject_id, :next_review],
              name: 'idx_topics_subject_next_review'
  end
end
```

### config/initializers/rack_attack.rb

```ruby
class Rack::Attack
  # Throttle auth endpoints: 10 req/min por IP
  throttle('auth/ip', limit: 10, period: 60) do |req|
    req.ip if req.path.start_with?('/api/v1/auth/') &&
              ['POST'].include?(req.request_method)
  end

  # Throttle API geral: 300 req/min por IP
  throttle('api/ip', limit: 300, period: 60) do |req|
    req.ip if req.path.start_with?('/api/')
  end

  # Resposta customizada 429
  self.throttled_responder = lambda do |env|
    now = Time.now
    match_data = env['rack.attack.match_data']
    retry_after = match_data[:period] - (now.to_i % match_data[:period])

    [
      429,
      {
        'Content-Type' => 'application/json',
        'Retry-After' => retry_after.to_s
      },
      [{ error: "Rate limit exceeded. Try again in #{retry_after} seconds." }.to_json]
    ]
  end
end

Rack::Attack.cache.store = Rails.cache
```

### Por que índices compostos?

- `ScheduleGenerator#remove_existing_auto_sessions` filtra por `status: 'scheduled'` + `scheduled_date range` — sem índice, full scan em sessions
- `sm2_topics` faz `WHERE next_review <= ?` em topics — com índice composto em `[subject_id, next_review]`, a query usa o índice para filtrar por subject primeiro
- `MetricsController#topics` agrupa sessões por `topic_id + status` — índice em `[topic_id, status]` acelera o GROUP BY

### Nota sobre Redis em produção

Para Railway, adicionar Redis como service e configurar:
```ruby
# config/environments/production.rb
config.cache_store = :redis_cache_store, { url: ENV['REDIS_URL'] }
```
Esta story configura `Rails.cache` apenas — não provisiona Redis (escopo do @devops).

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled

---

## File List

| Arquivo | Ação |
|---------|------|
| `backend/Gemfile` | Modificar |
| `backend/db/migrate/YYYYMMDD_add_performance_indexes.rb` | Criar |
| `backend/config/initializers/rack_attack.rb` | Criar |
| `backend/config/application.rb` | Verificar/Modificar |
| `backend/spec/requests/rack_attack_spec.rb` | Criar |

---

## Dev Agent Record

### Agent Model Used
_a ser preenchido_

### Completion Notes
_a ser preenchido_

### Debug Log
_a ser preenchido_

### File List
_a ser preenchido_
