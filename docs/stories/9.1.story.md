---
id: "9.1"
title: "Frontend — Timer de Sessão In-app"
status: Approved
epic: 9
story: 1
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools:
  - manual-review
created_at: "2026-03-22"
updated_at: "2026-03-22"
---

# Story 9.1 — Frontend — Timer de Sessão In-app

## Status: Draft

## Story

**Como** usuário do EduTrack,
**Quero** cronometrar minhas sessões de estudo diretamente no app,
**Para que** o campo `actual_minutes` seja preenchido automaticamente ao registrar o resultado.

---

## Contexto

Atualmente o campo `actual_minutes` raramente é preenchido porque o usuário precisa saber quanto tempo estudou. A maioria das sessões tem `actual_minutes: null` nos dados. Esta story implementa um componente de timer (Start / Pause / Stop) na tela de Agenda, dentro do card/modal de sessão ativa, que ao ser parado preenche `actual_minutes` automaticamente no formulário de conclusão.

**Fluxo:**
1. Usuário abre sessão agendada na Agenda
2. Clica "Iniciar Timer" → cronômetro começa (HH:MM:SS)
3. Pode pausar/retomar
4. Ao clicar "Parar" → tempo total é convertido para minutos e preenche o campo no formulário de conclusão
5. Usuário confirma resultado → session marcada como `completed` com `actual_minutes` correto

---

## Acceptance Criteria

- [ ] **AC1** — Componente `StudyTimerComponent` criado em `frontend/src/app/shared/components/study-timer/`
- [ ] **AC2** — Timer exibe tempo no formato `MM:SS` (até 99:59) ou `H:MM:SS` para sessões longas
- [ ] **AC3** — Botões: "Iniciar", "Pausar/Retomar", "Parar" com estados corretos (disable quando não aplicável)
- [ ] **AC4** — Timer emite evento `(timerStopped)` com `minutes: number` ao ser parado
- [ ] **AC5** — Na tela de Agenda, o timer é exibido dentro do modal/panel de detalhes da sessão ativa
- [ ] **AC6** — Ao parar o timer, `actual_minutes` no formulário de conclusão é preenchido automaticamente
- [ ] **AC7** — Timer persiste estado durante navegação na mesma sessão (não reseta ao minimizar modal)
- [ ] **AC8** — Se `actual_minutes` já estiver preenchido manualmente, o timer não sobrescreve
- [ ] **AC9** — Timer para automaticamente se o usuário navegar para outra rota
- [ ] **AC10** — Testes unitários Vitest cobrem: start, pause, resume, stop e cálculo de minutos

---

## Tasks / Subtasks

- [ ] **Task 1** — Criar `StudyTimerComponent` (AC1, AC2, AC3, AC4)
  - [ ] Gerar componente standalone em `frontend/src/app/shared/components/study-timer/`
  - [ ] Estado interno: `status: 'idle' | 'running' | 'paused'`, `elapsedMs: number`, `intervalId`
  - [ ] Método `start()`, `pause()`, `resume()`, `stop()`
  - [ ] `stop()` emite `EventEmitter<number>` com minutos totais (arredondado para cima)
  - [ ] Template com display de tempo formatado + 3 botões PrimeNG
  - [ ] `OnDestroy`: limpar interval e emitir parada se estiver rodando

- [ ] **Task 2** — Lógica de formatação de tempo (AC2)
  - [ ] Pipe ou método `formatTime(ms: number): string`
  - [ ] `< 60min`: exibe `MM:SS`
  - [ ] `>= 60min`: exibe `H:MM:SS`

- [ ] **Task 3** — Integrar timer no modal de sessão da Agenda (AC5, AC6, AC7, AC8)
  - [ ] Localizar componente que exibe detalhe/ação da sessão em `AgendaComponent` ou componente filho
  - [ ] Adicionar `<app-study-timer>` acima do formulário de conclusão
  - [ ] Binding: `(timerStopped)="onTimerStopped($event)"`
  - [ ] `onTimerStopped(minutes)`: preenche `actualMinutesControl` apenas se ainda não preenchido manualmente

- [ ] **Task 4** — Parar timer na navegação (AC9)
  - [ ] Usar `DestroyRef` ou `takeUntilDestroyed()` para limpar interval
  - [ ] `ngOnDestroy` garante que interval é cancelado

- [ ] **Task 5** — Testes Vitest (AC10)
  - [ ] `study-timer.component.spec.ts`
  - [ ] Testa: start → elapsedMs aumenta; pause → para; resume → continua; stop → emite minutos corretos
  - [ ] Mock de `setInterval` com `vi.useFakeTimers()`

---

## Dev Notes

### Estrutura do componente

```typescript
// frontend/src/app/shared/components/study-timer/study-timer.component.ts
@Component({
  selector: 'app-study-timer',
  standalone: true,
  imports: [CommonModule, ButtonModule],
  template: `...`
})
export class StudyTimerComponent implements OnDestroy {
  status: 'idle' | 'running' | 'paused' = 'idle';
  elapsedMs = 0;
  private intervalId?: ReturnType<typeof setInterval>;

  @Output() timerStopped = new EventEmitter<number>();

  start() {
    this.status = 'running';
    this.intervalId = setInterval(() => this.elapsedMs += 1000, 1000);
  }

  pause() {
    this.status = 'paused';
    clearInterval(this.intervalId);
  }

  resume() {
    this.start();
  }

  stop() {
    clearInterval(this.intervalId);
    this.status = 'idle';
    const minutes = Math.ceil(this.elapsedMs / 60000);
    this.timerStopped.emit(minutes);
    this.elapsedMs = 0;
  }

  get displayTime(): string {
    const totalSec = Math.floor(this.elapsedMs / 1000);
    const h = Math.floor(totalSec / 3600);
    const m = Math.floor((totalSec % 3600) / 60);
    const s = totalSec % 60;
    if (h > 0) return `${h}:${m.toString().padStart(2,'0')}:${s.toString().padStart(2,'0')}`;
    return `${m.toString().padStart(2,'0')}:${s.toString().padStart(2,'0')}`;
  }

  ngOnDestroy() {
    clearInterval(this.intervalId);
  }
}
```

### Integração no formulário de conclusão

```typescript
// No componente pai (agenda ou modal de sessão)
onTimerStopped(minutes: number) {
  const ctrl = this.completionForm.get('actual_minutes');
  if (!ctrl?.value) {  // não sobrescreve se já preenchido
    ctrl?.setValue(minutes);
  }
}
```

### PrimeNG Buttons

Usar `pButton` com ícones PrimeIcons:
- Start: `pi pi-play`
- Pause: `pi pi-pause`
- Resume: `pi pi-play`
- Stop: `pi pi-stop`

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled

---

## File List

| Arquivo | Ação |
|---------|------|
| `frontend/src/app/shared/components/study-timer/study-timer.component.ts` | Criar |
| `frontend/src/app/shared/components/study-timer/study-timer.component.html` | Criar |
| `frontend/src/app/shared/components/study-timer/study-timer.component.spec.ts` | Criar |
| `frontend/src/app/features/agenda/agenda.component.ts` | Modificar |
| `frontend/src/app/features/agenda/agenda.component.html` | Modificar |

---

## Dev Agent Record

### Agent Model Used
_a ser preenchido_

### Completion Notes
_a ser preenchido_

### Debug Log
_a ser preenchido_

### File List
_a ser preenchido_
